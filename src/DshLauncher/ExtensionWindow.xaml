<UserControl SnapsToDevicePixels="True" UseLayoutRounding="True" x:Class="DshLauncher.ExtensionWindow"
             xmlns:controls="clr-namespace:DshLauncher.Controls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{StaticResource UiFont}"
             Foreground="{StaticResource TextBrush}"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Loaded="Window_OnLoaded">
    <UserControl.Resources>
        <!-- 左侧「当前实例」卡里的工具条按钮：紧凑尺寸（Padding 9,4 + 12px 字号），
             避免 9 个按钮在大尺寸下换行到 3 行。
             MinHeight=0（主/危险变体）：PrimaryButton/DangerButton 自带 MinHeight=38，
             在 WrapPanel 里会把整行撑高，而同行的次级按钮因默认 VerticalAlignment=Stretch
             被拉到同样高（2026-09-18 用户反馈「插件矩阵 / 手动安装 Plugin 比旁边按钮大」）。
             清零后三种紧凑按钮都按内容自然高度渲染，彼此一致。 -->
        <Style x:Key="CompactToolbarButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="9,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,6,6" />
        </Style>
        <Style x:Key="CompactToolbarPrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="9,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,6,6" />
        </Style>
        <Style x:Key="CompactToolbarDangerButton" TargetType="Button" BasedOn="{StaticResource DangerButton}">
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="9,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,6,6" />
        </Style>
    </UserControl.Resources>
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <!-- 变更集 134：侧栏由固定 320 改为比例 + 上下限（窄窗口不再挤压右栏） -->
            <ColumnDefinition Width="0.26*" MinWidth="260" MaxWidth="380" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border x:Name="InstalledPanel"
                Grid.Column="0"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource LineBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource PanelPadding}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" MinHeight="96" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="当前实例" FontSize="18" FontWeight="SemiBold" />
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MaxWidth="190" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="CurrentInstanceNameText"
                                   Foreground="{StaticResource BlueBrush}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Bottom" />
                        <TextBlock x:Name="CurrentInstanceMetaText"
                                   Grid.Column="1"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   VerticalAlignment="Bottom"
                                   Margin="8,0,0,0" />
                    </Grid>
                    <StackPanel Margin="0,4,0,0">
                        <Button AutomationProperties.Name="当前实例安装目录（点击复制）" x:Name="CurrentInstanceRootPathLink"
                                Style="{StaticResource PathLinkButton}"
                                Click="PathLink_Click" />
                        <Button AutomationProperties.Name="当前实例 DSH_HOME（点击复制）" x:Name="CurrentInstanceDshHomeLink"
                                Style="{StaticResource PathLinkButton}"
                                Click="PathLink_Click"
                                Margin="0,3,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,14,0,8">
                    <WrapPanel>
                    <Button Content="刷新" Click="Refresh_Click" Style="{StaticResource CompactToolbarButton}" />
                    <Button x:Name="CheckUpdatesButton"
                            Content="检查更新"
                            Click="CheckUpdates_Click"
                            Style="{StaticResource CompactToolbarButton}"
                            ToolTip="对比已安装插件的真实版本与 registry latest" />
                    <Button x:Name="UpdateAllButton"
                            Content="全部更新"
                            Click="UpdateAll_Click"
                            Style="{StaticResource CompactToolbarPrimaryButton}"
                            Visibility="Collapsed" />
                    <Button x:Name="DoctorButton"
                            Content="依赖自检"
                            Click="Doctor_Click"
                            Style="{StaticResource CompactToolbarButton}"
                            ToolTip="检测 profile 中混入的核心包/缺失 bundle" />
                    <Button x:Name="PluginMatrixButton"
                            Content="插件矩阵"
                            Click="PluginMatrix_Click"
                            Style="{StaticResource CompactToolbarButton}"
                            ToolTip="对比同一插件在各实例的安装/启用状态（只读）" />
                    <Button x:Name="InstallPluginButton"
                            Content="手动安装 Plugin"
                            Click="InstallPlugin_Click"
                            Style="{StaticResource CompactToolbarPrimaryButton}" />
                    <Button x:Name="ImportSkillButton"
                            Content="导入 Skill"
                            Click="ImportSkill_Click"
                            Style="{StaticResource CompactToolbarButton}" />
                    <Button x:Name="AddMcpButton"
                            Content="添加 MCP"
                            Click="AddMcp_Click"
                            Style="{StaticResource CompactToolbarButton}" />
                    <Button x:Name="ImportPresetButton"
                            Content="导入 Agent Preset"
                            Click="ImportPreset_Click"
                            Style="{StaticResource CompactToolbarButton}" />
                    </WrapPanel>
                    <CheckBox x:Name="DshMarketHotReloadCheckBox"
                              Content="使用 dsh-market 热加载主题"
                              Checked="DshMarketHotReloadCheckBox_Changed"
                              Unchecked="DshMarketHotReloadCheckBox_Changed"
                              VerticalAlignment="Center"
                              Margin="2,2,0,0" />
                </StackPanel>

                <Border Grid.Row="2"
                        Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource LineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource PanelCornerRadius}"
                        Padding="8"
                        ClipToBounds="True">
                    <ListBox x:Name="ExtensionList"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{StaticResource TextBrush}"
                             SelectionChanged="ExtensionList_SelectionChanged"
                             KeyDown="ExtensionList_KeyDown"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             VirtualizingStackPanel.IsVirtualizing="True"
                             VirtualizingStackPanel.VirtualizationMode="Recycling"
                             ScrollViewer.CanContentScroll="True">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10"
                                        Margin="0,0,0,6"
                                        Background="{StaticResource CardBrush}"
                                        BorderBrush="{StaticResource LineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="{StaticResource ItemCornerRadius}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="62" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Kind}"
                                                   Foreground="{StaticResource BlueBrush}"
                                                   FontSize="11"
                                                   VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Stretch"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding Name}" />
                                            <TextBlock Text="{Binding Description, TargetNullValue=没有描述}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="11"
                                                       HorizontalAlignment="Stretch"
                                                       TextWrapping="NoWrap"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding Description}"
                                                       Margin="0,3,0,0" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2"
                                                   FontSize="11"
                                                   VerticalAlignment="Center"
                                                   Margin="8,0,2,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="已禁用" />
                                                    <Setter Property="Foreground" Value="{StaticResource DangerTextBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Enabled}" Value="True">
                                                            <Setter Property="Text" Value="已启用" />
                                                            <Setter Property="Foreground" Value="{StaticResource SuccessTextBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <Border Grid.Row="3"
                        Background="{StaticResource InfoBackgroundBrush}"
                        CornerRadius="{StaticResource PanelCornerRadius}"
                        Padding="10"
                        Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="当前选择" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock x:Name="SelectedName"
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,0" />
                        <TextBlock x:Name="SelectedMeta"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   Margin="0,5,0,0" />
                        <TextBlock x:Name="SelectedDescription"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,3,0,0" />
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="来源："
                                       Foreground="{StaticResource MutedBrush}"
                                       FontSize="11"
                                       VerticalAlignment="Center" />
                            <Button AutomationProperties.Name="所选位置路径（点击复制）" x:Name="SelectedLocationLink"
                                    Style="{StaticResource PathLinkButton}"
                                    Click="PathLink_Click" />
                        </StackPanel>
                        <WrapPanel Margin="0,10,0,0">
                            <Button Style="{StaticResource CompactToolbarButton}" x:Name="EnableButton" Content="启用" Click="Enable_Click" />
                            <Button Style="{StaticResource CompactToolbarButton}" x:Name="DisableButton" Content="禁用" Click="Disable_Click" />
                            <Button Style="{StaticResource CompactToolbarButton}" x:Name="UpdateButton" Content="更新" Click="Update_Click" />
                            <Button Style="{StaticResource CompactToolbarDangerButton}" x:Name="RemoveButton" Content="删除" Click="Remove_Click" />
                        </WrapPanel>
                        <TextBlock x:Name="HintText"
                                   Text="修改前请停止实例。Plugin 和 MCP 会在下次启动时生效。"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="4"
                           x:Name="StatusText"
                           Foreground="{StaticResource BlueBrush}"
                           TextWrapping="Wrap"
                           Margin="0,10,0,0" />
            </Grid>
        </Border>

        <Border x:Name="MarketplacePanel"
                Grid.Column="2"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource LineBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource CardPadding}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="MarketplaceSearchBox"
                             Height="38"
                             Padding="12,8"
                             VerticalContentAlignment="Center"
                             ToolTip="输入插件名称、包名或关键词"
                             TextChanged="MarketplaceSearchBox_TextChanged" />
                    <Button Grid.Column="1"
                            x:Name="MarketplaceRefreshButton"
                            Content="刷新目录"
                            Click="MarketplaceRefresh_Click"
                            Margin="8,0,0,0" />
                </Grid>

                <Grid Grid.Row="1" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="分类"
                                   Foreground="{StaticResource MutedBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ListBox Grid.Column="1"
                                 x:Name="MarketplaceCategoryList"
                                 Height="34"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 SelectedIndex="0"
                                 SelectionChanged="MarketplaceFilter_SelectionChanged" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" >
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="10,6" />
                                    <Setter Property="Margin" Value="0,0,4,0" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource HighlightSurfaceBrush}" />
                                            <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBoxItem Tag="">全部</ListBoxItem>
                            <ListBoxItem Tag="__featured__">精选</ListBoxItem>
                            <ListBoxItem Tag="UI">UI</ListBoxItem>
                            <ListBoxItem Tag="工具">工具</ListBoxItem>
                            <ListBoxItem Tag="工作流">工作流</ListBoxItem>
                            <ListBoxItem Tag="Agent">Agent</ListBoxItem>
                            <ListBoxItem Tag="模型">模型</ListBoxItem>
                            <ListBoxItem Tag="主题">主题</ListBoxItem>
                            <ListBoxItem Tag="开发">开发</ListBoxItem>
                            <ListBoxItem Tag="未分类">未分类</ListBoxItem>
                        </ListBox>
                    </Grid>
                    <WrapPanel Grid.Row="1" Margin="0,8,0,0" VerticalAlignment="Center">
                        <TextBlock Text="排序"
                                   Foreground="{StaticResource MutedBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ComboBox x:Name="MarketplaceSortBox"
                                  MinWidth="148"
                                  Height="38"
                                  SelectedIndex="0"
                                  ToolTip="排序方式"
                                  SelectionChanged="MarketplaceFilter_SelectionChanged">
                            <ComboBoxItem Tag="Relevance">综合排序</ComboBoxItem>
                            <ComboBoxItem Tag="PublishedAt">最新发布</ComboBoxItem>
                            <ComboBoxItem Tag="Stars">热门（Star）</ComboBoxItem>
                        </ComboBox>
                        <TextBlock Text="来源"
                                   Foreground="{StaticResource MutedBrush}"
                                   VerticalAlignment="Center"
                                   Margin="18,0,8,0" />
                        <ComboBox x:Name="MarketplaceSourceBox"
                                  MinWidth="168"
                                  Height="38"
                                  SelectedIndex="0"
                                  ToolTip="次级来源筛选"
                                  SelectionChanged="MarketplaceFilter_SelectionChanged">
                            <ComboBoxItem Tag="">全部来源</ComboBoxItem>
                            <ComboBoxItem Tag="CommunityCatalog">GitHub</ComboBoxItem>
                        </ComboBox>
                    </WrapPanel>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,14,0,12">
                    <Grid>
                        <TextBlock Text="插件市场" FontSize="20" FontWeight="SemiBold" />
                        <Button
                                AutomationProperties.Name="打开中文官网"
                                Click="MarketplaceSite_Click"
                                Padding="10,4"
                                HorizontalAlignment="Right"
                                ToolTip="打开插件官方（中文）网站"><StackPanel Orientation="Horizontal"><TextBlock Text="中文官网" /><controls:UiIcon Kind="ExternalLink" Size="16" Margin="{StaticResource IconTextGapTrailing}" /></StackPanel></Button>
                    </Grid>
                    <TextBlock Text="输入时直接在本地目录中筛选；只有点击“刷新目录”才会联网。安装前会再次检查 package.json 和 DSh bundle。"
                               Foreground="{StaticResource MutedBrush}"
                               FontSize="11"
                               Margin="0,4,0,0" />
                    <TextBlock x:Name="MarketplaceSummaryText"
                               Foreground="{StaticResource BlueBrush}"
                               FontSize="11"
                               Margin="0,4,0,0" />
                </StackPanel>

                <Border Grid.Row="3"
                        Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource LineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource PanelCornerRadius}"
                        Padding="8"
                        ClipToBounds="True">
                    <ListBox x:Name="MarketplaceList"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{StaticResource TextBrush}"
                             SelectionChanged="MarketplaceList_SelectionChanged"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             VirtualizingStackPanel.IsVirtualizing="True"
                             VirtualizingStackPanel.VirtualizationMode="Recycling"
                             ScrollViewer.CanContentScroll="True">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <!-- 卡片强制占满一行宽度：WPF ListBoxItem 默认 HorizontalContentAlignment=Left，
                                     内容少时卡片会按内容收缩、右侧留空；Stretch 后卡片随列表宽度拉伸。 -->
                                <!-- 变更集 143（用户反馈）：**条目宽度绑定到列表宽度**。
                                             原因：ListBox 在 ScrollViewer.CanContentScroll=True 下，虚拟化面板以**无限宽度**测量条目，带 TextWrapping 的长描述会把条目的"想要宽度"撑到视口之外，Stretch 也拉不回来 → 右侧操作按钮被挤出窗口（Skill 市场实测：列表右边 1406，按钮却在 x=1554）。
                                             绑定后条目宽度有限，长描述正常换行、按钮留在可视区。 -->
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Width" Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="14"
                                        Margin="0,0,0,10"
                                        Background="{StaticResource CardBrush}"
                                        BorderBrush="{StaticResource LineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="{StaticResource ItemCornerRadius}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Border Width="36"
                                                Height="36"
                                                Background="{StaticResource HighlightSurfaceBrush}"
                                                CornerRadius="{StaticResource ItemCornerRadius}"
                                                VerticalAlignment="Top">
                                            <Grid>
                                                <TextBlock Text="P"
                                                           Foreground="{StaticResource BlueBrush}"
                                                           FontWeight="Bold"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" />
                                                <Image Source="{Binding DeveloperAvatarUrl}"
                                                       Width="36"
                                                       Height="36"
                                                       Stretch="UniformToFill" />
                                            </Grid>
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="12,0,16,0">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource BlueBrush}"
                                                       TextDecorations="Underline"
                                                       Cursor="Hand"
                                                       ToolTip="打开插件 GitHub 仓库"
                                                       MouseLeftButtonUp="MarketplaceTitle_MouseLeftButtonUp"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding TargetText}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="12"
                                                       Margin="0,3,0,0"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Description}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="12"
                                                       Margin="0,4,0,0"
                                                       TextWrapping="Wrap"
                                                       MaxHeight="34"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding Description}" />
                                            <WrapPanel Margin="0,6,0,0">
                                                <TextBlock Text="{Binding SourceText}" Foreground="{StaticResource BlueBrush}" FontSize="11" Margin="0,0,12,0" />
                                                <TextBlock Text="{Binding Category}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,12,0" />
                                                <TextBlock Text="{Binding VersionStatusText}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,12,0" />
                                                <TextBlock Text="{Binding VerificationText}" Foreground="{StaticResource SuccessTextBrush}" FontSize="11" Margin="0,0,12,0" />
                                                <TextBlock Text="{Binding StarsText}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,12,0" />
                                                <TextBlock Text="{Binding PublishedText}" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                                            </WrapPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <Button Content="预览"
                                                    Click="MarketplaceThemePreview_Click"
                                                    Padding="12,6"
                                                    Margin="0,0,0,7">
                                                <Button.Style>
                                                    <!-- BasedOn 继承启动器隐式按钮样式（圆角等），与卸载/其他按钮一致 -->
                                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsTheme}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            <Button Content="{Binding ActionText}"
                                                    Click="MarketplaceAction_Click"
                                                    IsEnabled="{Binding CanAction}"
                                                    Padding="12,6"
                                                    Style="{StaticResource PrimaryButton}" />
                                            <Button Style="{StaticResource DangerButton}" Content="卸载"
                                                     Click="MarketplaceRemove_Click"
                                                     IsEnabled="{Binding CanRemove}"
                                                     Padding="12,6"
                                                     Margin="0,7,0,0" />
                                            <TextBlock Text="{Binding ThemeStatusText}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="12"
                                                       TextWrapping="Wrap"
                                                       Margin="0,7,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsTheme}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                        
                </Border>

                <StackPanel Grid.Row="4"
                            x:Name="MarketplaceProgressPanel"
                            Visibility="Collapsed"
                            Margin="0,10,0,0">
                    <TextBlock x:Name="MarketplaceProgressText"
                               Foreground="{StaticResource BlueBrush}"
                               TextWrapping="Wrap" />
                    <ProgressBar x:Name="MarketplaceProgressBar"
                                 Height="7"
                                 Margin="0,6,0,0"
                                 IsIndeterminate="True" />
                </StackPanel>

                <TextBlock Grid.Row="5"
                           x:Name="MarketplaceStatusText"
                           Foreground="{StaticResource MutedBrush}"
                           TextWrapping="Wrap"
                           Margin="0,10,0,0" />
            </Grid>
        </Border>

        <Grid Grid.Column="2">
            <!-- 阴影单独一层：避免 DropShadowEffect 把列表文字一起栅格化 -->
            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource LineBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource CardCornerRadius}"
                    Effect="{StaticResource CardShadowSoft}"
                    Visibility="{Binding Visibility, ElementName=SkillMarketPanel}" />
            <Border x:Name="SkillMarketPanel"
                    Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource LineBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource CardCornerRadius}"
                    Padding="{StaticResource CardPadding}"
                    ClipToBounds="True"
                    Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SkillMarketSearchBox"
                             Height="38"
                             Padding="12,8"
                             VerticalContentAlignment="Center"
                             ToolTip="按 Skill 名称、仓库或描述筛选"
                             TextChanged="SkillMarketSearchBox_TextChanged" />
                    <Button Grid.Column="1"
                            x:Name="SkillMarketRefreshButton"
                            Content="刷新目录"
                            Click="SkillMarketRefresh_Click"
                            Margin="8,0,0,0" />
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,14,0,12">
                    <TextBlock Text="Skill 市场" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="从 GitHub 仓库中发现并校验单个 Skill；安装会导入所选 Skill 目录及配套文件（需实例已停止）。内容为 GitHub 上的通用 Agent Skill 库，与 DSH 生态无绑定；同一仓库的同名 Skill 只保留一个，每个仓库最多收录 20 个。"
                               Foreground="{StaticResource MutedBrush}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="0,4,0,0" />
                </StackPanel>

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,0,0,12">
                    <TextBlock Text="分类"
                               Foreground="{StaticResource MutedBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0" />
                    <ListBox x:Name="SkillMarketCategoryList"
                             Height="34"
                             BorderThickness="0"
                             Background="Transparent"
                             SelectedIndex="0"
                             SelectionChanged="SkillMarketCategoryList_SelectionChanged" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" >
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="10,6" />
                                <Setter Property="Margin" Value="0,0,4,0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource HighlightSurfaceBrush}" />
                                        <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBoxItem Tag="">全部</ListBoxItem>
                        <ListBoxItem Tag="开发">开发</ListBoxItem>
                        <ListBoxItem Tag="设计">设计</ListBoxItem>
                        <ListBoxItem Tag="文档">文档</ListBoxItem>
                        <ListBoxItem Tag="效率">效率</ListBoxItem>
                        <ListBoxItem Tag="Agent">Agent</ListBoxItem>
                        <ListBoxItem Tag="其他">其他</ListBoxItem>
                    </ListBox>
                    <TextBlock Text="排序"
                               Foreground="{StaticResource MutedBrush}"
                               VerticalAlignment="Center"
                               Margin="18,0,8,0" />
                    <ComboBox x:Name="SkillMarketSortBox"
                              Width="132"
                              Height="34"
                              SelectedIndex="0"
                              ToolTip="排序方式"
                              SelectionChanged="SkillMarketFilter_SelectionChanged">
                        <ComboBoxItem Tag="Relevance">综合排序</ComboBoxItem>
                        <ComboBoxItem Tag="Stars">热门（Star）</ComboBoxItem>
                        <ComboBoxItem Tag="UpdatedAt">最近更新</ComboBoxItem>
                    </ComboBox>
                    <TextBlock Text="来源"
                               Foreground="{StaticResource MutedBrush}"
                               VerticalAlignment="Center"
                               Margin="18,0,8,0" />
                    <ComboBox x:Name="SkillMarketSourceBox"
                              MinWidth="180"
                              Height="34"
                              SelectedIndex="0"
                              ToolTip="按技能所在仓库筛选；“全部来源”旁的数字是当前列表的技能总数。仓库来自内置 GitHub 搜索，也可在“设置 / 诊断 → 市场源”里补充配置源"
                              SelectionChanged="SkillMarketFilter_SelectionChanged" />
                </StackPanel>

                <ListBox Grid.Row="3"
                         x:Name="SkillMarketList"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingStackPanel.IsVirtualizing="True"
                         VirtualizingStackPanel.VirtualizationMode="Recycling"
                         ScrollViewer.CanContentScroll="True">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <!-- 变更集 143（用户反馈）：**条目宽度绑定到列表宽度**。
                                             原因：ListBox 在 ScrollViewer.CanContentScroll=True 下，虚拟化面板以**无限宽度**测量条目，带 TextWrapping 的长描述会把条目的"想要宽度"撑到视口之外，Stretch 也拉不回来 → 右侧操作按钮被挤出窗口（Skill 市场实测：列表右边 1406，按钮却在 x=1554）。
                                             绑定后条目宽度有限，长描述正常换行、按钮留在可视区。 -->
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Width" Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <!-- 变更集 143（用户反馈）：卡片形态统一为**插件市场卡片**的形态——
                             CardBrush 底 + ItemCornerRadius + Padding 14 / Margin 0,0,0,10 + 36px 头像列
                             + 12px 描述 + 11px 元信息行 + 主按钮 PrimaryButton(12,6)。
                             差异：Skill 条目模型没有仓库 URL，故标题不做可点击链接（不造假链）。 -->
                        <DataTemplate>
                            <Border Padding="14"
                                    Margin="0,0,0,10"
                                    Background="{StaticResource CardBrush}"
                                    BorderBrush="{StaticResource LineBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource ItemCornerRadius}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Width="36"
                                            Height="36"
                                            Background="{StaticResource HighlightSurfaceBrush}"
                                            CornerRadius="{StaticResource ItemCornerRadius}"
                                            VerticalAlignment="Top">
                                        <TextBlock Text="S"
                                                   Foreground="{StaticResource BlueBrush}"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="12,0,16,0">
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding Name}" />
                                        <TextBlock Text="{Binding Repository}"
                                                   Foreground="{StaticResource MutedBrush}"
                                                   FontSize="12"
                                                   Margin="0,3,0,0"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Description, TargetNullValue=没有描述}"
                                                   Foreground="{StaticResource MutedBrush}"
                                                   FontSize="12"
                                                   Margin="0,4,0,0"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="34"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding Description}" />
                                        <WrapPanel Margin="0,6,0,0">
                                            <TextBlock Text="{Binding StarsText}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="11"
                                                       Margin="0,0,12,0" />
                                            <TextBlock Text="{Binding StatusText}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="11" />
                                        </WrapPanel>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <Button Content="{Binding ActionText}"
                                                Click="SkillInstall_Click"
                                                IsEnabled="{Binding CanInstall}"
                                                Padding="12,6"
                                                Style="{StaticResource PrimaryButton}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Grid.Row="4"
                           x:Name="SkillMarketStatusText"
                           Foreground="{StaticResource MutedBrush}"
                           TextWrapping="Wrap"
                           Margin="0,10,0,0" />
            </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
