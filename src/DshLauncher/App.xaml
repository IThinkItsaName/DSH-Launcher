<Application x:Class="DshLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <FontFamily x:Key="UiFont">Segoe UI, Microsoft YaHei UI</FontFamily>
        <!-- 蓝系：PCL 式 10 级渐变体系（#0b5bcb→#1370f3→#4890f5→…→#eaf2fe） -->
        <SolidColorBrush x:Key="BlueBrush" Color="#1370F3" />
        <SolidColorBrush x:Key="BlueDarkBrush" Color="#0B5BCB" />
        <SolidColorBrush x:Key="PageBrush" Color="#EAF2FE" />
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TextBrush" Color="#343D4A" />
        <SolidColorBrush x:Key="MutedBrush" Color="#8C8C8C" />
        <SolidColorBrush x:Key="LineBrush" Color="#D5E6FD" />
        <SolidColorBrush x:Key="GreenBrush" Color="#2EA66B" />

        <!-- 语义色（开发历史中散落的硬编码收敛于此）：状态/危险/警告/信息底 -->
        <SolidColorBrush x:Key="SuccessTextBrush" Color="#25875A" />
        <SolidColorBrush x:Key="DangerTextBrush" Color="#A25A54" />
        <SolidColorBrush x:Key="DangerBrush" Color="#B42318" />
        <SolidColorBrush x:Key="DangerStrongBrush" Color="#8F1C12" />   <!-- 危险操作悬停时的描边加深（变更集 140） -->
        <SolidColorBrush x:Key="WarningBrush" Color="#F1C26B" />
        <SolidColorBrush x:Key="WarningTextBrush" Color="#8A6D1B" />
        <SolidColorBrush x:Key="WarningBackgroundBrush" Color="#FFF9EA" />
        <SolidColorBrush x:Key="InfoBackgroundBrush" Color="#F5F9FD" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#F8FBFE" />
        <SolidColorBrush x:Key="HoverSurfaceBrush" Color="#F7FAFD" />
        <SolidColorBrush x:Key="MenuHoverBrush" Color="#E0EAFD" />   <!-- 菜单项悬停底（变更集 131） -->
        <!-- 变更集 132（A2 颜色令牌化收尾）：样式/模板里原以字面量写死的颜色 -->
        <Color x:Key="BlueColor">#1370F3</Color>
        <Color x:Key="BlueDarkColor">#0B5BCB</Color>
        <Color x:Key="ShadowColor">#3E5C7A</Color>
        <SolidColorBrush x:Key="OnBrandTextBrush" Color="#FFFFFF" />        <!-- 品牌蓝底上的文字/描边 -->
        <SolidColorBrush x:Key="ControlBorderBrush" Color="#C5D5E6" />      <!-- 输入类控件描边 -->
        <SolidColorBrush x:Key="SubtleSurfaceBrush" Color="#E7F1FC" />      <!-- 次级浅蓝面（禁用/选中/进度底） -->
        <SolidColorBrush x:Key="SelectionBrush" Color="#B8DCF8" />          <!-- 文本选择高亮 -->
        <SolidColorBrush x:Key="ScrollThumbBrush" Color="#C8D8EA" />        <!-- 滚动条滑块（常态/悬停/拖动） -->
        <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#96C0F9" />
        <SolidColorBrush x:Key="ScrollThumbDragBrush" Color="#4890F5" />
        <SolidColorBrush x:Key="StatusOkSoftBrush" Color="#342EA66B" />     <!-- 状态胶囊底（≈15% 绿/红/灰） -->
        <SolidColorBrush x:Key="StatusErrorSoftBrush" Color="#34D94A4A" />
        <SolidColorBrush x:Key="StatusIdleSoftBrush" Color="#2C96A3B5" />
        <SolidColorBrush x:Key="StatusOkTextBrush" Color="#1F7A50" />       <!-- 状态胶囊文字（深色调） -->
        <SolidColorBrush x:Key="StatusIdleTextBrush" Color="#606B7A" />
        <SolidColorBrush x:Key="AccentPurpleBrush" Color="#6A3D9A" />       <!-- TUI/home 来源标签 -->

        <!-- 标题栏 chrome（原 MainWindow 里散落的白色叠加） -->
        <SolidColorBrush x:Key="TitleBarChromeBrush" Color="#55FFFFFF" />
        <SolidColorBrush x:Key="TitleBarChromeBorderBrush" Color="#80FFFFFF" />
        <SolidColorBrush x:Key="TitleBarChromeHoverBrush" Color="#80FFFFFF" />
        <SolidColorBrush x:Key="TitleBarChromePressedBrush" Color="#B3FFFFFF" />
        <SolidColorBrush x:Key="TitleBarCloseHoverBrush" Color="#CE2111" />

        <!-- 语义面（原散落各页的浅色底/描边） -->
        <SolidColorBrush x:Key="DangerSurfaceBrush" Color="#FEF3F2" />
        <SolidColorBrush x:Key="SuccessSurfaceBrush" Color="#E7F6EC" />
        <SolidColorBrush x:Key="SuccessBorderBrush" Color="#2E7D32" />
        <SolidColorBrush x:Key="InfoSurfaceBrush" Color="#F4F8FC" />
        <SolidColorBrush x:Key="HighlightSurfaceBrush" Color="#E3F0FD" />
        <SolidColorBrush x:Key="NeutralSurfaceBrush" Color="#F4F5F7" />
        <SolidColorBrush x:Key="StatusIdleBrush" Color="#96A3B5" />

        <!-- 空态插画（MainWindow 无实例引导；GradientStop 需要 Color 类型） -->
        <Color x:Key="EmptyStateGradientStartColor">#4890F5</Color>
        <Color x:Key="EmptyStateGradientEndColor">#96C0F9</Color>
        <SolidColorBrush x:Key="EmptyStateAccentBrush" Color="#7FB2F9" />
        <SolidColorBrush x:Key="EmptyStateAccentSoftBrush" Color="#C6DCFC" />

        <!-- 圆角/间距 token（规范见 docs/UI-DESIGN.md） -->
        <CornerRadius x:Key="CardCornerRadius">12</CornerRadius>
        <CornerRadius x:Key="PanelCornerRadius">10</CornerRadius>
        <CornerRadius x:Key="ItemCornerRadius">8</CornerRadius>
        <Thickness x:Key="CardPadding">18</Thickness>
        <Thickness x:Key="PanelPadding">16</Thickness>
        <Thickness x:Key="ItemMargin">0,0,0,8</Thickness>
        <Thickness x:Key="IconTextGap">0,0,8,0</Thickness>   <!-- 图标在文字**之前**时的间距（变更集 130） -->
        <Thickness x:Key="IconTextGapTrailing">8,0,0,0</Thickness>   <!-- 图标在文字**之后**时的间距（变更集 148，如「中文官网 ↗」） -->
        <LinearGradientBrush x:Key="TitleBarGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="{StaticResource BlueDarkColor}" Offset="0" />
            <GradientStop Color="{StaticResource BlueColor}" Offset="1" />
        </LinearGradientBrush>

        <DropShadowEffect x:Key="CardShadow" Color="{StaticResource ShadowColor}" BlurRadius="16" ShadowDepth="3" Direction="270" Opacity="0.14" />
        <DropShadowEffect x:Key="CardShadowSoft" Color="{StaticResource ShadowColor}" BlurRadius="10" ShadowDepth="1" Direction="270" Opacity="0.08" />

        <!-- 文本渲染：Display/ClearType（WPF 默认 Ideal 在高 DPI 下发虚）；
             布局取整避免亚像素错位。应用到所有 Window 与嵌入 UserControl。 -->
        <Style TargetType="{x:Type Window}">
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
        </Style>
        <Style TargetType="{x:Type UserControl}">
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
            <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
        </Style>

        <!-- PCL 式细滚动条：无箭头、圆角 thumb、透明轨道（全局生效） -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Opacity="0" Focusable="False" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Opacity="0" Focusable="False" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="ThumbChrome" Background="{StaticResource ScrollThumbBrush}" CornerRadius="5" Margin="2" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbChrome" Property="Background" Value="{StaticResource ScrollThumbHoverBrush}" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbChrome" Property="Background" Value="{StaticResource ScrollThumbDragBrush}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                                <Setter Property="Width" Value="Auto" />
                                <Setter Property="Height" Value="10" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ContextInstanceMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MaxHeight" Value="410" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{StaticResource CardBrush}"
                                BorderBrush="{StaticResource LineBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="6"
                                ClipToBounds="True">
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled"
                                          CanContentScroll="True">
                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 菜单分隔线：默认模板不吃 Background/Margin（实测会出现"白线左右顶满"），
             所以自带模板自绘 1px 线；做成 keyed 样式以便在弹出层里显式引用。 -->
        <Style x:Key="MenuSeparatorStyle" TargetType="Separator">
            <Setter Property="Margin" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1"
                                Background="{StaticResource LineBrush}"
                                SnapsToDevicePixels="True" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Separator" BasedOn="{StaticResource MenuSeparatorStyle}" />
        <Style x:Key="ContextInstanceMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="ItemChrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,1">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="{StaticResource MenuHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="{StaticResource PageBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                <Setter TargetName="ItemChrome" Property="Background" Value="Transparent" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 危险菜单项（变更集 131）：只把前景改为危险色，悬停底仍用中性 MenuHoverBrush -->
        <Style x:Key="MenuItemDangerStyle" TargetType="MenuItem" BasedOn="{StaticResource ContextInstanceMenuItemStyle}">
            <Setter Property="Foreground" Value="{StaticResource DangerTextBrush}" />
        </Style>
        <!-- 隐式样式：所有菜单项默认走统一外观（变更集 131；此前 MainWindow 分体菜单未挂样式→系统默认外观） -->
        <Style TargetType="MenuItem" BasedOn="{StaticResource ContextInstanceMenuItemStyle}" />
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,9" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource SubtleSurfaceBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
            <Setter Property="SelectionBrush" Value="{StaticResource SelectionBrush}" />
            <Setter Property="SelectionTextBrush" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBoxChrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          Foreground="{TemplateBinding Foreground}"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="TextBoxChrome" Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboBoxHitTarget" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Chrome"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" />
                            <ContentPresenter x:Name="SelectionPresenter"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Stretch"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                            <TextBox x:Name="PART_EditableTextBox"
                                     Margin="{TemplateBinding Padding}"
                                     Padding="0"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Stretch"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Foreground="{TemplateBinding Foreground}"
                                     CaretBrush="{TemplateBinding Foreground}"
                                     Focusable="True"
                                     Visibility="Collapsed" />
                            <Path Grid.Column="1"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="{StaticResource MutedBrush}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"
                                  IsHitTestVisible="False" />
                            <ToggleButton x:Name="ToggleFull"
                                          Style="{StaticResource ComboBoxHitTarget}"
                                          Grid.ColumnSpan="2"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ToggleButton x:Name="ToggleArrow"
                                          Style="{StaticResource ComboBoxHitTarget}"
                                          Grid.Column="1"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Visibility="Collapsed"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   PopupAnimation="Slide">
                                <Border Background="{StaticResource CardBrush}"
                                        BorderBrush="{StaticResource LineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,4,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="SelectionPresenter" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ToggleFull" Property="IsHitTestVisible" Value="False" />
                                <Setter TargetName="ToggleArrow" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemChrome"
                                Background="Transparent"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                Margin="4,1">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="{StaticResource HighlightSurfaceBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="{StaticResource SubtleSurfaceBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource HoverSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabChrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="9"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabChrome" Property="Background" Value="{StaticResource BlueBrush}" />
                                <Setter TargetName="TabChrome" Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource OnBrandTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabChrome" Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 变更集 138（UI 统一 I）：统一键盘焦点框——虚线描边 + 2px 外扩，深色主题下也可见 -->
        <Style x:Key="AppFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="1"
                                   Stroke="{StaticResource BlueBrush}"
                                   StrokeThickness="2"
                                   StrokeDashArray="1 1"
                                   SnapsToDevicePixels="True" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ============================================================================
             图标几何注册表（变更集 147）。
             来源：Tabler Icons v3.46.0（MIT License，Copyright (c) 2020-2026 Paweł Kuna）
             出处/版本/许可全文：docs/THIRD-PARTY-NOTICES.md
             用法：<controls:UiIcon Kind="Play" Size="18" />（一律走 UiIcon，不在 XAML 里写裸字形）
             几何坐标系：24 x 24 描边路径（stroke-width 2）；笔画粗细由 UiIcon 按 Size/12 设置。
             注意：Tabler 图标带一条 24x24 画布边框路径（M0 0h24v24H0z，用于对齐）；本注册表已剔除，
             否则描边后会画出多余方框（2026-09-13 识图发现后修正）。
             ============================================================================ -->
        <GeometryGroup x:Key="Icon.Activity">
            <PathGeometry Figures="M3 12h4l3 8l4 -16l3 8h4" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Agent">
            <PathGeometry Figures="M16 18a2 2 0 0 1 2 2a2 2 0 0 1 2 -2a2 2 0 0 1 -2 -2a2 2 0 0 1 -2 2m0 -12a2 2 0 0 1 2 2a2 2 0 0 1 2 -2a2 2 0 0 1 -2 -2a2 2 0 0 1 -2 2m-7 12a6 6 0 0 1 6 -6a6 6 0 0 1 -6 -6a6 6 0 0 1 -6 6a6 6 0 0 1 6 6" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.ArrowLeft">
            <PathGeometry Figures="M5 12l14 0" />
            <PathGeometry Figures="M5 12l6 6" />
            <PathGeometry Figures="M5 12l6 -6" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.ChevronDown">
            <PathGeometry Figures="M6 9l6 6l6 -6" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Close">
            <PathGeometry Figures="M18 6l-12 12" />
            <PathGeometry Figures="M6 6l12 12" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Conversations">
            <PathGeometry Figures="M21 14l-3 -3h-7a1 1 0 0 1 -1 -1v-6a1 1 0 0 1 1 -1h9a1 1 0 0 1 1 1v10" />
            <PathGeometry Figures="M14 15v2a1 1 0 0 1 -1 1h-7l-3 3v-10a1 1 0 0 1 1 -1h2" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Export">
            <PathGeometry Figures="M12 21l-8 -4.5v-9l8 -4.5l8 4.5v4.5" />
            <PathGeometry Figures="M12 12l8 -4.5" />
            <PathGeometry Figures="M12 12v9" />
            <PathGeometry Figures="M12 12l-8 -4.5" />
            <PathGeometry Figures="M15 18h7" />
            <PathGeometry Figures="M19 15l3 3l-3 3" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Extensions">
            <PathGeometry Figures="M4 7h3a1 1 0 0 0 1 -1v-1a2 2 0 0 1 4 0v1a1 1 0 0 0 1 1h3a1 1 0 0 1 1 1v3a1 1 0 0 0 1 1h1a2 2 0 0 1 0 4h-1a1 1 0 0 0 -1 1v3a1 1 0 0 1 -1 1h-3a1 1 0 0 1 -1 -1v-1a2 2 0 0 0 -4 0v1a1 1 0 0 1 -1 1h-3a1 1 0 0 1 -1 -1v-3a1 1 0 0 1 1 -1h1a2 2 0 0 0 0 -4h-1a1 1 0 0 1 -1 -1v-3a1 1 0 0 1 1 -1" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.ExternalLink">
            <PathGeometry Figures="M12 6h-6a2 2 0 0 0 -2 2v10a2 2 0 0 0 2 2h10a2 2 0 0 0 2 -2v-6" />
            <PathGeometry Figures="M11 13l9 -9" />
            <PathGeometry Figures="M15 4h5v5" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Matrix">
            <PathGeometry Figures="M4 5a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v4a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1l0 -4" />
            <PathGeometry Figures="M14 5a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v4a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1l0 -4" />
            <PathGeometry Figures="M4 15a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v4a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1l0 -4" />
            <PathGeometry Figures="M14 15a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v4a1 1 0 0 1 -1 1h-4a1 1 0 0 1 -1 -1l0 -4" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Maximize">
            <PathGeometry Figures="M3 5a2 2 0 0 1 2 -2h14a2 2 0 0 1 2 2v14a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-14" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Minimize">
            <PathGeometry Figures="M5 12l14 0" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Palette">
            <PathGeometry Figures="M12 21a9 9 0 0 1 0 -18c4.97 0 9 3.582 9 8c0 1.06 -.474 2.078 -1.318 2.828c-.844 .75 -1.989 1.172 -3.182 1.172h-2.5a2 2 0 0 0 -1 3.75a1.3 1.3 0 0 1 -1 2.25" />
            <PathGeometry Figures="M7.5 10.5a1 1 0 1 0 2 0a1 1 0 1 0 -2 0" />
            <PathGeometry Figures="M11.5 7.5a1 1 0 1 0 2 0a1 1 0 1 0 -2 0" />
            <PathGeometry Figures="M15.5 10.5a1 1 0 1 0 2 0a1 1 0 1 0 -2 0" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Play">
            <PathGeometry Figures="M7 4v16l13 -8l-13 -8" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Plugins">
            <PathGeometry Figures="M9.785 6l8.215 8.215l-2.054 2.054a5.81 5.81 0 1 1 -8.215 -8.215l2.054 -2.054" />
            <PathGeometry Figures="M4 20l3.5 -3.5" />
            <PathGeometry Figures="M15 4l-3.5 3.5" />
            <PathGeometry Figures="M20 9l-3.5 3.5" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Plus">
            <PathGeometry Figures="M12 5l0 14" />
            <PathGeometry Figures="M5 12l14 0" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Restore">
            <PathGeometry Figures="M7 9.667a2.667 2.667 0 0 1 2.667 -2.667h8.666a2.667 2.667 0 0 1 2.667 2.667v8.666a2.667 2.667 0 0 1 -2.667 2.667h-8.666a2.667 2.667 0 0 1 -2.667 -2.667l0 -8.666" />
            <PathGeometry Figures="M4.012 16.737a2.005 2.005 0 0 1 -1.012 -1.737v-10c0 -1.1 .9 -2 2 -2h10c.75 0 1.158 .385 1.5 1" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Settings">
            <PathGeometry Figures="M10.325 4.317c.426 -1.756 2.924 -1.756 3.35 0a1.724 1.724 0 0 0 2.573 1.066c1.543 -.94 3.31 .826 2.37 2.37a1.724 1.724 0 0 0 1.065 2.572c1.756 .426 1.756 2.924 0 3.35a1.724 1.724 0 0 0 -1.066 2.573c.94 1.543 -.826 3.31 -2.37 2.37a1.724 1.724 0 0 0 -2.572 1.065c-.426 1.756 -2.924 1.756 -3.35 0a1.724 1.724 0 0 0 -2.573 -1.066c-1.543 .94 -3.31 -.826 -2.37 -2.37a1.724 1.724 0 0 0 -1.065 -2.572c-1.756 -.426 -1.756 -2.924 0 -3.35a1.724 1.724 0 0 0 1.066 -2.573c-.94 -1.543 .826 -3.31 2.37 -2.37c1 .608 2.296 .07 2.572 -1.065" />
            <PathGeometry Figures="M9 12a3 3 0 1 0 6 0a3 3 0 0 0 -6 0" />
        </GeometryGroup>
        <GeometryGroup x:Key="Icon.Tasks">
            <PathGeometry Figures="M3.5 5.5l1.5 1.5l2.5 -2.5" />
            <PathGeometry Figures="M3.5 11.5l1.5 1.5l2.5 -2.5" />
            <PathGeometry Figures="M3.5 17.5l1.5 1.5l2.5 -2.5" />
            <PathGeometry Figures="M11 6l9 0" />
            <PathGeometry Figures="M11 12l9 0" />
            <PathGeometry Figures="M11 18l9 0" />
        </GeometryGroup>

        <!-- 变更集 147：图标幽灵按钮（只放一个 UiIcon，透明无边框）。用于提示条「关闭」这类位置。
             注意：必须定义在隐式 Button 样式（本文件上部）之后——StaticResource 要求先定义。 -->
        <Style x:Key="IconGhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Background" Value="{StaticResource BlueBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BlueBrush}" />
            <Setter Property="Foreground" Value="{StaticResource OnBrandTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BlueDarkBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource BlueDarkBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource OnBrandTextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Foreground" Value="{StaticResource DangerTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <!-- 变更集 140（用户反馈）：悬停只"加深描边"，不再整块变实心红（底色与文字保持不变） -->
                <!-- 变更集 141（用户澄清）：危险按钮悬停 = **红底白字（底色反转）**（变更集 140 误改成"只加深描边"，此处改回）；
                     按下再深一档（DangerStrongBrush）以保留按下反馈。 -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource OnBrandTextBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerStrongBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource DangerStrongBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource OnBrandTextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="NavButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="16,13" />
            <Setter Property="FontSize" Value="15" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource MenuHoverBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TopNavButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource OnBrandTextBrush}" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavChrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavChrome" Property="Background" Value="{StaticResource TitleBarChromeBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="NavChrome" Property="Background" Value="{StaticResource TitleBarChromePressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 复选框：圆角方框 + 蓝色对钩（默认 Aero2 样式与整体设计不一致） -->
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="Box"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="5"
                                    Background="{StaticResource CardBrush}"
                                    BorderBrush="{StaticResource LineBrush}"
                                    BorderThickness="1"
                                    VerticalAlignment="Center"
                                    SnapsToDevicePixels="True">
                                <Path x:Name="Check"
                                      Data="M 4,9 L 8,13 L 14,5"
                                      Stroke="{StaticResource OnBrandTextBrush}"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource BlueBrush}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource BlueBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 进度条：蓝色圆角；不确定态用脉冲代替系统绿色块 -->
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Background" Value="{StaticResource SubtleSurfaceBrush}" />
            <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3"
                                    HorizontalAlignment="Left" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Setter TargetName="PART_Indicator" Property="HorizontalAlignment" Value="Stretch" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                            <DoubleAnimation Storyboard.TargetName="PART_Indicator"
                                                             Storyboard.TargetProperty="Opacity"
                                                             From="0.35"
                                                             To="1.0"
                                                             Duration="0:0:0.9" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 路径链接：像超链接一样可点（复制），悬停出悬浮卡片（全局，各窗口共用） -->
        <Style x:Key="PathLinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource AppFocusVisual}" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="LinkText"
                                   Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}"
                                   TextTrimming="CharacterEllipsis"
                                   HorizontalAlignment="Left" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="LinkText" Property="TextDecorations" Value="Underline" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PathCardToolTip" TargetType="ToolTip">
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{StaticResource CardBrush}"
                                BorderBrush="{StaticResource LineBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource ItemCornerRadius}"
                                Padding="10,8"
                                MaxWidth="420">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 表格列头与行统一（变更集 173，B3）。
             为什么用 **keyed 样式 + 逐处显式挂载**：本仓已两次因“隐式样式 + BasedOn={StaticResource {x:Type ...}}”
             在 XamlParseException 上翻车（变更集 134 ListView、138 Button，见 docs/UI-DESIGN.md 禁令）。
             因此这里只提供命名样式，由各表自己 `ColumnHeaderContainerStyle` / `ItemContainerStyle` 挂上。 -->
        <Style x:Key="TableColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
            <!-- 字重依据 UI-DESIGN「可交互/需要强调的小字用 12px + SemiBold」；列头可拖拽/排序 -->
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="48" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <!-- 变更集 174（B3b）：排序方向**不用列头箭头**——GridViewColumnHeader 没有 SortDirection 属性，
                 箭头要靠自定义 HeaderTemplate + 附加属性，成本高于收益；改为在页面状态行里说明当前排序。
                 模板保持系统默认，列宽拖拽/调整手柄不变。 -->
        </Style>
        <Style x:Key="TableRowStyle" TargetType="ListViewItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverSurfaceBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SubtleSurfaceBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
