# DSH Launcher 当前设计

- DSh 精确版本下载先写入 `versions` 下的同级临时目录，校验包版本和启动入口后才替换正式 `versions\<version>`；离开版本控制或关闭 Launcher 会取消下载，失败不会把半成品当成已安装版本。
- `.dshpack` 分享脱敏会同时移除敏感 YAML block scalar 的全部缩进正文，不能只替换 `apiKey`、`privateKey` 等键所在行。

- Plugin 失败诊断：Plugin 安装、更新、卸载和手动安装失败时，先使用现有 web profile 快照回档，再在当前实例 `DSH_HOME\\.dsh-launcher\\reports` 生成完整诊断 ZIP。该本地报告按用户要求保留原始错误、凭据和相关配置，不包含会话文件、`node_modules` 或运行依赖；若当前 DSh 可用，Launcher 会打开或复用 Chat，发送报告路径和继续排查安装的指令。该报告是本地排障包，不改变 `.dshpack` 的分享脱敏规则。

- 市场分类切换分别保存 Plugin 与 Skill 列表的滚动位置；搜索、排序和来源筛选仍只在当前内存快照上重新筛选。
- `.dshpack` 导入始终创建新的版本，但运行时只从当前 Launcher 设置的 DSh 安装位置解析，不沿用整合包或模板的 `RootPath`；该位置没有可用运行时时拒绝导入。
- 版本设置可以把已检测到的 DSH Desktop，或用户选择的本地 EXE、COM、BAT、CMD、PowerShell 脚本、LNK 快捷方式及其它 Windows 可打开文件绑定为“打开窗口”；Launcher 为可直接启动的入口注入该版本的 `DSH_HOME` / `DSH_AGENTS_HOME`，并保留独立的“Launcher 启动”按钮。手动绑定路径属于本机版本设置，不进入可分享 `.dshpack`。
- 已安装 Plugin 管理列表对名称和描述使用单行省略并保留悬停全文；状态明确显示“已启用”或“已禁用”，不显示布尔值。

- 技术栈：.NET 8 WPF，目标 Windows x64；发布版为**单个框架依赖 exe**（`DSH Launcher.exe` 约 3.62 MB，需目标机装 .NET 8 Desktop Runtime (x64)；缺运行时由 .NET apphost 弹系统官方对话框）；可选的中文提醒引导器（`src/DshLauncher.Bootstrapper/`，.NET Framework 4.8，三条路找运行时 + 一键下载便携运行时，变更集 165/168）源码保留；要免装 .NET 就用 `-p:SelfContained=true` 打自包含大包（约 64.8 MB）。不依赖 Node.js、npm、pnpm 或已经安装的 DSh。
- 界面：无边框、可调整大小的 Launcher 窗口；窗口和页面外边距保持紧凑，不再在各模块顶部重复显示大号页面标题。主窗口和 Chat 窗口的默认尺寸限制在当前工作区内并居中，确保 1K 等较低分辨率上仍能看到标题栏并移动窗口。标题栏中间固定显示“启动”、扩展、Agent、对话和设置导航；启动页左上角显示简洁的 `DSH Launcher` 品牌文字，进入扩展或 Agent 时同一位置改为紧凑的当前实例选择器、运行状态圆点和圆角下拉切换入口，点击名称直接进入该实例的插件管理。由扩展或 Agent 进入版本设置时，同一位置改为返回原管理页面的按钮。扩展和 Agent 左栏显示当前实例详情、已安装内容、条目操作和必要的快捷导入，不再重复提供实例选择；Plugin/Skill 市场的分类、搜索和筛选位于右侧市场。启动页左侧为窄的当前实例状态与重命名、设置、启动/停止/重启操作，右侧为宽的全部实例列表和版本控制入口；两侧使用独立容器，右侧列表在自身区域滚动，不以实例数量撑高左侧。双击任意实例时，停止实例走统一启动流程，已运行或 Attached 实例直接打开现有 Web UI。对话页先选择版本，再只列出该版本 `DSH_HOME` 下的对话，并可进一步筛选独立对话或工作区对话。Chat WebView2 不设置 Owner，并在句柄创建后写入独立的窗口级 AppUserModelID，使 DeepSeek 与 Launcher 在 Windows 任务栏中分组分离；窗口继续使用黑色 DeepSeek 图标，关闭 Chat 不停止实例，关闭 Launcher 时仍按现有生命周期清理其 Chat 窗口和 Managed 实例。启动页不展示 Provider 卡片、Provider 网络诊断或运行环境卡片；模型页已移除，只在设置中保留供应商跨版本同步。实例注册列表成功读取且为空时，完成 Node/DSh 检测后自动弹出首次运行引导；用户确认后才准备环境并创建独立 `DSH_HOME` 的首个版本。版本控制页按 PCL2 的左侧列表/右侧详情结构提供导入实例、复制、干净版本、删除版本和整合包导入；版本设置页按“个性化、配置、插件管理、快照回滚、导出”组织。代码、视觉和素材独立实现。
- 启动错误反馈：启动、重启或对话自动启动失败时，主页面只显示短原因和处理入口，完整输出通过通知卡片的“查看详情”按需打开；识别到 YAML 错误时直接引导到“版本控制 → 检查版本”。
- 启动时先同步读取本地实例注册文件并立即显示全部实例；遗留运行状态核对和 DSh/Node 扫描在界面恢复响应后继续，不以网络或进程探测阻塞实例列表。版本卡片只读取本地数据，显示 DSh 版本及当前 `DSH_HOME` 下的 Plugin/Skill 数量摘要，不触发网络请求或启动 DSh。
- 导入 DSH_HOME 时不复制整棵 `node_modules`；只离线恢复 web profile 的 dependencies、bundles 和 `cordis.patch.yml` 直接引用且源目录中实际存在的第三方 Plugin，旧导入实例在启动前也会补齐这些缺包。
- 实例隔离：Manager 数据位于 `%USERPROFILE%\\Documents\\DeepSeek\\launcher`；每个实例使用 `instances\\<id>\\dsh-home`，子进程同时注入 `DSH_HOME` 和实例专用 `DSH_AGENTS_HOME`。多个版本可以共享同一个 DSh 运行目录，但必须使用不同的 `DSH_HOME`；注册记录会拒绝越界 HOME、重解析点和重复 ID；取消注册不删除实例数据。普通 DSh 自动检测和“导入实例”复制当前有效的 `$DSH_HOME`（未设置时为 `~/.dsh`）；封装版 DeepSeek Desktop 则从其依赖元数据定位实际的 `%LOCALAPPDATA%\\DeepSeek Harness Data`，复制工作区、对话、设置和本机凭据，但跳过 Desktop 的 `webview2` 浏览器缓存。同一 DSh package root 是导入唯一地址：再次手动导入会用来源中的当前内容覆盖已有停止实例，自动检测到来源 HOME 变化也更新原记录，不再创建 `(2)/(3)`；加载注册文件时还会合并历史遗留的“相同 package root + 相同导入 DSH_HOME”重复项，但不会合并没有导入来源的复制版本或干净版本，也不会删除旧实例目录。覆盖保留目标 `.dsh-launcher` 版本设置，运行中的实例拒绝覆盖。所有导入还会跳过重解析点和整棵 `node_modules`。升级前已经注册、仍没有会话且工作区为空的停止实例仍可只补入缺少的数据。DSh 运行时会在 `dsh-home\\profiles\\node_modules` 下创建指向共享运行目录的 junction，这不是损坏数据：删除版本时只移除链接本身，复制版本和整合包导出跳过这些链接，任何操作都不穿透链接影响共享运行目录。
Launcher 单实例运行：主进程持有互斥锁并监听同一 Windows 会话内的窗口唤醒请求；第二个进程优先通过进程间通道恢复、显示并聚焦已有主窗口，兼容旧版时才回退到窗口句柄。已有进程正在退出且无法响应时，第二个进程短暂等待锁释放后接管启动，避免两个 Launcher 对同一实例分别持有管理与只读视图。关闭主窗口时先在仍可响应的 UI 上异步取消当前操作并清理 Managed 实例；清理完成后的最终关闭排到当前 `Closing` 事件结束后执行，既不在 UI 线程同步等待，也不在原关闭调用栈中重入。UI 线程未处理异常写入 `%LOCALAPPDATA%\DeepSeek\launcher\crash.log` 后继续运行，不静默退出。
- 生命周期：`installed` 使用注册的 `dsh` 入口，`source` 使用 Source `package.json`/`apps/cli/package.json` 声明的 Node.js engine；Runner 使用 loopback 空闲端口、HTTP 健康检查、实例独占锁和整棵子进程树清理。端口在分配后被其它程序抢占且 DSh 返回 `EADDRINUSE` 时，在保持同一 DSH_HOME 锁的前提下最多换端口重试 3 次；锁被其它 Launcher/残留进程持有与锁目录无权限使用不同提示。运行态区分 Launcher 管理的 `Managed` 与只连接已记录 loopback Web 端点的 `Attached`；Attached 可打开 Web UI，但 Stop、Restart 和退出清理都不触碰外部进程。启动成功后可打开独立 Chat WebView2 窗口；关闭 Chat 不停止实例。Start/Stop/Restart/对话触发的自动启动在 handler 入口占用同一个生命周期串行化 guard（`LifecycleBusyGuard`，只有占用者释放），持有覆盖 runtime 准备等待、实际启停和状态更新全程；runtime 准备进行中 Stop/Restart 不可用。Restart 在停止完成后与 Start 使用相同语义：先 runtime readiness（可能触发一键准备），再按最初目标实例 ID 重解析，目标被删除则中止。Launcher 异常退出遗留的运行实例在下次启动时按记录的 PID/端口/进程名收编回 Managed。
- Source：读取项目根 `package.json`、包管理器字段/锁文件、构建脚本、依赖目录和 CLI 入口；准备阶段执行 install 和 `run build`，只接受实际找到的 CLI 构建入口，超时/取消/非零退出会保留诊断并清理进程树。
- 扩展：Plugin 依据 DSh `profiles\\web\\package.json` 的 dependencies 与 `dsh.profile.bundles` 管理；安装、更新、删除通过 DSh CLI 完成。`@deepseek-ai/dsh-base` 和 `@deepseek-ai/dsh-web-app` 是 DSh 默认 Plugin，只显示状态，界面和服务层都拒绝启用、禁用、安装、更新或删除。调用官方 CLI 时优先使用现有 pnpm；只有当前 PATH 没有 pnpm 但有 Corepack 时，Launcher 才创建临时 pnpm shim 并仅注入该子进程，不修改系统 PATH。Skill 使用 DSh 的实例 `skills`、实例专用 `.agents\\skills` 和项目 `.dsh\\skills`/`.agents\\skills` 根；实例根可管理，项目根只读继承，因此同一 Source 项目的项目级 Skill 仍可能被多个版本共用。Agent Preset 使用实例 `.agent-presets`。Workflow 当前表示 DSh 随附 standard preset 提供的能力，不虚构独立目录。
- Skill 市场：Agent 页在后台读取 Launcher 根目录下的本地缓存；没有缓存或用户点击“刷新目录”时，才搜索 GitHub 名称含 `skill` 的仓库，并按 `default_branch` 扫描仓库树中的实际 `SKILL.md`。每个具有闭合 frontmatter 及非空 `name`、`description` 的 Skill 单独展示，无效文件不生成市场条目；同一仓库中名称相同的重复副本优先采用 `skills/`、`.agents/skills/`、`.claude/skills/` 下的标准位置。界面按开发、设计、文档、效率、Agent、其他分类筛选，搜索输入使用 180ms 防抖。仓库扫描和文件校验使用有限并发及短超时，进度文字持续更新，但只有结果变化且达到刷新间隔时才替换虚拟化列表；仓库分支与更新时间未变化时复用当前校验版本缓存，网络失败不永久记成格式不合格。安装只把所选 `SKILL.md` 所在目录及配套文件导入当前停止实例的 `skills`，不改变项目级共享 Skill；当前实例中已经安装的同名可管理 Skill 同时出现在左侧已安装列表，右侧市场显示“已安装”并禁止重复安装。Agent 左栏必须为已安装列表保留至少一张条目卡片的可见高度，不能被实例路径或当前选择区域挤没。
- 插件市场：扩展页左侧显示当前实例和已安装内容，右侧显示市场。市场从社区目录、GitHub `dsh-plugin` 标签和可选自定义目录聚合候选，明确的 DeepSeek 官方包/仓库才标记为官方；当前实例依赖只作为 Installed 状态。“精选”只展示 dsh-market 同源社区目录中的条目；GitHub 条目使用仓库 owner 的头像作为开发者图片，网络加载失败时保留字母占位。市场缓存优先，搜索、分类、来源和排序只筛选已合并的内存快照，只有“刷新目录”联网。npm、GitHub 与 monorepo 子路径会合并身份和元数据；插件标题仅在条目能解析出真实 GitHub 仓库时打开仓库，`@scope/package` 始终视为 scoped npm 包，无效的 `github.com/@scope/package` 地址会被丢弃。安装前校验 `package.json`、`dsh.bundle.patch` 和入口；根目录没有有效 package 时按 default branch（失败时回退 main/master）扫描常见 monorepo 子目录。安装目标只来自目录条目及已验证的 npm/GitHub `package.json`，README 不再改写安装指令；README 仅用于主题图片预览。停止实例的安装、更新和卸载继续调用官方 DSh Plugin CLI；Attached 实例保持只读，卸载和启停仍要求实例已停止。Managed 实例运行时，未安装插件的按钮显示“热加载”，并只通过当前实例的 dsh-market loopback `/install` 或 `/update` 接口操作；dsh-market 未安装、未启用、该实例关闭热加载，或候选不在 dsh-market 目录时，不直接修改运行中的 profile，而是提示用户停止实例后使用普通安装或手动安装。调用官方 CLI 前，Launcher 只在 web profile 的 `pnpm-workspace.yaml` 中把遗留的未决 `allowBuilds` 占位值明确设为 `false`，当前已验证目标仍单独通过 `--allow-build` 授权，不自动放行其它依赖脚本。Plugin CLI 会继承当前进程或 Git 全局代理，避免 GitHub codeload 绕过已有代理。设置页仍可把兼容模式设为默认。安装和更新使用独立可取消弹窗；Plugin 显示 pnpm 实际解析、复用、下载、添加数量，Skill 显示仓库 ZIP 的真实下载字节，无法计算总量的阶段保持不确定进度。完成或失败时如果 Owner 在操作期间异常变为最小化，会恢复其安装前窗口状态。CLI 使用非动态 reporter，失败输出过滤 pnpm 刷屏并优先显示根因。市场已验证包只授权对应顶层 Plugin 运行安装构建脚本。操作前备份包含 `pnpm-workspace.yaml` 的 web profile，成功后自动刷新实例与市场；运行中完成时提示刷新 DSh 页面，host 改动仍提示重启实例。GitHub 安装项会用仓库声明的 package name 重新匹配 Installed 状态。主题预览按需读取 GitHub README 图片，不在进入市场时预下载；无图、仅 SVG、图片过大或读取失败时明确提示。每个实例可独立关闭 dsh-market 热加载；开启且运行实例装有 `dsh-market` 时，Launcher 在应用主题前创建 Plugin 范围自动快照，再通过其 loopback 接口读取与应用主题，不修改 DSh Web UI；Wallpaper 尚未建立独立格式。
- 版本设置与整合包：每个版本的设置保存于自己的 `DSH_HOME\\.dsh-launcher\\version-settings.json`；Launcher 级设置保存于根目录 `launcher-settings.json`（全局“和所有版本配置同步”开关 + Launcher 级工作区名称列表）。设置/诊断页除全局开关外，还提供与“版本设置 → 配置”同源的单版本编辑区，可选择版本并修改全版本同步、对话独立/工作区/全量策略及模型同步；工作区管理可显示成员、添加、重命名和删除。重命名会更新成员版本，删除只把成员版本切回 Independent 并清除工作区名称，不删除对话文件。个性化页显示当前版本信息并允许保存版本名称，修改直接更新实例注册记录。新建干净版本时读取官方 npm package metadata 提供 DSh 版本选择；本机缺少所选版本时，精确安装到 Launcher 设置的 DSh 目录下 `versions\\<version>`，不修改系统 PATH，也不触发 UAC。配置页支持全版本同步、版本独立、工作区同步和全量兜底同步；另有独立的“所有版本自动同步模型”开关，模型同步不再跟随对话文件范围。版本控制页可检查 DSH_HOME、DSh Runtime、Node engine 兼容性、版本配置/Provider/MCP/web profile 和旧运行记录；自动修复只创建缺失 DSH_HOME、清除不存活的运行记录，并把失效 Installed 绑定切换到已验证的全局 DSh。停止且非 Attached 的版本可以从版本控制页或版本设置的“快照回滚”页创建和回滚本地 `.dshsnapshot`：快照精确保存版本设置、Provider 配置与官方凭据、MCP 和 Plugin profile，使用 Windows DPAPI 按当前用户加密，不包含会话或运行依赖；回滚前自动再建恢复点，完成后版本设置页会重新读取当前配置和 Plugin 状态。快照恢复先完成全部目标与回滚文件预处理，任何中途失败都会恢复已改文件；自动快照最多保留 10 个且总计不超过 256 MiB，手动快照不参与清理。版本配置保存和 Plugin 变更前也会自动创建快照。开启模型同步时，`ModelProviderSyncService` 只按停止版本中 `settings.yaml` 的最新写入时间选择配置来源，再把模型段、Provider 启用状态和同一来源的 DSh 官方 `.credentials.yaml` 作为一个事务复制到其它符合策略的停止版本；提交中途失败会回滚目标，凭据时间戳不会单独改变配置来源。凭据文件整份原子复制，不解析、不记录、不进入 Launcher 配置，并拒绝重解析点和超过 1 MiB 的文件。运行中的版本不会被写入；相关停止版本都没有 llm 配置时返回 `NoConfigurationSource`。插件管理页复用官方 Plugin CLI 和现有 `ExtensionService`，只对当前实例的可管理 Plugin 提供启用/禁用/删除。`.dshpack` 是默认扩展名的 ZIP 包，包含 `manifest.json` 和经过清理的 `dsh-home/` 设计配置；可选包含脱敏后的 Provider 结构、模型目录与启用状态，但绝不包含 `.credentials.yaml`、`.env`/`.env.*`、API Key、Token、URL 用户信息、sessions 或 Node.js 路径；导出脱敏只匹配确切敏感字段，不误删 `hotkey`、`publicKey` 等普通配置。导入时再次拒绝凭据路径并清理所有可分享文本。导入始终创建新的版本，不覆盖已有 DSH_HOME。扩展名可在导出页修改，包内路径必须留在 `dsh-home/` 内。
- 版本检查：存在 `settings.yaml` 时使用当前 DSh Runtime 自带的 YAML 解析器校验语法与顶层结构，只返回错误代码及行列，不读取或显示配置值；解析器不可用会作为配置异常明确报告。
- `.dshpack` 的 manifest 记录名称、说明、DSh 版本、创建时间、Plugin/Skill/Agent Preset/Provider 摘要和 workflow；Skill、Agent Preset 与配置文本在导出和导入时都会清理敏感值。导入先显示摘要预览，确认后创建新的独立版本。
- 插件市场验证：候选插件安装前读取 npm 或 GitHub 的 `package.json`，检查 `dsh.bundle.patch` 和可加载入口；GitHub 校验优先读取 `default_branch`，并支持 `tree/<branch>/<subpath>` 与 `#path:/...` monorepo 位置；没有包名/仓库、缺少 DSh bundle 或缺少入口的项目会被拒绝。Launcher 不直接修改 `dsh.profile.bundles` 或 `cordis.patch.yml`；运行中热加载仅采用 dsh-market 已提供的能力。
- MCP：配置符合 DSh MCP client 的 `stdio` 或 `streamable-http` 结构，元数据保存在实例 `.dsh-launcher\\mcp.json`，启用项生成实例 `launcher.patch.yml`，启动时通过 `--patch` 叠加；配置与 Plugin/Skill/Preset 修改都要求实例停止。
- 模型：编辑实例 `settings.yaml` 中的 `llm-deepseek` 和 `llm-pi-ai.providers`；保留无关顶层段落，只写环境变量引用、URL 和模型目录，不把 API Key 写入该文件。读取器兼容 DSh Web UI 当前生成的花括号映射、行尾逗号和行内模型数组。Provider 自动同步只重写这两个模型配置范围，并保留其它顶层段落；API Key 由 DSh 官方 `.credentials.yaml` 单独管理。
- Provider YAML 写入：读取器兼容 DSh Web UI 的花括号映射；单项编辑前只把 `providers` 子区块规范为块级 YAML，跨版本同步整体替换目标 `providers` 子区块并保留其它顶层设置，禁止在同一映射内混写两种 YAML 格式。
- Provider 界面范围：启动页不读取、显示或诊断 Provider，也不提供启用/禁用卡片；Launcher 只保留设置中的模型供应商跨版本同步与对应的本地同步状态。
- 运行时兼容：实例保存独立的启动描述，将 DSh package root 与实际宿主、入口和 pnpm 路径分开。普通 npm 安装继续直接调用 `dsh`，源码使用 Node 运行构建入口；重点兼容 `anywhere-labs/deepseek-harness-desktop` 的 DSH Desktop v2 Windows 包，严格校验 `DSH Desktop.exe`、`resources\app.asar.unpacked\lib\desktop-cli.js`、官方 DSh package/CLI 和内置 pnpm 后，通过 Electron Run-as-Node 调用其私有 CLI bootstrap。该封装运行时不要求系统另装 Node，并仍给每个 Launcher 版本注入独立 `DSH_HOME`；缺少任一必要文件时不猜测启动。Launcher 可单独打开 DSH Desktop 原生窗口，但同一版本已由 Launcher 运行时拒绝并发打开，避免共同写一个 HOME。
- 对话：按当前 DSh JSONL 持久化格式扫描实例 `sessions`，识别 `session.jsonl` 和 `session.jsonl.zstd`；压缩文件按首个 Zstandard frame 读取 session header，要求 `version=0`、安全非负整数 `createdAt`/`delegationDepth` 及当前字段类型，头部有效的会话可从 Chat 入口预选，文件可导入、导出、备份或删除并保留原始格式，修改类操作要求实例停止。对话页用“对话文件 / 备份与恢复”标签分开当前会话与当前实例备份；备份列表读取标题和 header，可选择恢复，已有相同会话 ID 时拒绝覆盖，恢复文件使用当前时间参与后续同步。对话与备份列表显示对应实例名称，不展示会话内部工作目录；`WorkingDirectory` 仍保留用于解析、导入和恢复。列表优先读取 `storages/session_projcache.json` 的会话标题，无标题时显示“未命名 · 项目 · 时间”，不把 session ID 或完整路径作为主名称。导入可选择目标版本和工作区；工作区决定目标 `sessions` 目录的落位，原始会话内容保持不变。双击有效会话时，已运行实例直接打开目标 Chat；已停止实例先启动并通过健康检查，再打开目标会话。当前使用已验证的 Chat `localStorage` 预选 session ID，尚未承诺官方 session deep link。无法读取 header 的文件仍列出但禁止打开；导出目标会归一化重复的 `.jsonl`/`.jsonl.zstd` 后缀。版本会话策略由 `ConversationSyncService` 执行：Independent 不合并，Workspace 按同名工作区合并，All 或“和所有版本配置同步”作为兜底加入所有版本；同步只在进入对话页、启动前、停止/重启边界和会话导入/删除后处理停止实例，运行中的 DSh 不被直接写入。删除会话使用实例 `.dsh-launcher\conversation-sync.json` 的删除标记，避免旧文件在下一次同步时复活；重新创建同一路径的新文件会清除对应标记。
- Node/DSh 检测：合并 Launcher 进程 PATH 与当前 Windows 用户/系统 PATH，再检查用户选择的安装位置和 Windows 常见目录并实际执行版本命令；即使 Launcher 的父进程仍持有旧 PATH，也能发现后来手动安装的 Node 和 DSh。Launcher 管理的 DSh 默认安装到 Manager 根目录下的 `runtime\dsh`，无需管理员权限；用户可在设置或首次引导中改为其它目录，修改不会移动已有运行时。DSh 只有在命令返回可解析的语义版本、附近能解析到 `@deepseek-ai/dsh` package root、且命令版本与 `package.json` 一致时才标记为可用；残留 shim、损坏包或版本错配会显示修复提示并进入运行环境准备。检测会保留扫描到的全部不同 package root；每个尚未注册的有效 Installed DSh 会自动成为一个版本，并获得独立 `DSH_HOME`，同一 root 后续扫描不会重复添加。多个 Node 候选使用有限并发执行版本检查，并从已检测 DSh 包或 Source package metadata 读取 `engines.node`；优先选择满足当前要求的最高版本，没有兼容项时保留最高有效版本用于显示 `Incompatible`，而不是误报 `Missing`。状态显示为 `Missing`、`Compatible`、`Incompatible` 或 `Unknown`。官方包未声明 engine 时不额外写死版本限制。单候选检测超时会清理进程树，窗口关闭会取消检测；Windows `.cmd` 入口通过 `cmd.exe` 调用。校验和启动 Installed/Source DSh 时都把已检测 Node 目录临时加入子进程 PATH，不永久修改系统环境。Node.js 下载提供官方页和 npmmirror，DSh 安装提供 npm 官方源和 npmmirror 选择。
- 检测性能：除 PATH 和 Windows 标准安装位置外，只定向检查 NVM for Windows、Volta、fnm、Scoop、npm prefix 和卸载注册表，不遍历整块磁盘。自动检测最多并发验证 4 个候选，并使用带宿主、入口和 package 文件指纹的 24 小时缓存；明确刷新会绕过缓存。自定义目录扫描只覆盖所选目录，限制为 6 层和 20,000 个目录，跳过重解析点并支持取消。
- DeepSeek Desktop 兼容运行时：自动检查当前用户与 Program Files 下的标准 DeepSeek Desktop / DSH Desktop 安装目录，并只读查询 Windows 卸载注册表中的自定义安装位置。旧版只有同时存在 Desktop 主程序、`runtime\node.exe`、有效的官方 `@deepseek-ai/dsh` package metadata 和 `app\node_modules\.bin\dsh` 启动文件时才接受；DSH Desktop v2 则按“运行时兼容”中的封装结构严格校验。旧版 Desktop 的 `.modules.yaml` 用于反查它实际使用的 DSH_HOME，标准本地数据目录只作为回退；缓存命中时也会重新解析该来源，避免沿用旧的错误路径。界面同时显示 Desktop 与 DSh 版本。检测、实例启动和 Plugin CLI 只给子进程临时注入封装应用自带运行环境，不修改系统 PATH。没有版本时，首次引导可直接使用已验证的封装运行时创建独立 `DSH_HOME` 版本，不重复下载 Node 或 DSh。
- 一键运行环境准备（设计原则）：Launcher 自身不捆绑 Node.js、npm 或 DSh；运行环境缺失只由用户主动触发"准备运行环境"，启动时不静默下载或安装。Node 使用系统级官方 MSI（官方源或 npmmirror 镜像）安装，保留 MSI 默认的系统 PATH 功能；Launcher 不重复写注册表环境变量。提权执行前验证 MSI 的 Authenticode 签名链与 Node.js 官方发布者，不自动卸载现有 Node；Windows Installer 不被 Launcher 强杀，超时后仍在后台运行的安装进程在退出前既阻止再次安装 Node，也阻止关闭 Launcher。全局 `@deepseek-ai/dsh` 只服务 Installed 实例和无实例目标的诊断场景；Source 使用项目自身 metadata 与 CLI，不安装全局 DSh。Node 兼容性始终以目标 runtime 的 metadata 为准：Installed 优先读自身 package root，runtime 失效重装/重绑定后才使用新检测 runtime 的 engine；Source 只读自身 `engines.node`；未声明保持未声明，不继承其它 runtime 的要求；Node 版本选择同样由目标 engine 决定，兜底版本不满足要求时停止安装。runtime 准备与实例生命周期操作（Start/Stop/Restart/对话触发的自动启动/设置页准备）共用同一个串行化边界，同一时刻只允许一个。准备流程始终针对最初触发的目标实例，完成后按原实例 ID 重新解析并重新读取 engine 再判定就绪；目标在准备期间被删除则中止。会话标题缓存等可选增强失败时只降级显示，不中断会话列表。
