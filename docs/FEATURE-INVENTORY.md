# DSH Launcher 功能清单（逐级分类）与完整性判定

> 用途：给后续**界面布置优化**提供一张可对照、可打勾、可回归的**功能台账**；
> 同时回答"当前功能是否完整"（在什么口径下完整、哪些不完整）。
>
> 相关文档：[CHANGESETS](CHANGESETS.md)（169 条改动）｜[BEHAVIOR-CHANGES](BEHAVIOR-CHANGES.md)（111 条行为差异）｜
> [UI-DESIGN](UI-DESIGN.md)（UI 规范）｜[UI-UNIFICATION-TODO](UI-UNIFICATION-TODO.md)（UI 遗留）｜
> [DSH_CONTRACT_INVENTORY](DSH_CONTRACT_INVENTORY.md)（与 dsh 的契约）｜[BEHAVIOR_MATRIX](BEHAVIOR_MATRIX.md)（行为×验证）｜
> [VERIFICATION](VERIFICATION.md)（三层验证与发布 SOP）｜[DIFF-REPORT](DIFF-REPORT.md)（与上游的差异盘点）

---

## 0. 口径、方法与边界

### 0.1 "功能完整"按口径分四问（结论见 §3）

| 口径 | 判定 | 依据 |
|---|---|---|
| ① 仓库自身声明口径 | **基本闭环**：README / CURRENT_DESIGN / DEV_STATE 声称的能力**均已实现**；未闭环的是 §4-B 的 12 项“已声明未做”与 §4-A 的 8 项“文档/产物不一致” | README、CURRENT_DESIGN、DEV_STATE、UI-UNIFICATION-TODO、work-log/79、work-log/197 |
| ② 与 dsh（上游 harness）契约 | **完整**：C1–C16 哨兵**全绿**，C17/C19 按设计由 SelfTest/容错解析覆盖 | 本次实测 harness（§0.3） |
| ③ 与上游启动器对照 | **不完整（分叉差距，非缺陷）**：本分支基线是上游 v1.0.7，未合并上游 v1.0.8–v1.2.4 的方向性能力 | DIFF-REPORT §4 |
| ④ 真实可运行性 | **通过**：构建 0/0 → 自测 204/0 → harness 386/0/0（含 UI 冒烟）→ 真机起窗口渲染正常 | 本次实测（§0.3） |

### 0.2 清单来源与可信度

- **事实来源**：源码（`src/DshLauncher/**.xaml(.cs)`，63,330 行）＋ `docs/` 与 `work-log/` 记录＋本次实跑输出。
- **口径来源**：用户确认"仓库自身口径 + 上游对照 + 真实使用流程实测"三基准、覆盖"界面 / 内部系统 / 工程验证"三层。
- **未确认项**：每条 L3 的具体交互语义若未在任何文档写明，本文只描述**代码事实**（入口、方法名），不替业务意图下结论；此类条目在 §4 单独列出待用户确认。

### 0.3 本次实测证据（2026-09-24）

| 检查 | 命令 / 方式 | 结果 |
|---|---|---|
| 构建 | `dotnet build src/DshLauncher/DshLauncher.csproj -c Release` | **0 警告 / 0 错误** |
| 仓库自测 | `dotnet run --project tests/DshLauncher.SelfTest/… -c Release` | **204 PASS / 0 FAIL** |
| 工作区 harness（非 UI） | `VerifyP0.dll`（不含 `--ui`） | **365 PASS / 0 FAIL / 0 SKIP** |
| 工作区 harness（含 UI 冒烟） | `VerifyP0.dll --ui` | **386 PASS / 0 FAIL / 0 SKIP**（变更集 170 后为 **388/0/0**，见 §6.3） |
| 契约哨兵 | 同上，`contract:` 前缀 | 全绿：`C1 上游=4 已装=4 启动器已核对=4`、C2/C6 文件名正则一致、C3 头字段、C4/C5 目录编码、C7 CLI 旗标、C8 `dsh web:` 输出行、C9 Node 下限、C10 bundles、C14–C16 权限/环境变量/遥测域 |
| 产物 | `dotnet publish -c Release -o <tmp>` | 单文件 `DSH Launcher.exe` **3,795,192 B**（与 `dist/` 逐字节同尺寸，可复现） |
| 真机冒烟 | 临时数据根启动 + UIA 取窗口 + 截图 | 窗口 **1475×900**；导航 6 项、实例状态卡（空态）、启动实例列（空态）渲染正常 |
| 代码盘点 | Services 105 / Models 25 / Watchdog 6 / Controls 3；12 个窗口 + 8 个内嵌页 | 见 §2 |

> ⚠ 临时产物（`_smoke-*`、`%TEMP%\verify-p0-*`）已清理；工作区源码与 `dist/` 未改动（`git status` 干净）。

---

## 1. 一级域总览（L1）

| # | L1 功能域 | 一句话 | 当前主入口 |
|---|---|---|---|
| 1 | 实例与版本 | 一个实例 = 一个独立 `DSH_HOME` + 一个运行时绑定 | 启动页 · 版本控制页 |
| 2 | 启动与运行 | 起停、守护、健康证据、崩溃恢复、安全模式 | 启动页实例卡片 · 实例设置→运行状况 |
| 3 | 运行环境 | Node / DSh / Desktop 的检测、安装、位置管理 | 设置→运行环境 · 首次运行引导 |
| 4 | 扩展生态 | Plugin / Skill / MCP / Preset / 市场 / 矩阵 | 扩展页 · Agent 页 · 实例设置→插件管理 |
| 5 | 会话与对话 | JSONL/zstd 会话的列出、打开、导入导出备份、全文检索 | 对话页 · Chat 窗口 |
| 6 | 同步与配置 | 跨版本对话/模型同步、Provider、环境变量、工作区 | 实例设置→配置 · 设置→常规 |
| 7 | 快照与整合包 | 本地加密快照 / 可分享 `.dshpack` / 失败诊断报告 | 版本控制页 · 实例设置→导出 |
| 8 | 观测·诊断·恢复 | 任务、日志、运行状况、诊断包、存储清理 | 任务页 · 设置→诊断与日志 · 实例设置→运行状况 |
| 9 | 安全与隐私 | 凭据体检、危险配置、脱敏、加密、来源校验 | 设置→诊断与日志（安全体检区） |
| 10 | 界面框架与交互基础 | 窗口/托盘/单实例/令牌体系/键盘可达性 | 全局 |
| 11 | 设置与偏好 | 5 个设置分类 | 设置 / 诊断页 |
| 12 | 工程与验证 | 自测 / harness / 契约哨兵 / CI / 文档 | 仓库（非用户可见） |

---

## 2. 逐级功能清单（L1 → L2 → L3）

> 每条的"入口"＝**当前唯一或主要可达路径**；带 `★` 的条目在 §5 的信息架构建议里被点名。

### 2.1 实例与版本

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 1.1 实例注册与台账 | 1.1.1 注册/注销/重命名/唯一名 ｜ 1.1.2 越界 HOME、重解析点、重复 ID 拒绝 ｜ 1.1.3 取消注册不删数据 ｜ 1.1.4 遗留重复项合并 ｜ 1.1.5 便携数据根的 HOME 路径重定位（`E1018`） | 版本控制页；内部（`InstanceRegistry`） |
| 1.2 导入实例 | 1.2.1 从文件夹 ｜ 1.2.2 从 `.lnk` 快捷方式 ｜ 1.2.3 从源码目录 ｜ 1.2.4 扫描本机 DSH 环境（`%USERPROFILE%\.dsh*` + `DSH_HOME`，按 web/tui/other 分类） ｜ 1.2.5 导入整合包 ｜ 1.2.6 同 package root 覆盖已停止实例、运行中拒绝 ｜ 1.2.7 跳过 `node_modules`/`webview2`/重解析点、缺包补齐、失败回滚 | 版本控制页「导入实例」下拉（5 项双行菜单） |
| 1.3 实例操作 | 1.3.1 重命名 ｜ 1.3.2 实例设置 ｜ 1.3.3 复制版本 ｜ 1.3.4 新建干净版本（先弹窗后异步补全官方版本） ｜ 1.3.5 删除版本（只读文件预扫描 + 清属性） ｜ 1.3.6 双击（停止→启动；运行→打开 Web UI） | 启动页实例卡片；版本控制页 ★ |
| 1.4 实例 ↔ 运行时绑定 | 1.4.1 更换运行版本（预检 Node engine / 核心 peerDeps / 会话代际 / 方向 → 可选快照 → 重绑定 → 失败回滚） ｜ 1.4.2 切换历史（50 条）与一键回退 ｜ 1.4.3 DSh 更新提示（实例级开关 + 卡片"有新版本"徽标，6h 缓存） ｜ 1.4.4 运行时重绑定自愈（`InstanceRuntimeRebinder`） | 版本控制页选中版本详情「运行版本」 |
| 1.5 版本目录 | 1.5.1 `versions\<ver>` 精确安装（临时候选目录校验后替换、可取消） ｜ 1.5.2 检查版本（DSH_HOME / Runtime / Node engine / 配置 / Provider / MCP / web profile / 旧运行记录 + DSh 自带 YAML 解析器校验） ｜ 1.5.3 修复可处理项（只建缺失 HOME、清不存活记录、重绑定失效 Installed） | 版本控制页「版本检查」 |
| 1.6 实例列表 | 1.6.1 本地优先加载（先显示、后探测） ｜ 1.6.2 卡片资源摘要与状态胶囊 ｜ 1.6.3 启动时默认选中"最近使用" | 启动页右栏「启动实例」 |
| 1.7 工作区（同步用） | 1.7.1 成员显示 ｜ 1.7.2 添加 ｜ 1.7.3 重命名 ｜ 1.7.4 删除（只解除同步、不删对话） | 设置→常规→工作区同步管理 |

### 2.2 启动与运行

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 2.1 启动方式 | 2.1.1 `OpenMode` = Web / Desktop / 隔离 ｜ 2.1.2 主按钮按最近选择执行 ｜ 2.1.3 切换只写设置不启动 ｜ 2.1.4 运行时自带 desktop surface 时回落 Web | **启动页实例卡片主按钮右侧 ▼ 菜单（当前唯一入口）** ★ |
| 2.2 生命周期 | 2.2.1 Start / Stop / Restart ｜ 2.2.2 同版本复用与跨 Runner `DSH_HOME` 锁 ｜ 2.2.3 `EADDRINUSE` 换端口重试 3 次 ｜ 2.2.4 HTTP 健康检查 ｜ 2.2.5 `LifecycleBusyGuard` 串行化 ｜ 2.2.6 runtime readiness 门（可能触发"准备运行环境"） | 启动页按钮；托盘；对话页双击 |
| 2.3 运行态判定 | 2.3.1 `Managed` ｜ 2.3.2 `Attached`（可开 Web UI，不被 Stop/Restart/退出误杀） ｜ 2.3.3 外部实例 5s 收敛 ｜ 2.3.4 孤儿进程检测 | 启动页状态胶囊 |
| 2.4 实例守护（进程内） | 2.4.1 5s 探测循环（可配 2–120s） ｜ 2.4.2 台账落盘 `watchdog-state.json` ｜ 2.4.3 幽灵实例转正（PID 死+端口活+身份校验） ｜ 2.4.4 接管重启 ｜ 2.4.5 停止判定 15s 宽限 + 5min 重生观察窗 ｜ 2.4.6 残留进程清理（桌宠/helper） ｜ 2.4.7 Launcher 崩溃后由台账续管 | 设置→常规→实例守护（仅间隔）；其余自动 |
| 2.5 启动健康证据 | 2.5.1 四层证据 Process/Log/Http/Page ｜ 2.5.2 只认本次启动后的新增日志行 ｜ 2.5.3 页面 DOM 探针 ｜ 2.5.4 落盘 `startup-evidence.jsonl`（200 条） ｜ 2.5.5 就绪后 HTTP 连续失败降级提示 | 实例设置→运行状况→启动证据 |
| 2.6 安全模式 | 2.6.1 `.dsh-safe` Tier1（剥离第三方、保留核心）/ Tier2（最小核心） ｜ 2.6.2 用户文件 SHA-256 零污染证据 ｜ 2.6.3 正常启动成功后自动清理 ｜ 2.6.4 卡片"安全模式"徽标 | 启动失败时弹窗；实例卡片徽标 |
| 2.7 崩溃恢复 | 2.7.1 策略四选（仅通知/自动重启/冷却关闭/重启后冷却） ｜ 2.7.2 退避 5s→15s→1m→5m（上限 1–10） ｜ 2.7.3 崩溃现场落盘（10 条） ｜ 2.7.4 原因归类（规则表 + 主/次因 + 置信度） ｜ 2.7.5 主动停止不误判 ｜ 2.7.6 冷却跨启动保留 + 清冷却并重启 ｜ 2.7.7 稳定 10 分钟重置计数 | 实例设置→运行状况→崩溃恢复 |
| 2.8 逐插件定位 | 2.8.1 二分禁用（复制清单 + 改写 bundles + node_modules 联接） ｜ 2.8.2 2–4 轮锁定 ｜ 2.8.3 一键"禁用 X 并正常启动"（自动存回滚点） | 实例设置→运行状况→插件排查 |
| 2.9 空闲自动停止 | 2.9.1 每实例开关 + 5/15/30/60/120/240 分钟 ｜ 2.9.2 后台任务不算空闲（外部连接/CPU≥1%/会话写入/dsh 输出/Launcher 忙） | 实例设置→运行状况 |
| 2.10 Chat 窗口 | 2.10.1 WebView2 承载 dsh Web UI（带 launch token） ｜ 2.10.2 无 Owner + 独立 AppUserModelID（任务栏分组分离） ｜ 2.10.3 关闭 Chat 不停止实例 ｜ 2.10.4 版本变化时清 WebView2 缓存（保留 Cookie） | 启动实例（Desktop 方式）；启动页双击运行中实例 |
| 2.11 终端路径 | 2.11.1 在终端打开（Windows Terminal，注入 `DSH_HOME`） ｜ 2.11.2 复制 TUI 三命令（安装/新会话/续接，含 `--no-open` 判定） | 实例卡片 ▼ 菜单；实例设置→个性化 ★ |
| 2.12 托盘 | 2.12.1 关闭到托盘 / 退出（两选一） ｜ 2.12.2 「运行中的实例」二级菜单（打开/停止） ｜ 2.12.3 双击恢复 | 托盘图标；设置→常规→关闭行为 |
| 2.13 单实例与唤醒 | 2.13.1 互斥锁 + 命名管道唤醒（旧版回退窗口句柄） ｜ 2.13.2 退出阶段接管 ｜ 2.13.3 关闭时异步取消操作 + 清理 Managed 实例（排到 `Closing` 之后） | 全局 |

### 2.3 运行环境

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 3.1 Node 检测 | 3.1.1 合并进程/用户/系统 PATH ｜ 3.1.2 常见安装位置与版本管理器定向检查 ｜ 3.1.3 多候选并发（≤4）+ 24h 缓存 ｜ 3.1.4 四态 Missing/Compatible/Incompatible/Unknown | 设置→运行环境 |
| 3.2 DSh 检测 | 3.2.1 语义版本 + package root + 版本一致性三重校验 ｜ 3.2.2 多 package root 自动登记为实例（排除安装目录级载荷） | 设置→运行环境；启动时自动 |
| 3.3 Desktop 兼容运行时 | 3.3.1 旧版 DeepSeek Desktop（内置 node + `.modules.yaml` 反查 HOME） ｜ 3.3.2 DSH Desktop v2（Electron Run-as-Node 调 `desktop-cli.js`） ｜ 3.3.3 同版本并发写 HOME 拒绝 | 自动检测；版本控制页 |
| 3.4 一键准备运行环境 | 3.4.1 便携 Node（免管理员，优先）→ 失败再问 MSI ｜ 3.4.2 MSI 前验 Authenticode 签名与官方发布者 ｜ 3.4.3 npm 非 global 安装 `@deepseek-ai/dsh` + 补 `dsh.cmd` + 依赖树可达性自检 ｜ 3.4.4 与生命周期共用串行化边界 | 设置→运行环境；首次运行引导；启动时缺环境弹窗 |
| 3.5 安装位置管理 | 3.5.1 默认 `run_time`（exe 同目录，便携优先）／不可写回退（`E1009`） ｜ 3.5.2 修改位置 ｜ 3.5.3 移动已有安装（单选/多选、同盘重命名、跨盘复制校验、失败回滚） ｜ 3.5.4 "要移动的运行时"卡片（复选框，来源＝实例运行时） | 设置→运行环境 |
| 3.6 版本下载源 | 3.6.1 官方 npm ／ npmmirror ｜ 3.6.2 落盘为默认 ｜ 3.6.3 新建版本 / 更换版本弹窗可临时覆盖 | 设置→运行环境「版本下载源」 |
| 3.7 自定义目录扫描 | 3.7.1 只扫所选目录（≤6 层 / ≤20,000 目录 / 跳过重解析点 / 可取消） | 设置→运行环境「扫描自定义目录」 |

### 2.4 扩展生态

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 4.1 Plugin 管理 | 4.1.1 已安装列表（启用/禁用/更新/删除/刷新） ｜ 4.1.2 依赖自检（doctor：核心混入/代际错配/bundle 缺失） ｜ 4.1.3 检查更新 / 全部更新（失败不中断、不兼容自动跳过） ｜ 4.1.4 核心包保护（`dsh-base` / `dsh-web-app` 只读） | **扩展页左栏** 与 **实例设置→插件管理** ★ 两处 |
| 4.2 依赖自愈 | 4.2.1 profile bundle/依赖不可解析时 profile 内 `pnpm install` ｜ 4.2.2 导入实例启动前补齐缺包 | 自动（启动/导入路径） |
| 4.3 Plugin 安装 | 4.3.1 手动安装（装前校验 `dsh.bundle.patch` + 可加载入口） ｜ 4.3.2 市场安装 ｜ 4.3.3 快速 / 兼容两种模式 ｜ 4.3.4 `@deepseek-ai/*` peerDeps 预检 ｜ 4.3.5 独立进度窗（真实包计数） ｜ 4.3.6 失败：残留回滚 + pnpm 失败翻译 + 镜像重试 + 未脱敏诊断报告 + Chat 接力 ｜ 4.3.7 运行中经 dsh-market 热加载（不可用时提示先停止） | 扩展页工具条 / 市场卡片 |
| 4.4 MCP 管理 | 4.4.1 `stdio` / `streamable-http` 结构 ｜ 4.4.2 元数据 `mcp.json` + 启用项生成 `launcher.patch.yml`（`insert` 列表） | 扩展页「添加 MCP」 |
| 4.5 Skill 管理 | 4.5.1 导入 Skill（目录 + 配套文件） ｜ 4.5.2 已安装状态与"已安装"禁用 | Agent 页；扩展页「导入 Skill」 |
| 4.6 Agent Preset | 4.6.1 导入 Preset（`.agent-presets`） | 扩展页「导入 Agent Preset」 |
| 4.7 Profile 管理 | 4.7.1 活动 profile 解析（`profiles/<名>`） ｜ 4.7.2 bundles 读取 ｜ 4.7.3 启动前校验（无 Web App bundle 拒绝启动） ｜ 4.7.4 受限同步到终端面 profile | 扩展页顶部（Agent 页隐藏）；实例设置 |
| 4.8 插件市场 | 4.8.1 多来源聚合（GitHub 社区目录 + `dsh-plugin` topic + 自定义目录 + 2 个中文源适配器，默认停用） ｜ 4.8.2 缓存优先、只有"刷新目录"联网（gzip/br 自动解压，180s 预算） ｜ 4.8.3 强身份合并（npm 包名/install spec/GitHub 仓库） ｜ 4.8.4 分类 / 搜索 / 排序 / 来源筛选 ｜ 4.8.5 精选 ｜ 4.8.6 开发者头像（失败占位） ｜ 4.8.7 已安装状态与热加载按钮 ｜ 4.8.8 主题预览（按需读 README 图片） ｜ 4.8.9 GitHub monorepo 子路径校验 | 扩展页右栏 |
| 4.9 Skill 市场 | 4.9.1 GitHub 仓库树发现 `SKILL.md`（≤20/仓库） ｜ 4.9.2 frontmatter 校验与标准位置优选 ｜ 4.9.3 分类 / 180ms 防抖搜索 / 排序 / 来源 ｜ 4.9.4 已安装禁用 | Agent 页右栏 |
| 4.10 插件 × 实例矩阵 | 4.10.1 行=至少一实例启用的插件（排除核心与运行时依赖） ｜ 4.10.2 列=实例 ｜ 4.10.3 三态单元格 + 复制 TSV ｜ 4.10.4 只读 | 扩展页「插件矩阵」→ 内嵌页 |
| 4.11 dsh-market 集成 | 4.11.1 热加载 `/install` `/update` ｜ 4.11.2 主题应用（先建 Plugin 范围快照） ｜ 4.11.3 每实例热加载开关 | 扩展页 |

### 2.5 会话与对话

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 5.1 会话读取 | 5.1.1 JSONL 与 `zstd` ｜ 5.1.2 按会话目录归并、只列最高代际（含代际列） ｜ 5.1.3 标题取自 `session_projcache.json`，无标题回退"未命名 · 项目 · 时间" ｜ 5.1.4 归属列显示实例名（不显示工作路径） ｜ 5.1.5 头部无效仍列出但禁止打开 | 对话页 |
| 5.2 会话操作 | 5.2.1 打开（已停止则先启动并通过健康检查） ｜ 5.2.2 导入（可选目标版本与工作区落位） ｜ 5.2.3 导出（归一化重复后缀） ｜ 5.2.4 备份/恢复（相同 session ID 拒绝覆盖） ｜ 5.2.5 删除（写删除标记、明确"退回更早一代"） | 对话页两张表 |
| 5.3 全文检索 | 5.3.1 跨实例正文检索（含 zstd） ｜ 5.3.2 单文件 ≤32 MiB、损坏跳过 ｜ 5.3.3 按命中次数排序 + 片段 ｜ 5.3.4 双击切到所属实例打开 | 对话页搜索框 |
| 5.4 格式兼容 | 5.4.1 v0–v4 命名与头部 ｜ 5.4.2 导入跟随目标实例编码 ｜ 5.4.3 旧实例拒绝新版会话并明确报错 | 内部（`SessionFileNames` / `ConversationService`） |
| 5.5 会话同步策略 | 5.5.1 Independent / Workspace / All ｜ 5.5.2 只在进入对话页、启动前、停止/重启边界、导入/删除后处理停止实例 | 实例设置→配置；设置→常规 ★ |

### 2.6 同步与配置

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 6.1 实例设置 | 6.1.1 个性化（版本名、数据位置、实例环境变量、终端命令） ｜ 6.1.2 配置（同步范围、模型同步） ｜ 6.1.3 插件管理 ｜ 6.1.4 运行状况 ｜ 6.1.5 导出（整合包、包扩展名） | 启动页「实例设置」 |
| 6.2 跨版本同步 | 6.2.1 全版本同步开关 ｜ 6.2.2 对话文件范围（独立/工作区/全量） ｜ 6.2.3 模型 Provider 同步（按最新写入时间选源 + 单事务 + 失败回滚） ｜ 6.2.4 工作区成员管理 | 实例设置→配置；**设置→常规→版本数据同步（同源第二入口）** ★ |
| 6.3 Provider / 模型 | 6.3.1 `llm-deepseek` 与 `llm-pi-ai.providers` 编辑 ｜ 6.3.2 只写环境变量引用/URL/模型目录，不写 API Key ｜ 6.3.3 花括号映射规范化 | 内部服务（启动页已移除 Provider 卡片） |
| 6.4 凭据 | 6.4.1 `.credentials.yaml` 整份原子复制（不解析、不记录、≤1 MiB、拒绝重解析点） ｜ 6.4.2 旧格式（`version/records/refs`）保守归一化 | 自动 |
| 6.5 实例环境变量 | 6.5.1 增删键值 ｜ 6.5.2 保留名 `DSH_HOME`/`DSH_AGENTS_HOME`/`PATH` ｜ 6.5.3 敏感值 DPAPI 加密落盘 ｜ 6.5.4 启动时注入 | 实例设置→个性化 |

### 2.7 快照与整合包

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 7.1 本地快照 | 7.1.1 内容=版本设置/Provider/官方凭据/MCP/patch/Plugin profile ｜ 7.1.2 DPAPI 加密 ｜ 7.1.3 不含会话与运行依赖 ｜ 7.1.4 自动快照 ≤10 个且 ≤256 MiB、手动不参与清理 ｜ 7.1.5 版本配置保存与 Plugin 变更前自动建点 | 版本控制页「配置快照与回滚」 |
| 7.2 快照回滚 | 7.2.1 回滚前自动恢复点 ｜ 7.2.2 全量预处理后提交，中途失败还原已改文件 ｜ 7.2.3 单文件精确恢复 | 同上 |
| 7.3 整合包导出 | 7.3.1 `.dshpack` / `.dspack`（manifest v4） ｜ 7.3.2 可改扩展名 ｜ 7.3.3 脱敏（不含凭据/`.env`/会话/API Key/YAML 多行密钥正文） | 实例设置→导出 |
| 7.4 整合包导入 | 7.4.1 容器 v2/v3 + 旧 `.tgz`；manifest v2–v5 ｜ 7.4.2 摘要预览确认 ｜ 7.4.3 `files[]` 默认拒绝、显式同意、sha256+size 双校验、镜像回退 ｜ 7.4.4 版本不符先提示再选择 ｜ 7.4.5 一律新建实例、`package.json` 权威重建、失败整体回滚 ｜ 7.4.6 导入再次拒绝凭据路径 | 版本控制页「导入整合包」 |
| 7.5 插件失败诊断报告 | 7.5.1 本地未脱敏 zip（保留错误与凭据，排除会话/node_modules） ｜ 7.5.2 Chat 接力发送报告路径与排查指令 | 插件安装/更新/卸载失败时自动 |

### 2.8 观测 · 诊断 · 恢复

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 8.1 任务中心 | 8.1.1 长任务台账（环境准备/插件技能/实例导入/更换版本） ｜ 8.1.2 运行中取消（与进度窗双向串联） ｜ 8.1.3 保留 50 条 + 崩溃遗留归一化 ｜ 8.1.4 顶栏角标 | 任务页（顶栏导航） |
| 8.2 日志中心 | 8.2.1 只读浏览 `launcher.log` + 轮转旧文件 ｜ 8.2.2 按天分组 + 级别/实例/关键字过滤 ｜ 8.2.3 读 5000 / 渲染 500、坏行计数、不阻塞写入 ｜ 8.2.4 一键清理（活跃日志刻意保留） ｜ 8.2.5 打开日志目录 | 设置→诊断与日志→日志中心 |
| 8.3 运行状况 | 8.3.1 运行概况 ｜ 8.3.2 CPU/内存折线（20 分钟环形） ｜ 8.3.3 进程树 ｜ 8.3.4 运行日志（2000 行环形、不落盘） ｜ 8.3.5 启动证据 ｜ 8.3.6 清理残留进程（keepPid + 二次确认） ｜ 8.3.7 崩溃恢复 ｜ 8.3.8 空闲自动停止 ｜ 8.3.9 插件排查 | 实例设置→运行状况 |
| 8.4 诊断包 | 8.4.1 `--diagnose [--out]` 与设置页按钮 ｜ 8.4.2 脱敏 zip（用户目录→`%USER%`、密钥打码） ｜ 8.4.3 不含 `.credentials.yaml` 与会话；含启动证据 | 设置→诊断与日志 |
| 8.5 存储与清理 | 8.5.1 只统计/清理 Launcher 自有文件（崩溃日志/轮转/市场缓存/运行时缓存/回滚点/备份副本/换版本导出） ｜ 8.5.2 走回收站 ｜ 8.5.3 实例数据/会话/凭据/快照**只统计不清理** ｜ 8.5.4 异步（实测 13.2s → UI 不卡） | 设置→诊断与日志→存储与清理 |
| 8.6 结构化日志与错误码 | 8.6.1 JSONL 行日志（1MB 轮转） ｜ 8.6.2 错误码 E1xxx/E2xxx/E3xxx/E4xxx/E9xxx ｜ 8.6.3 崩溃日志 2MB 轮转 + 同类 60s 折叠 + 每进程 200 条 | 日志中心；`%LocalAppData%\DeepSeek\launcher\` |
| 8.7 WebView2 缓存治理 | 8.7.1 版本账本（dsh 版本变化清一次，保留 Cookie） ｜ 8.7.2 手动"清除桌面窗口缓存" | 设置→诊断与日志 |

### 2.9 安全与隐私

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 9.1 凭据体检 | 9.1.1 只报位置（文件/行/列/模式名） ｜ 9.1.2 显式开启才给脱敏预览（前 3+后 4） ｜ 9.1.3 导出 JSON（保留 20 份） ｜ 9.1.4 跳过 `node_modules`/`sessions`/`storages`；纯读取不调 CLI | 设置→诊断与日志→安全体检 |
| 9.2 危险配置检查 | 9.2.1 权限档位 / `DSH_PERMISSION_MODE` / `approval.policy` / 沙箱 `workspaceRoot` ｜ 9.2.2 遥测模式与导出口 ｜ 9.2.3 依赖来源 ｜ 9.2.4 凭据文件保护 ｜ 9.2.5 HMR | 同上 |
| 9.3 行为时间轴 | 9.3.1 读社区探针 `audit/*.jsonl`（`@marcog-h/dsh-audit`） ｜ 9.3.2 只读白名单字段，`raw` 永不读取 | 同上 |
| 9.4 启动前危险提示 | 9.4.1 权限类 4 项命中时非阻断提示一次（每会话·每实例+profile） ｜ 9.4.2 自动重启/接管/隔离启动不弹 | 启动动作前 |
| 9.5 数据保护 | 9.5.1 环境变量值 DPAPI ｜ 9.5.2 快照 DPAPI ｜ 9.5.3 诊断包/.dshpack 脱敏 ｜ 9.5.4 凭据不进日志 | 全局 |
| 9.6 来源与安装校验 | 9.6.1 MSI Authenticode 链 + 官方发布者 ｜ 9.6.2 插件装前校验 ｜ 9.6.3 GitHub 直装失败走进程级 `GIT_CONFIG_*` 镜像（不改用户 git 配置） | 安装路径 |

### 2.10 界面框架与交互基础

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 10.1 窗口骨架 | 10.1.1 无边框可调、最大化占工作区且直角、还原圆角 ｜ 10.1.2 顶部导航 6 项 + 标题栏上下文区（品牌 / 实例选择器 / 返回按钮） ｜ 10.1.3 提示条（自适应消失、悬停暂停、手动关闭、错误常驻） | 全局 |
| 10.2 尺寸与记忆 | 10.2.1 启动尺寸=最小尺寸（1180×720，小屏收缩） ｜ 10.2.2 窗口位置/大小/最大化记忆（多屏越界回中） ｜ 10.2.3 内嵌页"返回 <来源页>" | 全局 |
| 10.3 设计系统 | 10.3.1 颜色令牌（App.xaml 唯一合法处，含 C# 侧） ｜ 10.3.2 字号阶梯 11/12/13/14/15/16/18/20（+24/28/30 品牌例外） ｜ 10.3.3 圆角/间距/阴影令牌 ｜ 10.3.4 Tabler Icons 矢量图标注册表 + 图标阶梯 ｜ 10.3.5 按钮三级（Primary/Danger/次级）+ 紧凑变体 ｜ 10.3.6 胶囊 / 菜单 / 链接 / 悬浮卡片 / 空态 / 滚动条规范 | `docs/UI-DESIGN.md` |
| 10.4 列表与表格 | 10.4.1 16 个长列表显式虚拟化 ｜ 10.4.2 对话页 17 列按可用宽度代码分配 ｜ 10.4.3 条目宽度绑定列表宽度（防横向溢出） ｜ 10.4.4 滚动位置记忆（`ui-state.json`） | 各列表页 |
| 10.5 键盘与可达性 | 10.5.1 统一焦点框 `AppFocusVisual` ｜ 10.5.2 无文字按钮全部有可访问名 ｜ 10.5.3 Enter=默认、Esc=取消 ｜ 10.5.4 扩展页 Delete 删除快捷键 ｜ 10.5.5 不手动设 TabIndex | 全局 |
| 10.6 高 DPI | 10.6.1 13 个根元素像素对齐 ｜ 10.6.2 ClearType 降级仅限 MainWindow 透明窗 ｜ 10.6.3 图标为矢量字形 | 全局 |
| 10.7 自绘对话框 | 10.7.1 `AppDialog` 全量替换 `MessageBox`（54 处） ｜ 10.7.2 长文本收敛（≤120 字符/行、≤1200 总长） | 全局 |

### 2.11 设置与偏好

| L2 | L3 功能点 | 入口 |
|---|---|---|
| 11.1 运行环境 | Node/DSh 状态、DSh 安装位置、移动已有安装、重新检测、扫描自定义目录、准备运行环境（官方/镜像）、版本下载源 | 设置→运行环境 |
| 11.2 常规 | Plugin 安装模式（快速/兼容）、关闭行为、版本数据同步、实例守护间隔 | 设置→常规 |
| 11.3 插件与技能来源 | 两块平行：列表（固定高度内滚）+ 启用/测试/删除 + 添加；预置 2 个中文源（默认停用） | 设置→插件与技能来源 |
| 11.4 网络与账户 | 代理（Launcher + 实例注入）、余额显示（默认关，内存读凭据） | 设置→网络与账户 |
| 11.5 诊断与日志 | 导出诊断包、打开日志目录、存储与清理、打开日志中心、清除桌面窗口缓存、凭据体检/危险配置/行为时间轴 | 设置→诊断与日志 |
| 11.6 首次运行引导 | 无实例时自动弹出；选源 + 设 DSh 安装位置；取消后主按钮显示"准备首个版本" | 独立窗口 |

### 2.12 工程与验证

| L2 | L3 功能点 | 位置 |
|---|---|---|
| 12.1 仓库自测 | 纯逻辑、不联网、不开窗；本次 **204 PASS / 0 FAIL** | `tests/DshLauncher.SelfTest` |
| 12.2 工作区 harness | 端到端 + UI 冒烟（反射触发 Loaded，不显示窗口）；本次 **386 PASS / 0 FAIL / 0 SKIP** | `_verify-p0`（不进仓库/CI） |
| 12.3 契约哨兵 | C1–C19 与上游源码 + 已安装运行时双源比对；上游缺失整段 SKIP | `_verify-p0/Program.cs` |
| 12.4 CI | 构建 + 自测 + 发布（windows-latest，.NET 8） | `.github/workflows/ci.yml`（**见 §4 A1**） |
| 12.5 文档体系 | CHANGESETS 169 / BEHAVIOR-CHANGES 111 / VERIFICATION / UI-DESIGN / ADR×10 / BEHAVIOR_MATRIX / DIFF-REPORT / CONTRACT | `docs/` |
| 12.6 发布形态 | 默认框架依赖单文件（3.62 MB，需 .NET 8 Desktop Runtime）；可选自包含（64.8 MB）；可选引导器（源码保留） | `README` / csproj |

---

## 3. 界面承载矩阵（现有页面 × 功能域）

> 读法：把 §2 的 L1 域映射到**今天的实际入口**，用于判断"哪一页承载了多少职责"。

| 界面 | 类型 | 承载域 | 主要分区 |
|---|---|---|---|
| **主窗口·启动页** | 内嵌 | 1.3/1.4/1.6、2.1/2.2/2.3/2.12、11.6 | 左：实例状态卡（徽标×4、路径链接×2、端点、资源、余额、启动分体按钮、停止/重启、重命名/实例设置）｜右：实例列表 + 版本控制入口 |
| **扩展页** | 内嵌（`ExtensionWindow`，`agentOnly=false`） | 4.1–4.3、4.7、4.8、4.10、4.11 | 左：当前实例 + 8 个工具条按钮 + 已装列表 + 当前选择（启用/禁用/更新/删除）｜右：资料源 / 搜索 / 分类 / 排序 / 来源 + 市场卡列表 |
| **Agent 页** | 内嵌（同一控件，`agentOnly=true`） | 4.5、4.9 | 左：已装 Skill｜右：Skill 市场（分类/搜索/排序/来源） |
| **对话页** | 内嵌 | 5.1–5.3 | 顶部版本+显示筛选；Tab1 对话文件（工具条 6 按钮 + 表）；Tab2 备份与恢复 |
| **任务页** | 内嵌 | 8.1 | 一张表 + 刷新/清空历史 |
| **设置 / 诊断页** | 内嵌（代码构建） | 3.1–3.7、11.2–11.5、9.1–9.3、6.2、1.7 | 左：5 分类（运行环境/常规/插件与技能来源/网络与账户/诊断与日志）｜右：内容 |
| **版本控制页** | 内嵌 | 1.1/1.2/1.3/1.5、1.4、7.1/7.2、7.4 | 左：版本列表 + 导入实例/新建干净版本｜右：选中版本详情（版本检查、运行版本、切换历史、快照与回滚、整合包格式） |
| **实例设置页** | 内嵌 | 6.1、6.2/6.5、4.1、2.5–2.9、7.3 | 左：5 分类（个性化/配置/插件管理/运行状况/导出）｜右：内容 |
| **插件矩阵** | 内嵌 | 4.10 | 表 + 图例 + 刷新/复制 |
| **环境扫描** | 内嵌 | 1.2.4 | 列表 + 导入选中 |
| **日志中心** | 内嵌 | 8.2 | 2×2 按钮 + 过滤行 + 按天分组列表 |
| **Chat 窗口** | 独立 | 2.10 | WebView2 |
| **首次运行引导** | 独立 | 11.6 | 单页向导 |
| **新建版本 / 版本切换 / 整合包导入 / 旧包导入 / 版本不符 / 进度窗×2 / 文本输入 / 主题预览 / 自绘对话框** | 独立 | 1.3.4 / 1.4.1 / 7.4 / 7.4 / 7.4 / 3.4+4.3 / 1.3.1 等 | 模态窗 |

**两栏骨架现状（实测）**：启动页 / 版本控制 / 实例设置 / 设置页 / 扩展页左栏统一 **320 + 18**（扩展页于变更集 171 从 `0.26*` 改为固定 320）；对话页筛选行 **2\* + \***。

---

## 4. 缺口与不一致清单

> 每条给出**证据**与**建议动作**。分三类：**A 已确认的不一致/缺陷**、**B 已声明但未做**、**C 有意不做**（记录以免被当成缺陷）。

### A. 已确认的不一致 / 缺陷

| # | 项 | 证据 | 建议动作 |
|---|---|---|---|
| **A1** | **CI 会把主程序覆盖成引导器** | `.github/workflows/ci.yml` 末两步仍按变更集 165 的"两文件"形态：`Build bootstrapper` + `Copy-Item …net48\DSH Launcher.exe → dist\DSH Launcher.exe`；而变更集 169 已把主程序 `AssemblyName` 改回 `DSH Launcher`（csproj 实测），publish 产物就叫 `dist\DSH Launcher.exe` ⇒ 复制会**覆盖主程序**，产物不可用 | 删除末两步（或改为可选 job），并同步注释里的 `DSH Launcher.App.exe` 旧名 |
| **A2** | `docs/VERIFICATION.md` 前置过期 | 文档要求 `$env:DOTNET_ROOT='…\dotnet-sdk'`（"本机自备 SDK"）；README 已改为"全局 `C:\Program Files\dotnet` 8.0.425，**不需要** `DOTNET_ROOT`" | 同步 VERIFICATION.md 的 ① 与发布 SOP 段 |
| **A3** | `CURRENT_DESIGN.md` 三处过期 | ① 写"版本设置页按…**快照回滚**…组织"，实际导航为 个性化/配置/插件管理/**运行状况**/导出（快照已迁版本控制页，变更集 79）；② 写标题栏固定显示"启动、扩展、Agent、对话和设置导航"，实际含**任务**；③ 写版本控制页提供"导入实例、复制、干净版本、删除版本和整合包导入"，实际还有**检查版本/运行版本/切换历史/快照** | 按现状改写三处 |
| **A4** | 仓库根**没有 LICENSE** | `ls -a` 无许可文件；上游 v1.2.2 起为 MIT（DIFF-REPORT §4） | 若继续对外发布/PR，补 LICENSE（并确认与上游许可兼容） |
| **A5** | 两个零引用服务文件 | `Services/VersionOpenTargetService.cs`（**有意**保留，与上游 v1.0.7 逐字节一致以避免 PR 无关 diff，work-log/116/175）、`Services/ProviderDiagnosticService.cs`（**无此说明**：Provider 诊断已从界面移除，全仓 0 引用） | 前者保留并在此登记；后者要么删除、要么在 DEV_STATE 写明保留理由 |
| **A6** | 契约表编号缺 C18 | `DSH_CONTRACT_INVENTORY.md` 从 C17 直接跳到 C19 | 补一条说明（复用号/作废），避免读者以为漏抄 |
| **A7** | DEV_STATE"当前主要相关文件"含已移除能力 | 仍列 `VersionOpenTargetService.cs`；未提 `ProviderDiagnosticService.cs` 已无消费者 | 随 A5 一并整理 |
| **A8** | `dist/` 未替换安装副本 | 变更集 169 自述"**未替换安装副本**（用户口径）"；工作区 `D:\Program Files (x86)\dsh_launcher\` 下的 exe 需按发布 SOP 核对 MD5 | 做一次发布核账（停→建→测→发→核→起） |

> **状态（2026-09-24 晚，变更集 170）**：A1（CI 覆盖主程序）、A2、A3、A5（删除 `ProviderDiagnosticService.cs`）、A6、A7 **已处理**；A4 按用户决定**暂不补 LICENSE**；A8 按用户决定**保持现状**（不替换安装副本）。

### B. 已声明但未做（功能缺口）

| # | 项 | 来源 | 现状 |
|---|---|---|---|
| **B1** | 150% / 200% DPI 逐屏验收 | README 已知问题；UI-TODO §1.11 | 只以 125% 为基准；清单已写入 UI-DESIGN |
| **B2** | 主题色 5 选 / FAB / 壁纸模式 | UI-TODO §1.12 | 未做（深色模式经用户决定不做；`DynamicResource` 全仓 0 处是前置代价） |
| **B3** | 列头与行高统一；列表列头排序 | UI-TODO §1.6 | 未做（§1.6 记录了"隐式样式会崩"的禁令，需先解决外观统一方式） |
| **B4** | 术语表；按钮文案动宾一致；错误码文案一致 | UI-TODO §1.10 | 未做（术语表是纯文档项，成本低） |
| **B5** | `RollForward=LatestMajor`（.NET 9/10 机器）与"用户级免管理员装运行时" | README 已知问题（work-log/191） | 未做；仅影响"没装 .NET 又不想装"的人群 |
| **B6** | 国内镜像的**真实下载**未在本机验证 | README 已知问题（work-log/161/171） | 未做（本机已有版本，不触发下载） |
| **B7** | `MainWindow.OnClosing` 偶发 `InvalidOperationException` | README 已知问题 | 待复现定位（已有一次"关闭重入"修复，见 DEV_STATE） |
| **B8** | 插件市场"滚动追加" | work-log/79 I2 | **用户决定搁置**；当前初始展开 5000，仍走增量 |
| **B9** | 联网类自测的离线化 | BEHAVIOR_MATRIX §六、work-log/79 | 未做（测试会真连外网） |
| **B10** | 真实第三方 `.dspack` 样本；`files[]` 真实下载的自动化 | BEHAVIOR_MATRIX §六 | 未做（只有自造样本 + 往返） |
| **B11** | 与上游 v1.0.8–v1.2.4 的能力差 | DIFF-REPORT §4 | 本分支缺：**全局 Provider/模型管理页**（v1.0.8）、**下载中心页**（v1.0.9）、**可选视觉特效**（v1.2.0）、**整合包市场**（v1.2.2）、**MIT 许可**（v1.2.2）、**既有 HOME 管理**（v1.2.3）；另有"会话格式/整合包"两块**重复实现**需对接上游 |
| **B12** | 可观测性两项（实例日志落盘、沙箱失败签名提示） | work-log/197 §七 | **已评估、用户 2026-09-24 决定先不动**（降为方案存档）；本次审计确认它们仍然是真实缺口（实例日志"进程内不落盘"→ Launcher 退出后 dsh 的 stdout/stderr 永久丢失） |

### C. 有意不做（不要当缺陷重复提）

- Provider 卡片 / Provider 网络诊断：**从启动页移除**（只保留设置内的跨版本同步）。
- 深色模式、主题色多选：用户 2026-09-13 决定不做。
- 工作区权限预检、自动 `takeown`/`icacls`、给 dsh 提权：明确不做（会破坏沙箱语义与"绝不改用户配置"契约）。
- 终端启动作为"启动方式"：改为"复制命令"（变更集 112）。
- "手动绑定自定义打开方式"：删除（变更集 112）。
- 插件安装同步到所有 profile：决定不做（work-log/79 I3）。
- 插件市场"已经到底了"提示：不做（随 I2 搁置）。

### D. 待用户确认（口径不明，本文不替业务下结论）

1. **"完整"是否包含对齐上游 v1.0.8–v1.2.4**（B11）？若"要"，则需要一次 rebase/移植评估（DIFF-REPORT §5 给了三种配合方式）。
2. **`ProviderDiagnosticService`（A5）**是删除还是补回界面（曾用于"API 测试"）。
3. **快照回滚的入口**：现在只在版本控制页；`CURRENT_DESIGN.md` 的旧表述是否代表你更希望它回到实例设置？
4. **实例日志落盘（B12）**：work-log/197 记"先不动"，本次审计认为它是观测缺口——是否重新排期？

---

## 5. 面向界面布局优化的信息架构建议

> 结论先行：**功能面已经很宽（12 域 / 281 个 L3 功能点），当前主要矛盾不是"缺功能"，而是"同一功能多处入口、高频设置入口过深、页面命名与内容不匹配"。**
> 建议按"先标注去重 → 再对齐骨架 → 再动导航"的顺序，分 4 个小步走，每步都可独立验证并截图回归。

### 5.1 现状问题（全部基于代码/文档事实）

| # | 问题 | 事实依据 |
|---|---|---|
| **P1** | **同一功能两处入口**（**S1 已处理：两处都保留 + 互相跳转**）：① Plugin 管理在「扩展页左栏」与「实例设置→插件管理」都有启停删（变更集 159 只把*依赖自检*迁过去，其余仍重复）；② 版本数据同步在「设置→常规」与「实例设置→配置」是同一份数据（CURRENT_DESIGN 自述"同源"） | `ExtensionWindow.xaml(.cs)`、`VersionSettingsWindow.xaml(.cs)`、`MainWindow.CreateSettingsPage/AddVersionSyncSection` |
| **P2** | **高频设置入口过深/唯一化**（**S1 已处理：已在实例设置→个性化恢复下拉**）：启动方式（`OpenMode`：Web/Desktop/隔离）当时**只能**从启动页实例卡片的 ▼ 菜单改，实例设置里无入口（变更集 112 删掉整张卡片） | `MainWindow.xaml.cs:3752-3807`；`VersionSettingsWindow.xaml` 5 个导航项无"启动方式" |
| **P3** | ~~两栏骨架两套口径~~ **已统一（变更集 171）**：扩展页 `0.26*`(260–380) → **320+18**，与主窗/版本控制/实例设置/设置一致（⚠ 这确实回退了变更集 134 的刻意决定，系用户 2026-09-24 重新确认后所选） | 各 XAML `ColumnDefinition`（§3 末） |
| **P4** | **页面命名 ≠ 内容**：「版本控制」页实际是"实例/版本总控台"（导入实例、新建版本、版本检查、更换运行版本、切换历史、快照、整合包）；「实例设置」与「版本设置」是同一个页面的两个叫法 | `VersionControlWindow.xaml` 分区标题；MainWindow 两处调用 `CreateSettingsPage` 类入口 |
| **P5** | **日志/证据分散 4 处**：日志中心（`launcher.log`）、运行状况→运行日志（dsh stdout 环形、不落盘）、诊断包、`watchdog.log`；用户难以判断该看哪一处 | §2.8；work-log/197 §七.1 |
| **P6** | **状态/徽标分散**：实例卡片有 4 个徽标（安全模式/崩溃冷却/有新版本/呈现面）+ 资源摘要 + 余额；设置页有 Node/DSh 状态；运行状况页有启动证据与健康；启动失败又有提示条 | `MainWindow.xaml` 359–545；`MainWindow.xaml.cs` 更新状态段 |
| **P7** | **顶栏 6 项无分组**："启动/扩展/Agent/对话/任务/设置·诊断"平铺，而"任务"与"设置·诊断"同属运维类 | `MainWindow.xaml` 199–253 |
| **P8** | **空态/初值引导不均**：启动页有插画空态 + "准备首个版本"；版本列表补了空态（变更集 145）；其余页多为一行灰字 | UI-TODO §1.7 及其变更集 145 记录 |
| **P9** | **设置内"环境/账户/来源/同步"混层**：5 个分类里"插件与技能来源"其实是市场配置，"版本数据同步"其实是每实例的同步策略 | `MainWindow.xaml.cs:3349-3353`、`AddVersionSyncSection` |

### 5.2 目标信息架构（建议稿，供讨论）

**顶层导航按"意图"分三组**（保持 6 项数量不变，只加分隔与视觉分组）：

```
[实例]  启动                             ← 实例选择 + 状态 + 启动/停止 + 版本控制入口
[内容]  扩展 · Agent · 对话              ← 生态与会话
[运维]  任务 · 设置/诊断                 ← 长任务、日志、诊断、环境、偏好
```

**页面内统一成"三段式"**（对 8 个内嵌页一致，减少学习成本）：

```
① 概览行（当前对象 + 关键状态 + 主操作）   ← 已基本具备（标题栏上下文区 / 页面标题 20px）
② 操作区（工具条；主操作靠左、次级成组、>3 个自动换行）
③ 详情区（卡片/列表，滚动只发生在内层）
```

**功能归属映射（建议的"唯一主入口"）**：

| 功能域 | 建议主入口 | 现入口 | 动作建议 |
|---|---|---|---|
| Plugin 管理（启停删/更新/doctor） | 扩展页左栏 | 扩展页 + 实例设置→插件管理 | **已落地（变更集 170）**：两处都保留启停删，实例设置卡头加「去扩展页」；扩展页靠标题栏实例名进实例设置（原有路径 + 文案说明） |
| 版本数据同步 / 工作区 | 设置→常规 | 设置→常规 + 实例设置→配置 | **已落地（变更集 170）**：两处都保留完整编辑，各加一个互跳按钮（跳到设置时直接落在“常规”分类） |
| 启动方式（OpenMode） | 实例设置→个性化（下拉） | ~~仅启动页 ▼ 菜单~~ → 两处 | **已落地（变更集 170）**：已在实例设置→个性化恢复下拉，与实例卡片 ▼ 菜单共用同一份 `OpenMode`（只改启动方式、不同步、不建快照） |
| 运行日志 | 实例设置→运行状况 | 运行状况 + 日志中心 + 诊断包 | 运行状况内加「打开日志中心」；两者关系写进 tooltip（哪本日志记什么） |
| 环境/Node/DSh 状态 | 设置→运行环境 | 设置 + 启动页提示 + 运行状况 | 启动页只留"缺什么 → 去哪里"的一句话入口 |
| 快照/整合包 | 版本控制页（选中版本详情） | 版本控制 + 实例设置→导出 | 保持；把「实例设置→导出」的入口文案改为"导出到整合包"以区别快照 |
| 任务 | 顶栏任务页 | 顶栏 + 进度窗 | 保持（角标已解决"看不见长任务"） |

### 5.3 分批实施建议（每批一个变更集／一次截图回归）

| 批次 | 内容 | 是否改布局 | 风险 | 验证 |
|---|---|---|---|---|
| **S1 入口标注与去重（不改布局）** | 按 §5.2 表格逐条决定"保留哪一处"，给重复入口加"去设置/去扩展"跳转；补齐 tooltip 说明 | 极低 | 低（只加链接与文案） | SelfTest + harness UI 断言 + 单页截图（`quick-shot.ps1`） |
| ↑ **已完成（变更集 170）** | 用户口径＝两处都保留 + 互相跳转；另恢复「默认启动方式」下拉 | 极低 | 低 | 构建 0/0、SelfTest 204/0、harness **388/0/0**（含 2 条新门禁）；真机 UIA 目视未做（会触发真实同步） |
| **S2 骨架对齐** | 扩展页左栏 `0.26*` → `320 + 18` | 低 | 低（用户 2026-09-24 重新确认） | `_verify-p0` 门禁改写为「两栏页左栏统一 320+18」 |
| ↑ **已完成（变更集 171）** | `ExtensionWindow.xaml` → `320` + 18 + `*` | 低 | 低 | 构建 0/0、SelfTest 204/0、harness **388/0/0**；真机截图目视未做（开扩展页需真实实例） |
| **S3 导航分组** | 顶栏加 2 处分隔/分组；`设置/诊断` 名称按内容复核（是否改回"设置"） | 低 | 低 | 截图 + 键盘走查（Tab 顺序） |
| **S4 三态/空态统一** | 按 UI-DESIGN「空态/加载态/错误态」逐页对齐（重点：版本控制、日志中心、任务页的引导性空态） | 低 | 低 | 三态门禁已有断言，扩展覆盖 |
| **S5（可选）设置分类重整** | 把"版本数据同步"从设置分类里摘出/改名；把"插件与技能来源"改名为"市场来源" | 中 | 低 | 文档 + 截图 |

> 自查：UI 统一 A–K（变更集 127–153）已经解决了**令牌/字号/图标/圆角/按钮/三态/键盘/DPI** 这些"同一套视觉语言"的问题；
> 剩下的是**结构与入口**问题（P1–P9），因此本文的建议集中在"信息架构"而不是"样式"。
>
> **进展（2026-09-24 晚，变更集 170 = S1）**：P1（两处重复入口）、P2（启动方式入口错位）**已按用户选择处理**——两处入口都保留并互相跳转（实例设置→插件管理 ↔ 扩展页；实例设置→配置 ↔ 设置→常规）；「默认启动方式」下拉回到「实例设置 → 个性化」，与实例卡片 ▼ 菜单同一份数据。
> **S2 已完成（变更集 171）**：扩展页左栏 `0.26*`(260–380) → **320+18**，与其余两栏页一致；已明确知道这会回退变更集 134 的自适应，属用户重新确认后的选择。至此 **P1/P2/P3 已处理**，剩 P4–P9（页面命名、日志分散、状态分散、顶栏分组、空态、设置分类）。
> ⚠ S1 的**未覆盖**：新下拉与两个跳转按钮未做真机 UIA 目视（点开会触发真实实例的跨版本同步，故未做），仅由构建 0/0 + SelfTest 204/0 + harness 388/0/0（含 2 条结构门禁）保护。

---

## 6. 附录

### 6.1 代码规模与文件索引（本次实测）

| 位置 | 数量 | 说明 |
|---|---|---|
| `src/DshLauncher/Services/` | 105 个 `.cs` | 业务逻辑（含 `ExtensionService` 3 个 partial） |
| `src/DshLauncher/Models/` | 25 个 `.cs` | 数据模型 |
| `src/DshLauncher/Watchdog/` | 6 个 `.cs` | 进程内守护 + 进程查询 + 资源采样 |
| `src/DshLauncher/Controls/` | 3 个 | `UiIcon`、`MetricChart` |
| 窗口 / 内嵌页 XAML | 5 个 Window + 8 个 UserControl | 另有 7 个纯代码窗口（引导/进度/导入/提示/预览/自绘对话框） |
| `MainWindow.xaml.cs` | 8,320 行 | 启动页 + 设置页（代码构建）+ 生命周期编排 |
| `src/DshLauncher/Services/` 最大文件 | 2,418 / 2,118 / 1,562 行 | `MarketplaceService` / `ExtensionService` / `DshInstanceRunner` |
| `tests/DshLauncher.SelfTest/Program.cs` | 2,824 行 | 204 断言 |
| `_verify-p0/Program.cs` | 5,402 行 | 386 断言（工作区级，不进仓库） |

### 6.2 复现命令

```bash
# 构建 + 自测
dotnet build src/DshLauncher/DshLauncher.csproj -c Release
dotnet run --project tests/DshLauncher.SelfTest/DshLauncher.SelfTest.csproj -c Release

# 工作区 harness（需本机已装 dsh 运行时 + 上游源码克隆；先停启动器）
cd _verify-p0 && dotnet build -c Release
dotnet bin/Release/net8.0-windows/win-x64/VerifyP0.dll --ui

# 发布（先停启动器，否则 MSB4018）
dotnet publish src/DshLauncher/DshLauncher.csproj -c Release -o dist
```

### 6.3 变更记录

| 日期 | 内容 |
|---|---|
| 2026-09-24 | 建立本清单：三级分类（12 域 / 85 个 L2 / 281 个 L3）+ 界面承载矩阵 + 完整性四口径判定 + 缺口清单（A1–A8 / B1–B12 / C / D）+ 信息架构建议（P1–P9 / S1–S5）；附本次实测证据（构建 0/0、SelfTest 204/0、harness 386/0/0、真机冒烟） |
| 2026-09-24（晚） | **变更集 170（S1）**：A1/A2/A3/A5/A6/A7 已处理（A4 暂不补、A8 保持现状）；P1/P2 已处理（两处入口互相跳转 + 默认启动方式回到实例设置）；验证 构建 0/0 / SelfTest 204/0 / harness **388/0/0**（+2 条 S1 门禁） |
| 2026-09-24（晚） | **变更集 171（S2）**：扩展页左栏 `0.26*` → **320+18**（回退变更集 134，用户重新确认）；`UI-DESIGN.md` 补「两栏页左栏」规则；harness 门禁改写；验证 构建 0/0 / SelfTest 204/0 / harness **388/0/0** |
