# 变更集清单（相对上游 v1.0.7）

> 本文件由仓库 [README](../README.md) 拆分而来：`dsh-launcher-dev` 相对上游
> `121103qwq/DSH-Launcher` **v1.0.7**（`846c44c1`）的**全部改动**，按变更集编号升序排列。
> 每一号 = 一次可独立验证的改动（构建 0 警告 0 错误 → 仓库自测 → harness → 发布 → 台账）；
> 逐条过程记录（背景 → 证据 → 方案 → 实施 → 验证）在工作区的 `work-log/`（不随本仓库发布）。
>
> - **1–20**：fork 后第一批修复与增强
> - **21 起**：P0 借鉴落地（2026-09-09，基于 work-log/27 的三仓对比，提交见 `git log`）
> - 说明（原文保留）：其余文件与上游逐字节一致；构建 0 警告 0 错误；功能全部实测通过。

| # | 文件 | 变更 |
|---|---|---|
| 1 | `Services/ExtensionService.ProfileRepair.cs` | **新增**：方案 A 插件依赖自愈（`EnsureProfileDependenciesAsync`：bundle/依赖不可解析 → profile 目录 `pnpm install` → 复检）+ `FindMissingBundlePackages` + `BuildPnpmStartInfo`（Windows .cmd 经 cmd.exe 包装） |
| 2 | `Services/ExtensionService.cs` | `class` → `partial class`（配合新文件） |
| 3 | `Services/DshInstanceRunner.cs` | 注入 `ExtensionService?`；`StartAsync` 导入实例自愈调用（失败即中止并给可操作错误）；`openBrowser` 参数 + `SupportsNoOpen` 版本守卫（≥0.1.0-rc.8 才传 `--no-open`，0.1.1-rc.2 正确支持） |
| 4 | `MainWindow.xaml.cs` | 共享 ExtensionService 接线；托盘（关窗驻留/双击恢复/菜单退出）；启动分流（Web→浏览器 / Desktop→Chat / 运行中重开同样分流）；按钮文案（运行中→「打开实例」）；关闭行为两选项（最小化到托盘/关闭启动器与实例）；最大化 `WM_GETMINMAXINFO`（占满工作区）+ `UpdateMaximizeVisuals`（直角化） |
| 5 | `MainWindow.xaml` | 标题栏：DeepSeek 图标 Image（后恢复 DSHLauncher）、三个窗口控制按钮统一圆角方形（最大化 50×50）、根元素命名（AppRootBorder/TitleBarBorder） |
| 6 | `Services/TrayIconService.cs` | **新增**：WinForms NotifyIcon（零新依赖） |
| 7 | `Models/VersionSettingsModels.cs` | `VersionOpenMode` 重定义 `{Web, Desktop, Custom}` + 旧值转换器；`CloseBehavior` 两选项 + `CloseBehaviorConverter`（旧 Exit → ExitAndStopInstances） |
| 8 | `VersionSettingsWindow.xaml(.cs)` | 启动方式三选项 + 关闭行为相关文案；移除 DSH Desktop 校验 |
| 9 | `Services/VersionPackageService.cs` | 旧 `VersionOpenMode.Launcher` → `Desktop` |
| 10 | `ExtensionWindow.xaml` | 市场卡片强制撑满整行（`HorizontalContentAlignment=Stretch`，插件市场 + Skill 市场） |
| 11 | `ChatWindow.xaml` | Chat（Desktop 启动）窗口图标 → `DeepSeekOpenPlatform.ico`（用户提供的"DeepSeek 开放平台.ico"） |
| 12 | `DshLauncher.csproj` | 图标资源声明（DeepSeekOpenPlatform.ico 入 `<Resource>`）；ApplicationIcon 维持 `DSHLauncher.ico`（启动器原样） |
| 13 | `Services/MarketplaceService.cs` | **插件市场修复**：① 明确 DeepSeek 官方（`@deepseek-ai/*` 包 / `deepseek-ai` 仓库）条目标记为 DSh 官方来源且保留原来源（“DSh 官方”筛选不再恒空；“精选”/社区目录筛选仍可见），合并以官方为主来源；② `NormalizeCategory` 未命中关键词时归一为「未分类」，不再出现无法筛选的孤儿类别 |
| 14 | `ExtensionWindow.xaml` | 插件市场分类新增「未分类」Tab（与 13 的类别封口配套） |
| 15 | `ExtensionWindow.xaml.cs` | 运行中 dsh-market 热加载「更新」增加与安装对称的前检：插件不在 dsh-market 目录时轻提示“停止实例后普通更新”，不再触发回档+诊断报告重流程 |
| 16 | `ExtensionWindow.xaml(.cs)` | 页面高度固定为视口可用高度（`RootLayout.Height`）：插件数量多时滚动只发生在内层列表，不再出现内外两级滚动条；**同时移除** 4dd3644 加入的“插件市场自定义目录”配置口（用户反馈目录格式不兼容暂缓；对应代码仅在历史提交中） |
| 17 | `Services/DshInstanceRunner.cs` 等 | **dsh 0.1.2-rc.1 浏览器 token 适配**：新版 web 应用要求 `?token=` 认证（裸地址 401），启动输出行 `dsh web: http://127.0.0.1:<port>/?token=…` 仅打印在进程 stdout。启动后从进程输出解析带 token 地址（事件捕获+缓冲扫描双通道，健康检查后最多等 5s），存入 `Instance.AuthenticatedWebUrl`（与 `WebUrl` 共存：后者保持裸地址语义，用于展示/校验/attach）；Chat 窗口与 Web 模式重开浏览器均优先使用带 token 地址（WebView2 首次导航即种 cookie，之后免 token）；停止/清空时同步清空；adopt 与 attach 场景无法取得 token 时回退裸地址（用户可自行粘贴带 token URL） |
| 18 | `MainWindow.xaml.cs` | **启动崩溃修复**：`HandleWmGetMinMaxInfo` / `ClampMaximizedWindowPos` 对 `lParam==NULL` 的早期 `WM_GETMINMAXINFO`/`WM_WINDOWPOSCHANGING` 未防护，窗口初始化阶段触发 `NullReferenceException`（crash.log 实测抓到的启动崩溃）；两处均早退。修复后干净启动无新崩溃日志 |
| 19 | `App.xaml` / `MainWindow.xaml(.cs)` | **PCL2 风 P0 打磨**：①配色升级为 PCL 式 10 级蓝渐变体系（`#0B5BCB→#1370F3→#4890F5→#96C0F9→#D5E6FD→#E0EAFD→#EAF2FE`）+ 灰阶/红绿强调色；②标题栏线性渐变（左深右亮的 PCL 式）+ 返回/实例胶囊改半透明白；③顶部导航胶囊化（圆角 10、hover `#55FFFFFF`、pressed `#B3FFFFFF`）；④窗控按钮改 34px 圆形（hover 半透明白、**关闭钮悬停红底**）；⑤启动面板右栏加 PCL 式渐变圆空态插画；⑥**窗口工作区自适应**（≤92% 工作区并居中，DPI 感知——修复 125% DPI 下窗控按钮被裁出屏幕右缘的长期问题）；⑦字体加雅黑回退 |
| 20 | `src/DshLauncher/Watchdog/`（新目录）+ `MainWindow.xaml.cs` + `App.xaml.cs` | **实例守护（进程内版）**：无独立 watchdog 进程——监控循环内建于 Launcher（后台 5s 定时），零额外 exe/管道/Mutex。职责：①实例台账（注册/注销/快照，落盘 `%LocalAppData%\DeepSeek\launcher\watchdog-state.json`）；②周期探测——**识别 dshmarket 自重启换 PID 的幽灵实例**（登记 PID 死 + 端口活 → 反查新 PID → 身份校验（DSH_HOME 命令行或 dsh web 特征）→ 转正）；③**接管重启**：用户决策——非启动器重启的实例不收养，Launcher 追加一次标准重启（杀无主实例连同 powershell/cmd 包装链→终端窗口消失，CreateNoWindow 重拉，完全受管）；④停止判定 15s 宽限（覆盖市场重启窗口）+ 5 分钟重生观察窗；⑤残留清理（桌宠 electron/市场 helper 按 DSH_HOME 命令行 + 端口）；⑥正常退出全量收尾清账；崩溃兜底=下次启动台账恢复 + 孤儿检测提示 |
| 21 | `Services/ErrorCodes.cs`（新） | 错误码目录（E1xxx 运行环境 / E2xxx 插件 / E3xxx 网络代理 / E4xxx 体验 / E9xxx 内部），用户可见错误与结构化日志共用同一套码（借鉴 Ruler4396，MIT） |
| 22 | `Services/LauncherLog.cs`（新） | 轻量 JSON 行结构化日志 `%LocalAppData%\DeepSeek\launcher\launcher.log`（1MB 轮转），写入失败静默降级 |
| 23 | `Services/WindowStateStore.cs`（新）+ `MainWindow.xaml.cs` | 窗口位置/大小/最大化记忆：关闭写 `window-state.json`，启动多屏可见性校验后恢复（越界回退居中）；已恢复时跳过首次居中重算 |
| 24 | `Services/WebCacheVersionLedger.cs`（新）+ `MainWindow.xaml.cs` | WebView2 缓存版本账本：dsh 版本变化时清一次磁盘缓存（保留 Cookie/登录态）；无基线只写基线、空版本不清不写；仅在无其它 Chat 窗口时执行 |
| 25 | `Services/DiagnoseExportService.cs`（新）+ `App.xaml.cs` + 设置页 | `--diagnose [--out path]` 与设置页按钮：导出脱敏 zip（env/日志/状态/错误码汇总/实例设置），用户目录→%USER%、密钥值打码，**不含 .credentials.yaml/会话** |
| 26 | `Services/ProxySettings.cs`（新）+ `Models/VersionSettingsModels.cs` + `DshInstanceRunner.cs` + 设置页 | Launcher 级代理：`HttpClient.DefaultProxy` 全局生效 + 启动实例注入 `HTTP(S)_PROXY/NO_PROXY`；地址归一化/校验，非法只告警不阻断 |
| 27 | `Services/BalanceService.cs`（新）+ `MainWindow.xaml(.cs)` + 设置页 | DeepSeek 余额卡片（默认关闭，设置页显式启用）：内存读取所选实例 DSH_HOME 的凭据并查询余额，不落盘/不写日志/不进诊断包 |
| 28 | `Services/BrowserGuard.cs`（新）+ `MainWindow.xaml.cs` | 浏览器守卫：仅当 dsh 不支持 `--no-open` 且本次不由 Launcher 开浏览器时，启动后 30s 内结束命令行带该端口的浏览器进程 |
| 29 | `Services/ExtensionService.Diagnostics.cs`（新）+ `ExtensionWindow.xaml(.cs)` | 插件 doctor（核心包混入/代际错配/bundle 缺失，核心 bundle 由 CLI 嵌套树提供不再误报）+ 更新检查（node_modules 真实版本 vs registry latest，npmmirror→npmjs）+ 批量更新（失败不中断其余）+ 市场搜索/筛选/滚动持久化 |
| 30 | `Services/UiStateStore.cs`（新）+ `Services/TrayIconService.cs` + `MainWindow.xaml.cs` | UI 状态持久化（`ui-state.json`）；托盘“运行中的实例”二级菜单（每实例 打开/停止） |
| 31 | `Services/PnpmFailureClassifier.cs`（新）+ `Services/PluginCommandFailedException.cs`（新）+ `Services/ExtensionService.cs` | pnpm 失败分类（20 种模式→可操作中文提示）+ 瞬时/GitHub 网络失败自动重试一次 + 三段式错误提示（原因/重试/残留清理） |
| 32 | `Services/PluginProfileResidue.cs`（新）+ `Services/GitMirrorEnvironment.cs`（新） | 失败安装残留回滚（只清本次新增的 package.json 依赖/bundles、node_modules、cordis.patch.yml）；GitHub 直装失败依次用 `GIT_CONFIG_*` 进程级镜像重写（gh-proxy/gitclone，不改用户 git 配置） |
| 33 | `Services/PortableNodeService.cs`（新）+ `NodeRuntimeDetector.cs` + `MainWindow.xaml.cs` | 便携版 Node（免管理员）：npmmirror/官方源下载 zip → 校验 node.exe 可运行 → 原子替换到 Launcher 数据目录 `node\`，检测时优先于系统 Node；低于社区红线 22.19 自动抬版；失败时再询问是否改用官方 MSI |
| 34 | `MarketplaceService.VerifyManualInstallAsync`（新）+ `ExtensionWindow.xaml.cs` | 手动安装「装前校验」：npm 包查 registry 清单（npmmirror→npmjs）、GitHub 查 raw package.json、本地路径直接读，要求 `dsh.bundle.patch` + 可加载入口（main/module/exports/dsh.client）；普通 npm 包默认拦截（可显式确认后继续）；无法联网/无法识别时放行但提示 |
| 35 | `ExtensionWindow.xaml(.cs)` + `App.xaml.cs` | 扩展页左栏实例路径改为“尾部省略 + Tooltip + 复制按钮”（不再换行挤占列表）；`App.OnStartup` 增加仅验证用的 `DSH_LAUNCHER_VERIFY=1` 开关（不执行单实例/代理/激活管道） |
| 36 | `ExtensionWindow.xaml(.cs)` | 路径交互升级为“超链接 + 悬浮卡片”：实例目录/DSH_HOME 与插件详情来源均显示尾部、点击即复制、悬停出独立浮层卡片（含完整路径/类型/状态/描述）；去掉单独的复制按钮；按钮区与选择面板重新排版（热加载开关独立一行） |
| 37 | `ExtensionWindow.xaml(.cs)` | 实例头部压缩为一行：实例名 + 版本·类型紧跟（间距 8px、**下对齐**，名字超长省略，最大 190px），去掉原来版本/installed 各占一行的空白 |
| 38 | `MainWindow.xaml(.cs)` | 启动页「实例状态」胶囊清晰度：小字 11→12px 加粗、加 1px 同色描边、背景不透明度 38→52（默认 30→44）、`SnapsToDevicePixels`/`UseLayoutRounding`；卡片阴影改为**独立分层**（不再把卡片内文字一起栅格化） |
| 39 | 全部窗口 XAML + `App.xaml` + `docs/UI-DESIGN.md`（新） | 全量 UI 审查与规范化：4 处阴影挂内容→独立分层；语义色/圆角/间距令牌化（8 种硬编码语义色清零）；补齐 CheckBox/ProgressBar 自定义样式；市场列表去 `MaxHeight`、备份表列宽收敛、主窗口间距收敛；新增长期设计规范文档 |
| 40 | `MainWindow.xaml.cs`（`CreateSettingsPage`） | 「设置 / 诊断」改为**左侧分类栏 + 右侧内容**（运行环境 / 常规 / 网络与账户 / 诊断与日志），与版本设置页同一套 `NavButton` 导航外观；分类切换保持滚动位置在右侧面板 |
| 41 | `MainWindow.xaml(.cs)` + `VersionControlWindow.xaml(.cs)` + `NewVersionWindow.xaml.cs` | 设置页分类栏改为**圆角卡片**；「新建干净版本」**先弹窗再异步补全官方版本列表**（原来要等 npmjs 响应数秒，像按钮坏了）+ 忙碌态禁用/重入保护 |
| 42 | `VersionControlWindow.xaml.cs` + `MainWindow.xaml` + `VersionPackageService.cs` | 修复创建版本失败（后台线程读对话框 WPF 控件 → 先在 UI 线程取值）；窗口尺寸语义纠正为“**最小尺寸 = 启动尺寸**”（1180×720）；删除版本后清理 `instances/<id>` 空壳目录 |
| 43 | `MainWindow.xaml.cs` | 修复非最大化下右侧/底部内容被裁切：`Window_OnLoaded` 对 `SystemParameters.WorkArea`（本就是 DIP）**二次除以 DpiScale** → Width 小于 MinWidth，原生窗口偏小、布局偏大；删除该冗余块（`WindowSizeHelper.FitInitialSize` 已正确处理），并新增启动尺寸诊断日志 |
| 44 | `Services/DshInstallMoveService.cs`（新）+ `MainWindow.xaml.cs` | 「移动已有安装」：把已安装的 DSh 运行目录（含 `versions/`）整体搬到新位置——同盘原子重命名、跨盘复制+校验+删源；同步重写实例的 RootPath/Exe/LaunchSpec 与 `DshInstallDirectory`；目标非空/实例运行中/嵌套目录均拒绝，失败尽力回滚（E1008） |
| 45 | `Services/FileSystemCleanup.cs`（新）+ 11 处删除点 + `DshInstallMoveService` + `VersionSnapshotService` | 修复“删除版本 Access denied”：dsh 的 attachments 对象存储带 ReadOnly，递归删除前先清属性（11 处统一收口）；「移动已有安装」改为以**当前选中实例的运行时**为源（回退配置目录）；新增快照单文件恢复 `TryExtractSnapshotFile` |
| 46 | `Services/FileSystemCleanup.cs` + `VersionControlWindow.xaml.cs` | 删除版本确认框预扫描并提示“含 N 个只读文件，删除时会自动清除只读属性”（`CountReadOnlyFiles`） |
| 47 | `MainWindow.xaml.cs` | 「移动已有安装」新增**「要移动的运行时」下拉框**（配置的安装位置 + 各实例运行时，去重），不再依赖启动页的选中实例；移动成功后自动重建列表 |
| 48 | `MainWindow.xaml.cs` | 「要移动的运行时」改为**圆角卡片 + 复选框行**（每行显示名称与完整当前位置，单选）；「重新检测 / 扫描自定义目录」上移到卡片之上（按反馈去掉了卡片内重复的扫描按钮） |
| 49 | `App.xaml` + `MainWindow.xaml(.cs)` + `Services/PathDisplay.cs`（新）+ `ExtensionWindow.xaml(.cs)` | 路径链接样式提升为全局；启动页「实例状态」卡片的 目录/DSH_HOME 改为链接（点击复制、悬停悬浮卡片、未选实例隐藏）；`Tail` 尾部显示逻辑收口到 `PathDisplay` |
| 50 | `MainWindow.xaml.cs` + `Services/DshInstallMoveService.cs` | 运行时复选框改为**独立多选**（不再互相取消）；新增 `MoveManyAsync`：勾选多个时每个运行时分别搬入目标目录下的同名子文件夹，安装位置指向目标根 |
| 51 | `MainWindow.xaml.cs` | 「要移动的运行时」卡片**只列实例**（每行：实例名 + 其运行时路径）；配置的安装位置无实例关联时不再显示（无实例时卡片给提示） |
| 52 | `MainWindow.xaml.cs` | 卡片**列出全部已登记实例**（不再限于 Launcher 数据根；仅排除系统 npm/nodejs 目录）；`<root>\versions\<ver>` 正确归一到 `<root>`；共用同一运行时的多个实例合并为一行（标签用、分隔） |
| 53 | `Services/LauncherPaths.cs` + `Services/VersionSettingsService.cs` + `Services/ErrorCodes.cs` | **默认安装位置改为 `<exe 同目录>\run_time`**（便携优先）；exe 同目录不可写时自动回退旧默认 `<数据根>\runtime\dsh` 并记 E1009；设置页文案同步 |
| 54 | `DshLauncher.csproj` + `Services/WebView2DataFolder.cs`（新）+ `ChatWindow.xaml.cs` + `Services/WebCacheVersionLedger.cs` + `Services/LauncherPaths.cs` | **独立运行/便携化**：① 自包含单文件发布参数写进 csproj（裸 publish 即得 0 dll 单文件）；② WebView2 数据目录 exe 旁不可写 → 回退 `%LocalAppData%`（E1010，缓存账本同步双布局）；③ 便携数据根：exe 旁 `launcher-data` 或 `DSH_LAUNCHER_DATA_ROOT`（不可写回退 E1011）；诊断包新增 `webview2_data=` |
| 55 | `Services/DshCredentialStoreNormalizer.cs`（新）+ `Services/DshHomeImportService.cs` + `Services/ModelProviderSyncService.cs` + `Services/VersionSnapshotService.cs` + `MainWindow.xaml.cs` | **上游对照后的三个同源缺陷修复**：① 旧格式凭据（version/records/refs）保守归一化（E1012），接入导入/覆盖/合并/Provider 同步/快照恢复五处；② 快照补 `profiles/web/pnpm-workspace.yaml`（pnpm 构建许可）；③ 列表 `SelectedItem` 瞬时清空防护（实例仍在列表中则忽略并回写绑定） |
| 56 | `Models/DshEnvironmentVariables.cs`（新）+ `Services/VersionSettingsService.cs` + `Services/DshRuntimeCommandFactory.cs` + `Services/DshInstanceRunner.cs` + `VersionSettingsWindow.xaml(.cs)` + `App.xaml.cs` + `ExtensionWindow.xaml` + `ConversationWindow.xaml` | **短件三项**：① 实例级环境变量（保留项 DSH_HOME/DSH_AGENTS_HOME/PATH；敏感值 DPAPI 加密落盘、读取还原；启动时注入；版本设置页可增删保存；E1013）；② 启动阶段异常不再无窗口驻留（无主窗口则提示 + 退出）；③ UI 尾巴：市场/技能描述限高 + 省略号 + ToolTip、对话页筛选行自适应、ClearType 范围澄清 |
| 57 | `Watchdog/InstanceResourceSampler.cs`（新）+ `Watchdog/WatchdogCore.cs` + `Watchdog/WatchdogRuntime.cs` + `MainWindow.xaml(.cs)` + `Services/ConversationService.cs` + `Models/EcosystemModels.cs` + `ConversationWindow.xaml(.cs)` | **会话全文检索 + 实例资源监控**：① 资源采样挂在守护进程既有探测轮（CPU/内存/运行时长/进程数，无独立定时器），启动页卡片展示；② 修孤儿检测误报（根 PID → 认整个进程树）；③ 对话页全文检索（JSONL+zstd、32MiB/文件上限、损坏跳过、命中排序、片段高亮位置、跨实例双击打开） |
| 58 | `VersionSettingsWindow.xaml(.cs)` + `Controls/MetricChart.cs`（新）+ `Services/InstanceLogBuffer.cs`（新）+ `Watchdog/InstanceResourceSampler.cs` + `Watchdog/WatchdogCore.cs` + `Watchdog/WatchdogRuntime.cs` + `Watchdog/ProcessQuery.cs` + `App.xaml` + `MainWindow.xaml.cs` + `Models/InstanceHealthModels.cs`（新） | **实例设置「运行状况」大栏**：运行概况 + CPU/内存折线图（20 分钟环形历史）+ 进程树 + 运行日志窗（dsh 输出/生命周期事件，2000 行环形不落盘）+ 清理残留进程（keepPid 保留本体 + 二次确认）；顺带修两个真实缺陷：`FindProcessesForDshHome` 因 `Name=="cmd"` 永不匹配导致按 DSH_HOME 清理静默失效、运行状况页头标题漏分支 |
| 59 | `Services/SafeProfileService.cs`（新）+ `Services/StartupHealthEvidence.cs`（新）+ `Services/DshInstanceRunner.cs` + `Models/DshInstanceRunResult.cs` + `MainWindow.xaml.cs` + `ChatWindow.xaml.cs` + `Services/ErrorCodes.cs` | **#11 安全模式 + 四层启动健康证据（第一阶段）**：① `.dsh-safe` 隔离 profile（Tier1 剥离第三方保留核心 / Tier2 最小、用户文件只读、SHA-256 零污染证据、正常启动自动清理）；② 启动健康四层证据：进程（exitCode）、日志（新增行签名）、HTTP（探针异常只记不判）、页面（WebView2 失败上报）；③ 启动失败且存在第三方插件时**会话内只问一次**、Tier1→Tier2 自动降级；提示区分安全模式/普通启动（E1014/E1015） |
| 60 | `ChatWindow.xaml.cs` + `Services/StartupEvidenceStore.cs`（新）+ `Services/HttpHealthMonitor.cs`（新）+ `Watchdog/WatchdogRuntime.cs` + `MainWindow.xaml(.cs)` + `VersionSettingsWindow.xaml(.cs)` + `Models/InstanceHealthModels.cs` | **#11 安全模式第二阶段**：① 页面层升级为判死输入（`ProbePageAsync` DOM 探针 + 失败时安全模式重启提议）；② 「运行状况」新增“启动证据”区块（进程/日志/HTTP/页面，含接管记录）；③ 就绪后 HTTP 连续 3 次失败报降级（探针异常按 miss 计）；④ 实例卡片“安全模式”徽标 |
| 61 | `Services/InstanceIdleTracker.cs`（新）+ `Services/DshInstanceRunner.cs` + `Watchdog/ProcessQuery.cs` + `MainWindow.xaml.cs` + `VersionSettingsWindow.xaml(.cs)` + `Models/VersionSettingsModels.cs` | **空闲自动停止（可开关）**：每实例开关 + 5–240 分钟阈值；后台任务不算空闲（进程树外部连接/CPU≥1%/会话写入/dsh 输出/Launcher 忙）；停止复用既有链路并写证据；修两个真缺陷（活动信号漏接 CPU 与写入；运行状况页每秒刷新覆盖用户正在编辑的控件） |
| 62 | `Services/StartupEvidenceStore.cs` + `MainWindow.xaml.cs` + `VersionSettingsWindow.xaml(.cs)` + `Services/DiagnoseExportService.cs` | **启动证据落盘**：内存环形 + `<DSH_HOME>/.dsh-launcher/startup-evidence.jsonl`（最近 200 条、每 25 次追加裁剪、损坏行容忍）；运行状况页「清空启动证据」；诊断包附带证据文件 |
| 63 | `Models/CrashRecoveryModels.cs`（新）+ `Services/CrashRecoveryService.cs`（新）+ `Services/DshInstanceRunner.cs` + `MainWindow.xaml(.cs)` + `VersionSettingsWindow.xaml(.cs)` + `Models/VersionSettingsModels.cs` + `Services/ErrorCodes.cs` | **崩溃恢复策略**：每实例「仅通知 / 自动重启（退避 5s→15s→1m→5m，默认上限 5）/ 直接冷却关闭 / 重启后冷却」；主动停止不误判（手动/空闲/安全模式/更新/退出）；崩溃现场落盘（退出码/运行时长/日志尾部/证据/资源，最近 10 条）；冷却跨 Launcher 重启保留 + 「崩溃冷却」徽标 + 「清冷却并重启」；稳定运行 10 分钟重置计数；E1016 |
| 64 | `Services/PluginBisectService.cs`（新）+ `Services/SafeProfileService.cs` + `Services/DshInstanceRunner.cs` + `MainWindow.xaml.cs` + `VersionSettingsWindow.xaml(.cs)` + `Services/ErrorCodes.cs` | **逐插件定位**：隔离 profile（复制清单 + 改写 bundles + node_modules 目录联接）二分禁用，2–4 轮锁定肇事插件；结果一键「禁用并正常启动」（自动存回滚点）；清理不跟随联接；E1017 |
| 65 | `Models/CrashCauseModels.cs`（新）+ `Services/CrashCauseClassifier.cs`（新）+ `Models/CrashRecoveryModels.cs` + `Services/CrashRecoveryService.cs` + `MainWindow.xaml.cs` + `VersionSettingsWindow.xaml(.cs)` | **崩溃原因归类**：有序规则表（Node 不可用/端口占用/模块解析/插件异常/内存/权限/磁盘/正常退出）+ 端口与 Node 探针；置信度高/中/低（低置信只标未知）；原因落盘（记录 + 状态 + 证据 + 建议）；端口冲突自动先清残留再重启 |
| 66 | `Models/PluginMatrixModels.cs`（新）+ `Services/PluginMatrixService.cs`（新）+ `PluginMatrixWindow.xaml(.cs)`（新）+ `ExtensionWindow.xaml(.cs)` + `MainWindow.xaml.cs` | **插件 × 实例矩阵**：行=至少在一个实例启用的插件（排除核心/运行时依赖假行），列=实例，单元格三态（已启用/已装未启用/未安装）；动态列 + 复制为 TSV；只读视图 |
| 67 | `ExtensionWindow.xaml` + `docs/UI-DESIGN.md` | **扩展页工具条按钮紧凑化**：当前实例卡内 9 个按钮改 `CompactToolbarButton`（12px / Padding 9,4 / Margin 0,0,6,6），主操作同理；实测 44→33 设备像素、3 行→2 行 |
| 68 | `Services/DshInstanceRunner.cs` + `MainWindow.xaml.cs` | **外部连接（Attached）失效收敛**：修 `_attached` 只加不删（没开实例却提示“请先停止实例”）；按 PID/端口活性校验并自动摘除；外部实例也登记 watchdog；5s 周期收敛 + 停止守卫区分死活 |
| 69 | `Services/StartupHealthEvidence.cs` + `Services/DshInstanceRunner.cs` + `Services/DialogText.cs`（新）+ `MainWindow.xaml.cs` | **启动健康检查日志基线 + 对话框文本收敛**（work-log/47）：① 健康检查只扫本次进程启动后的新增日志行（`logBaselineAt` + `StartupLogClassifier.Since`），修复“旧失败签名行让本会话内后续启动/安全模式/自动重启瞬间假失败”（清空日志才恢复）；② 新增 `DialogText.ForMessageBox`（每行 ≤120 字符硬折 + 总长 ≤1200 + 截断提示），安全模式询问改用 `FormatStartFailure` 摘要 + 证据 + 插件清单 + 指向运行日志（原来原样塞 ~5.3KB 输出，弹窗实测 1584×1182、是/否按钮出屏），「启动详情」弹窗同步收敛 |
| 70 | `Services/PluginCompatibility.cs`（新）+ `Models/DshCoreBundles.cs`（新）+ `Services/MarketplaceService.cs` + `Models/MarketplaceModels.cs` + `ExtensionWindow.xaml.cs` + `Services/DshInstanceRunner.cs` + `Models/ManagerInstance.cs` + `Services/ExtensionService.cs` + `Services/PluginMatrixService.cs` + `Services/SafeProfileService.cs` + `MainWindow.xaml.cs` | **46 号遗留 4 项（work-log/48 验证、49 实施）**：① **装前/更新前兼容性预检**——新增 `PluginCompatibility`（npm 语义化范围匹配含预发布规则 + 实例内实际版本解析），手动安装/市场安装/单条更新/批量更新均检查 `@deepseek-ai/*` peerDependencies，不兼容返回 `Incompatible` 并警告（computer-user 这类“装完就崩”会被拦住；批量更新自动跳过）；② **生命周期日志**——启动/停止写 `launcher.log`（实例名/端口/PID/normal|safe）；③ **核心包口径统一**——新增 `DshCoreBundles`（计数/列表/矩阵/安全模式共用），实例卡片不再把核心算成插件、插件管理/扩展页不再列核心；④ **harness 隔离**——`ExtensionWindow.UiStateStore` 可注入，UI 冒烟用临时目录，不再污染真实 `ui-state.json` |
| 71 | `Services/DshEnvironmentScanner.cs`（新）+ `Services/ScannedHomeImportService.cs`（新）+ `EnvironmentScanWindow.xaml(.cs)`（新）+ `VersionControlWindow.xaml(.cs)` + `MainWindow.xaml.cs` | **#14 本机 DSH 环境扫描导入（work-log/50）**：扫描 `%USERPROFILE%\.dsh*` + `DSH_HOME`，按 `dsh.profile.bundles` 分类 web/tui/other；勾选后一 home 一实例（只收 `profiles/web`，重复源跳过、失败回滚）；「导入实例」菜单新增「扫描本机 DSH 环境」与接上死代码的「从源码目录导入」 |
| 72 | `Services/DshEnvironmentScanner.cs` + `Services/ScannedHomeImportService.cs` + `Services/DshHomeImportService.cs` + `EnvironmentScanWindow.xaml(.cs)` | **清理：移除 WSL 扫描冗余**（work-log/50 第五节）：本机 WSL 服务被禁用（`Wsl/0x80070422`），WSL 分支无法验证且无使用场景，删除发行版枚举/`sh` 清单协议/UNC 转换/WSL 源跳过插件包复制/UI 标签与文案；harness 255 PASS |
| 73 | `Services/SessionFileNames.cs`（新）+ `Services/ConversationService.cs` + `Services/ConversationSyncService.cs` + `EnvironmentScanWindow.xaml` | **dsh 0.1.5-rc 会话格式兼容（work-log/51）**：① 会话头接受任意版本（0..N，含 `isSeeded` 可选）；② 文件名识别 dsh canonical 规则（`session.jsonl[.zstd]` = v0、`session.vN.jsonl[.zstd]`），导入按**头部版本**命名、编码跟随目标实例（不一致时流式转码）；③ 旧 dsh 实例拒绝 vN 导入并明确报错；④ 跨实例同步按会话目录归并、取最高代际、跨编码/不支持版本时跳过；⑤ 修掉变更集 71 扫描页 `Run Text` TwoWay 绑定导致的 57 条 E9001 XamlParseException |
| 74 | `Services/InstanceVersionSwitchService.cs`（新）+ `Services/InstanceRuntimeRebinder.cs` + `Services/PluginCompatibility.cs` + `Services/ConversationService.cs` + `Services/SessionFileNames.cs` + `Models/VersionSettingsModels.cs` + `Services/VersionSettingsService.cs` + `VersionSwitchWindow.xaml(.cs)`（新）+ `VersionSettingsWindow.xaml(.cs)` + `MainWindow.xaml.cs` | **更换实例运行版本（升级/降级，work-log/52）**：入口并入「版本设置 → 版本与快照」页（与原「快照回滚」同页，页面因此改名）；解析目标（候选目录：`versions/<v>` → 主运行根 → 实例当前根，缺则从官方 npm 下载）→ 预检（Node 引擎 / 插件核心 peerDeps / 会话代际 / 方向）→ 可选快照与「一键导出会话」→ 强制重绑定（只改 RootPath/入口/启动描述/版本号，DSH_HOME 不变）→ 失败回滚原绑定；另加实例级「DSh 更新提示」开关（默认关） |
| 75 | `docs/DSH_CONTRACT_INVENTORY.md`（新）+ `Services/SessionFileNames.cs` + `Services/ConversationService.cs` + `Services/DshInstanceRunner.cs` + `_verify-p0/Program.cs` | **dsh 契约哨兵 + 降级实测（work-log/53）**：① 13 条契约清单（C1–C13：会话格式/文件名/头部字段/目录编码/根布局/编码/CLI/就绪输出行/Node 引擎/profile bundles/插件 peerDeps/`--no-open`/DSH_HOME 固定文件）；② harness 新增 13 条 `contract:` 哨兵——直接与**上游源码**和**已安装运行时**比对（文件名正则、目录编码向量取自上游测试、CLI 旗标、`dsh web:` 输出行、Node 下限），并用 252 个上游会话样本 + 9 会话语料回归启动器真实读写链路（含导出逐字节保真）；上游缺失时整段 SKIP；③ 降级单向性用已装两个运行时的实物证据固定（0.1.2 无 format catalog/迁移链、文件名恒为 `session.jsonl`）；④ 产出发现 F1：多代际会话目录在对话页列多行（已记录待决定） |
| 76 | `Services/LauncherLog.cs` + `App.xaml.cs` + `VersionSwitchWindow.xaml(.cs)` + `_verify-p0/Program.cs` | **日志隔离 + 确认面板自绘（work-log/54）**：① `LauncherLog` 支持 `DSH_LAUNCHER_LOG_ROOT` 覆盖，harness 与崩溃兜底日志一律写临时目录，不再污染用户真实 `launcher.log`（`diagnose` 用例在 Launcher 运行中也稳定）；② 更换运行版本的确认步骤改为窗口内自绘面板（`ConfirmPanel`：逐条说明 + 实际生效的勾选项 + 「返回」/「确认切换」），彻底移除 `MessageBox`，换版本/改勾选会作废旧确认 |
| 77 | `Services/VersionSwitchHistoryService.cs`（新）+ `Services/DshUpdateNoticeService.cs`（新）+ `Services/InstanceVersionSwitchService.cs` + `Services/DshVersionCatalogService.cs` + `VersionSwitchWindow.xaml.cs` + `VersionSettingsWindow.xaml(.cs)` + `MainWindow.xaml(.cs)` | **切换历史 + 一键回退 + 卡片更新徽标（work-log/55）**：① 每次换版本留痕（`version-switch-history.json`，含方向/快照/会话备份，保留 50 条），「版本与快照」页新增历史卡片与「回退到上一版本」（只预选进向导，仍需检查+确认）；② 实例卡片新增「有新版本」徽标，与实例设置共用 `DshUpdateNoticeService`（6 小时缓存、失败不缓存、只对开启开关的实例联网）；③ **修掉官方版本排序缺陷**：旧比较器丢掉预发布标签（alpha.2 与 rc.2 同序）→ 「官方最新」被算成 0.1.5-alpha.2，改为复用 semver 比较器 |
| 78 | `Services/ConversationService.cs` + `Models/EcosystemModels.cs` + `ConversationWindow.xaml(.cs)` | **对话页按会话目录归并（work-log/56，work-log/53 发现 F1）**：dsh 的模型是"一个会话目录 = 一个会话、文件是格式代际、读最高代际"，对话页原来每文件一行 → 从旧版升上来的实例同一会话显示两行（旧内容是升级前状态，导出/删除容易选错）。现在按目录归并只列最高代际，新增「代际」列与状态行提示；删除确认说明"会退回更早一代"；「降级前一键导出」改为逐代际全量导出（列表与备份语义显式分开） |
| 79 | `VersionControlWindow.xaml(.cs)` + `VersionSettingsWindow.xaml(.cs)` + `MainWindow.xaml.cs` + `Services/VersionSwitchHistoryService.cs` | **版本相关操作集中到「版本控制」页（work-log/57）**：运行版本（更换 + 更新提示开关/提示行）、切换历史（含一键回退）、配置快照与回滚全部搬到「版本控制」的选中版本详情；实例设置里的「版本与快照」整页移除（快照与版本控制页原本完全重复）；切换成功后左列表与详情同步换实例对象；保存前自动快照保留（窗口自建快照服务）。harness 302 PASS；UI 端到端：版本控制页完成整轮切换并写台账 |
| 80 | `Services/LauncherTaskService.cs`（新）+ `Models/LauncherTaskModels.cs`（新）+ `LauncherTaskWindow.xaml(.cs)`（新）+ `MainWindow.xaml(.cs)` + `PluginProgressWindow.cs` + `ExtensionWindow.xaml.cs` + `VersionSwitchWindow.xaml.cs` + `VersionControlWindow.xaml.cs` | **任务中心（work-log/59）**：长任务统一台账——`LauncherTaskService`（无 WPF 依赖、`Changed` 事件、`launcher-tasks.json` 上限 50 条、详情截断、损坏当空、崩溃遗留的"进行中"归一化为中断）+ 内嵌页「任务」（运行中与历史一张表：时间/类别/对象/标题/状态/耗时/摘要 + 取消；清空历史自绘两段确认）+ 顶栏入口与运行中角标；台账与进度窗取消令牌**双向串联**（两边点取消等效）；四类长任务接线：运行环境准备/修复、插件与技能装删改、实例导入（扫描进度逐条上报）、更换运行版本。harness 322 PASS；UIA 实测任务页渲染与中断归一化 |
| 81 | `Services/DshProfileService.cs`（新）+ `ExtensionWindow.xaml(.cs)` + `Services/ExtensionService.cs` + `Services/ExtensionService.ProfileRepair.cs` + `Services/PluginProfileResidue.cs` + `Services/DshInstanceRunner.cs` + `Models/VersionSettingsModels.cs` + `Services/VersionSettingsService.cs` | **Profile 切换（work-log/60）**：dsh profile＝`$DSH_HOME/profiles/<name>`（`dsh.profile.bundles` + `cordis.patch.yml`，共享 `profiles/node_modules`，shipped 模板按名首次使用时由 dsh 初始化）。新增只读的 `DshProfileService`（枚举/读 bundles/判定是否带 Web App/解析当前 profile，**不创建不修改**任何 profile 目录）；插件链路里写死的 `web`（CLI `--profile`、workspace、profile manifest、残留检测、修复文案）全部改为经解析器取当前 profile；实例设置新增 `ActiveProfile`（空值＝沿用 `web` 别名，行为不变）；启动前校验：非 Web App bundle 的 profile **拒绝启动并说明原因**（headless/acp/sdk 交给命令行）；插件页新增「当前 Profile」选择器（只列已存在 profile + bundles/来源/patchReload 提示，Agent 页隐藏）。harness 新增 11 条 `p1/profile:` 与 2 条 `contract/profile:`（含上游 `PROFILE_TEMPLATES` 哨兵）；UI 实测插件页渲染选择器 |
| 82 | `App.xaml.cs` + `Services/LauncherStorageService.cs`（新）+ `Services/MarketplaceService.cs` + `MainWindow.xaml.cs` | **日志治理与存储清理（work-log/61）**：① 崩溃日志三重保险——单文件 2 MB 轮转（`crash.log.old`）、同类异常 60 秒内折叠（签名＝类型＋消息前 120 字符＋栈首行）、每进程上限 200 条（超限只留一行说明）。起因是实测发现 `crash.log` **1.2 GB**（扫描页模板 TwoWay 绑定到只读属性 → 每 3 毫秒抛一次、两天 92,629 条）；② 新增 `LauncherStorageService`（只统计/清理 **Launcher 自有文件**：崩溃日志/轮转旧日志/市场缓存/运行环境缓存/插件回滚点/会话备份副本/换版本导出，删除走**回收站**；实例数据、自动快照、手动快照、凭据、会话一律不碰）；③ 插件回滚点加保留上限（最近 10 份）；④ 设置页「诊断与日志」加「存储与清理」入口（两步确认，预览占用后执行）。harness 344 PASS；安全反证（会话/凭据/手动快照必须存活）当场抓出"清理会对实例目录下手"的缺陷 |
| 83 | `tests/DshLauncher.SelfTest/`（新）+ `.github/workflows/ci.yml`（新）+ `docs/VERIFICATION.md`（新） | **工程治理（work-log/62）**：仓库自带纯逻辑自测（29 条断言：会话文件命名与代际、语义化版本比较、dsh profile 解析、任务台账、存储清理边界含**安全反证**、会话代际归并、长文本收敛、核心 bundle 常量），`dotnet run --project tests/DshLauncher.SelfTest` 即可跑，全程用临时目录、不联网、不开窗口；新增 CI 工作流（构建 → 自测 → 发布，windows-latest + .NET 8）；新增 `docs/VERIFICATION.md`（三层验证 + 发布 SOP + 测试边界约定）
| 84 | `Services/MarketSourceSettingsService.cs`（新）+ `Services/SkillMarketService.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **设置里新增「插件与技能来源」大项（work-log/64）**：插件与 Skill 两块结构平行——列出/添加/移除/连通测试自定义来源（插件＝本地目录 JSON 或网址，走既有 `marketplace-sources.json`；Skill＝GitHub `owner/repo`（扫描其中 SKILL.md，已真正接入市场候选）或本地目录 JSON / 网址（已保存，扫描接入留待下一步））；去重/上限 50/损坏按空/校验非法输入；探测走统一 UA（实测有些目录站会 403 拒默认 UA）。SelfTest +4 条，harness 更新"设置页 5 类"断言 |
| 85 | `Services/MarketSourceSettingsService.cs` + `Services/MarketplaceService.cs` + `Services/SkillMarketService.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **来源列表带开关与删除、固定高度内部滚动（work-log/65）**：来源条目从纯字符串升级为 `{value, enabled}`（读侧兼容旧格式）；插件与 Skill 两块列表都改成固定高度（158px）内部滚动的管理条，每条带「启用」开关、「测试」、「删除」；停用不删除、只影响市场消费；插件侧解析同步支持本地 `.json` 与停用项；SelfTest +2 条（开关落盘/旧格式兼容） |
| 86 | `Services/Deepseek1024CatalogService.cs`（新）+ `Services/MarketplaceService.cs` + `Models/VersionSettingsModels.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **中文插件源 #16（work-log/66）**：接入 `deepseek1024.com`（13,656 条，带 `install` 标识与安装/失败统计）——分页并发拉取 + TTL 30 分钟缓存（写原始载荷、映射现算）+ 拉取失败回退旧缓存 + 单页失败不放弃整次；映射处理该源的真实形状（`id=owner/repo/子路径`、`repository` 只有仓库名、`description.en/zh`），信息不足的条目直接丢弃；市场侧按 `MarketplaceSourceKind.ZhCatalog` 合并，**默认关闭**，由设置 →「插件与技能来源」里的开关打开；SelfTest +5 条 |
| 87 | `Services/DshfindCatalogService.cs`（新）+ `Services/MarketplaceService.cs` + `Models/VersionSettingsModels.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **中文插件源 #16 第二个源：dshfind.com（work-log/67）**：一次拉全量（在线实测 14,043 条，排除 74 条 archived）+ **TTL 30 分钟缓存 + 单飞**（8 MB 载荷并发刷新只发一次）+ 失败回退旧缓存；映射用 `fullName`、`i18nDescriptions` 的 zh 优先、无分类取首个 tag、官方/精选/★/标签进来源名、缺 install 时以 `github:owner/repo` 兜底；默认关闭、独立开关；SelfTest +5 条 |
| 88 | `Services/MarketSourceSettingsService.cs` + `Services/MarketplaceService.cs` + `Models/VersionSettingsModels.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **来源管理归一：唯一固定来源＝GitHub（work-log/68）**：两个中文源不再作为独立开关，改为**预置在自定义列表里的条目**（`adapter:zh1024` / `adapter:dshfind`，默认停用、可开关可删除、可测试连通）；`MarketplaceService` 改为按列表里启用项驱动适配器；移除 2 个启动器设置字段与 2 个复选框；SelfTest 期望同步（首次使用预置 2 条停用条目） |
| 89 | `MainWindow.xaml.cs` + `_verify-p0/Program.cs` | **设置页导航顺序调整（work-log/69）**：把「插件与技能来源」从末位挪到**「常规」与「网络与账户」之间**（顺序：运行环境 → 常规 → 插件与技能来源 → 网络与账户 → 诊断与日志）；harness 的导航顺序断言同步为逐项校验 |
| 90 | `Services/DshPackFormat.cs` / `DshPackArchive.cs` / `DshPackImportService.cs` / `DshPackFileDownloader.cs` / `DshPackWriter.cs` / `PackExportService.cs`（均新）+ 实例设置导出按钮 + `docs/DSH_CONTRACT_INVENTORY.md` | **#24 整合包规范对齐（work-log/70、72）**：容器识别（`.dspack` v2/v3、旧 `.tgz`）+ manifest **v2–v5** 解析与校验；导入到新建实例（权威重建 `package.json`、`overrides/`、`home/`，失败整体回滚）；`files[]` 下载（默认拒绝、显式同意、sha256+size 双校验、镜像回退）；导出 **manifest v4 + `.dspack` v2**（自家 v1 保留为旧格式）；SelfTest +40 条（含往返）。（#24 收尾随 work-log/77：导入确认面板自绘 `PackImportWindow`/`LegacyPackImportWindow`、v1 导入链路修复（优先用模板实例运行时）与配色/下拉统一，不单独占号。） |
| 91 | `src/DshLauncher/Services/CredentialAuditService.cs`（新）+ `tests/.../Program.cs`（+8 断言） | **#20 安全审计 · 增量 1（work-log/73）**：Launcher 侧被动凭据体检——按硬边界扫描 DSH_HOME 配置/凭据文件，只报告**命中位置**（文件/行/列/模式名），默认不回显任何值；显式开启才给脱敏预览（前 3 + 后 4）；配套**安全反证**：报告序列化不含凭据值及中段特征、体检纯读取（临时 HOME 零变化）、日志零泄露、node_modules 不扫 |
| 92 | `src/DshLauncher/Services/DangerousConfigAuditService.cs`（新）+ 设置页安全体检分区扩展（+7 断言） | **#20 增量 5：危险配置检查（work-log/74）**：按用户勾选范围检查权限档位/`DSH_PERMISSION_MODE`/`approval.policy`/**沙箱范围 workspaceRoot**/遥测模式与导出口/依赖来源/凭据文件保护/HMR；**只直接读文件，不调 `dsh --dump-config`**（调研发现它会写盘）；读不出的 `!!js` 表达式记 Notes 不猜；安全样本零发现 + 危险样本全命中（双向自校准）+ 纯读取反证 |
| 93 | `src/DshLauncher/Services/AuditProbeTimelineService.cs`（新）+ 安全体检页第三块「行为时间轴」 | **#20 增量 3（work-log/75）**：读取社区探针 `@marcog-h/dsh-audit` 的 `audit/*.jsonl`（两种命名都认）做行为时间轴；**只读白名单字段，`raw` 原文永不读取**，`key` 片段仅在显式开启预览时读取；容错解析（非 JSON 跳行、类型不对不丢行）+ 上限（8 MB/5000 条）；未装探针时空态不引导安装；反证 F/G（raw 零泄漏、纯读取） |
| 94 | `docs/ARCHITECTURE_DECISIONS.md` + `docs/BEHAVIOR_MATRIX.md`（均新，借鉴清单 #23 收尾） | **工程治理收尾**：10 条 ADR（测试不碰真实数据 / 凭据只看位置 / 安全反证+双向自校准 / 体检不调 dsh CLI / 导入永远新建实例+回滚 / 契约哨兵防静默失效 / 来源管理语义 / **运行时行为不许用推断代替数据** / 只借鉴不复制 / 发布 SOP）与「行为×验证方式矩阵」（实例、整合包、安全、界面、工程五类，标注 ST/HV/人工/无覆盖） |
| 95 | `PackVersionMismatchWindow.cs`（新）+ `Services/PackTemplateResolution.cs`（新）+ `VersionControlWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **整合包版本不一致：先提示、再选择（work-log/78）**：规范 `.dspack` 原先硬拒绝、v1 `.dshpack` 原先不检查（会造出「标 X 版本、实际跑 Y 运行时」的实例）；现统一为提示后让用户选「用模板实例的版本 / 下载所需版本 / 取消」，含候选模板定位（匹配优先、标注）与版本比较（精确匹配、忽略大小写/空白/前导 v、空值不匹配）；SelfTest 130→133 |
| 96 | `ExtensionWindow.xaml(.cs)` | **市场滚动追加修复（work-log/79 I2）**：初始展开量 150→5000（修「拉到底不刷新」，实测 150/3168）；滚动到底继续显示已抓取快照（不联网）、批大小 50；中间一次「外层滚动」推断经复核回退；滚动诊断日志默认关（`DSH_LAUNCHER_MARKET_SCROLL_DIAG=1` 开启）；市场追加议题按用户决定搁置；harness 348 / SelfTest 133 |
| 97 | `Services/PresentationSurfaceService.cs`（新）+ `MainWindow.xaml(.cs)` + `tests/DshLauncher.SelfTest/Program.cs` | **呈现面识别 + 「在终端打开」（TUI MVP，work-log/80）**：按 profile 名与 `dsh.profile.bundles` 判 Web/终端/无界面/未识别（bundles 优先、名字兜底、未知不硬套）；终端面实例可「在终端打开」（Windows Terminal：`-d <目录> <dsh 入口> --profile <名>`；缺 `wt.exe`/参数显式提示）；不做 PTY/终端仿真；SelfTest 133→136 |
| 98 | `MainWindow.xaml(.cs)` + `VersionSettingsWindow.xaml` | **实例卡片启动切分按钮 + 「更多打开方式」（work-log/81）**：卡片按钮重排——启动切分按钮（左＝启动横占 + 右＝▼）/ 停止·重启均分 / 重命名·实例设置均分；原独立按钮「隔离启动」「打开窗口」「在终端打开」折叠进 ▼ 菜单；版本设置「绑定打开方式」改名「更多打开方式」（手动绑定 Custom 保留）；本轮只做入口与呈现，菜单写回语义见变更集 99 |
| 99 | `MainWindow.xaml(.cs)` + `Models/VersionSettingsModels.cs` + `VersionSettingsWindow.xaml(.cs)` + `Services/PresentationSurfaceService.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **启动切分按钮 · 菜单行为落地（work-log/82）**：▼ 菜单 Web/Desktop/**隔离启动**改为**只切换不启动**（点击写回 `version-settings.json` 的 `OpenMode`；`VersionOpenMode` 新增 `Isolated`，设置页「更多打开方式」同步第 4 项；主按钮按最近选择执行，隔离启动走安全 profile 链路）；菜单当前方式打勾（`IsCheckable`）、「打开窗口」按 `CanOpenDesktopShell`、「在终端打开」按呈现面置灰（禁用 tooltip）；dsh 自带 desktop surface 判据（官方 scope `@deepseek-ai/dsh-desktop*`）命中则隐藏 Desktop/打开窗口并回落 Web；**修掉真实缺陷**：`VersionOpenModeConverter` 旧值映射（`Launcher`→Desktop）在真实读路径上是死代码（options 里的 `JsonStringEnumConverter` 抢先）→ 属性级 `[JsonConverter]` 修复；SelfTest 136→142、harness 348 PASS、UIA 18 PASS（快照+还原真实设置） |
| 100 | `Services/DangerousConfigAuditService.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **启动前危险配置提示（work-log/83）**：用户主动启动实例前，当前 profile 命中权限类危险项（沙箱 `danger-full-access` / `DSH_PERMISSION_MODE` / `approval: never`）即非阻断提示一次（每个 Launcher 会话、每个实例+profile；**只提示不改**，完整清单仍在安全体检）；`FindStartupPermissionDangers` 只挑 4 个权限类 Danger id（遥测/HMR/来源不打扰启动）；自动重启/接管重启/隔离启动不弹；SelfTest +1（危险样本命中、安全样本零提示） |
| 101 | `Services/PresentationSurfaceService.cs` + `MainWindow.xaml(.cs)` + `tests/DshLauncher.SelfTest/Program.cs` | **实例卡片呈现面徽标（work-log/84）**：按当前 profile 的 `dsh.profile.bundles`（读不到按 profile 名兜底）判定呈现面，非 Web 面（终端/无界面/未识别）才显示徽标 + 说明 tooltip；与 ▼ 菜单「在终端打开」同一套判据；SelfTest +1（143 PASS）、harness 348 PASS、UIA 4 PASS（快照+还原） |
| 102 | `MainWindow.xaml(.cs)` | **启动页只读显示当前 profile（work-log/85）**：实例卡形态行下方显示 `profile：<活动 profile>`（复用 `DshProfileService.ResolveActiveName`；切实例/守护探测轮/设置保存三处刷新），tooltip 说明 profile 的作用与切换入口；UIA 5 PASS（快照+还原） |
| 103 | `Services/LogCenterService.cs`（新）+ `LogCenterWindow.xaml(.cs)`（新）+ `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **日志中心（work-log/86）**：设置 →「诊断与日志」→ 内嵌页，只读浏览 `launcher.log`（含 `.old` 轮转）：按天分组（本地时区）+ 级别/关键字/实例过滤 + 尾部长上限（读 5000 / 渲染 500），坏行计数、`FileShare.ReadWrite` 不阻塞写入；SelfTest 150 PASS（+7）、harness 348 PASS、UIA 8 PASS（真实 756 条：分组/过滤/返回） |
| 104 | `VersionSettingsWindow.xaml(.cs)` | **O4 验收发现的两个缺陷修复（work-log/87）**：① 「检查并复位 profile 残留」按钮**漏了 Click 接线**（work-log/71 的 A3 声称已完成，实际点了没反应）→ 补接线；② 卫生检查/复位的结果文本被运行状况页 1s 周期刷新覆盖（<1s 消失，与 work-log/39 同类）→ 加 20 秒保留期，周期刷新在保留期内不覆盖状态文本；O4 UIA 7 PASS（缓存清理/隔离启动确认框/卫生复位） |
| 105 | `App.xaml` + `MainWindow.xaml.cs` + `Services/DshInstanceRunner.cs` + `Services/ExtensionService.cs` + `Services/MarketplaceService.cs` + `Services/ModelService.cs` + `docs/UI-DESIGN.md` | **死代码清理（work-log/88）**：全仓扫描（0 引用私有方法 / XAML 资源）→ 逐步删除 **16 个零引用方法**（5 个未接线处理函数 + 9 个扫描命中 + 2 个级联）、**11 个未引用设计令牌**，并清掉级联的“安装态残留”（1 个恒 false 字段 + 2 个无消费者属性 + 8 处通知点）；+1/−222 行（代码）；复扫双清零；SelfTest 150 / harness 348 / MainWindow 冒烟 8 PASS；发布 MD5 `4dbe42df…` |
| 106 | `Services/InstanceUiPluginScanner.cs`（新）+ `Models/VersionSettingsModels.cs` + `Services/VersionSettingsService.cs` + `VersionSettingsWindow.xaml.cs` + `MainWindow.xaml.cs` + `tests/...` | **UI 插件检测 + 「启动方式显示」开关（work-log/89）**：扫描实例 profile 的插件（bundles ∪ dependencies，且必须声明 dsh 插件）识别 TUI/GUI（spec 声明＞keywords＞包名；官方核心包不参与，`dsh-desktop*` 例外）；实例设置「个性化」新增开关子列表（默认全开）控制卡片 ▼ 菜单显示（在终端打开/打开窗口/隔离启动；Desktop/Web 常显）；未启用 TUI 插件在「在终端打开」时提示并可一键启用；4 处保存路径补字段防抹（ReadConfigurationSettings/CopySettings/SavePluginSettings/MainWindow）；修租传递依赖库 keywords 误报；SelfTest 153、harness 348、UIA 7 PASS；发布 MD5 `386278a8…` |
| 107 | `Services/InstanceUiPluginScanner.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **TUI 插件检测修复：旧版 profile 本地 node_modules（work-log/90）**：扫描器改为两处布局都扫（`profiles/<名>/node_modules` 优先 + `profiles/node_modules` 共享，同名去重）——0.1.2 等旧版把插件装在 profile 本地，原实现只扫共享目录导致检测不到（用户报告的“装了 TUI 插件但不显示在终端打开”根因）；SelfTest 夹具覆盖两种布局；SelfTest 153 / harness 348 / 真实数据 UIA 7 PASS；发布 MD5 `525815ea…` |
| 108 | `Services/PresentationSurfaceService.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **「在终端打开」漏注入 DSH_HOME（work-log/91）**：wt.exe 原用 `UseShellExecute=true` 且不传 `DSH_HOME` → 终端里的 dsh 退到默认 home `~/.dsh`，自定义 profile（dsh-tui）直接报 `profile does not exist`（内置模板 web/acp/headless/sdk 会自建，所以一直未暴露）；新增可测工厂 `CreateWindowsTerminalStartInfo`（注入 DSH_HOME/DSH_AGENTS_HOME/PATH）+ 「profile 不存在」启动前校验；SelfTest 154 / harness 348 / 真实数据 E2E 9 PASS；发布 MD5 `ffea652e…` |
| 109 | `Models/VersionSettingsModels.cs` + `Services/LaunchModePolicy.cs`（新）+ `Services/TerminalLaunchService.cs`（新）+ `MainWindow.xaml(.cs)` + `VersionSettingsWindow.xaml.cs` + `Services/VersionSettingsService.cs` + `Services/VersionPackageService.cs` + `tests/…/Program.cs` | **终端启动＝一种启动方式 + 终端工作区（work-log/92）**：新增 `VersionOpenMode.Terminal`——▼ 菜单「终端启动（终端面）」只切换（与 Web/Desktop/隔离一致），主按钮变「终端启动」并在 Windows Terminal 里跑活动 profile；非终端面/入口隐藏时回退 Web（`LaunchModePolicy`）；实例设置新增「终端启动的工作区」（目录+浏览+清除）与「每次终端启动都先让我选目录」开关，`TerminalLaunchService` 解析 cwd（dsh-tui 用进程 cwd 当工作区，插件无 --cwd 参数）；修 `VersionSettingsService.Clone` 漏字段（新字段一存就被抹）；SelfTest 157 / harness 348 / 真实数据 E2E 13 PASS；发布 MD5 `96d73bf9…` |
| 110 | `VersionSettingsWindow.xaml(.cs)` | **实例设置：启动方式内容集中到一张「启动方式」卡片（work-log/93）**：原「更多打开方式」升级为「启动方式」卡片，内含小节「默认启动方式」（原下拉/自定义目标/状态/保存）+「启动方式显示」（原代码追加卡片）+「终端启动的工作区」（原代码追加卡片）；两段代码追加改为追回到 `LaunchModeExtraSections`（标题 14→13、间距 18）；无设置字段变更；SelfTest 157 / harness 348 / UIA 布局 9 PASS（Y 坐标验证同卡片与顺序）；发布 MD5 `8c8b5233…` |
| 111 | `VersionSettingsWindow.xaml(.cs)` + `Services/LaunchModePolicy.cs` + `MainWindow.xaml.cs` + `tests/…/Program.cs` | **默认启动方式下拉：与「启动方式显示」联动 + 「未设置」置空（work-log/94）**：下拉按显示开关过滤被关闭的方式（隔离/终端），当前默认被关闭时回退 Web 并在状态文字里说明（**仅在显示/生效层，不改写用户设置**）；补「终端启动」项（按呈现面置灰）与「未设置（按运行时自动）」项（清掉 OpenMode→null，按运行时自动）；`LaunchModePolicy` 新增 `isolatedModeVisible`（隔离被隐藏→Web）；SelfTest 158 / harness 348 / UIA 真机 16 PASS；发布 MD5 `6fdbad11…` |
| 112 | `Models/VersionSettingsModels.cs` + `Services/{LaunchModePolicy,TerminalLaunchService,VersionSettingsService,VersionPackageService,PresentationSurfaceService}.cs` + `MainWindow.xaml(.cs)` + `VersionSettingsWindow.xaml(.cs)` + `ExtensionWindow.xaml(.cs)` + `tests/…/Program.cs` | **启动方式收敛：只留 Web / Desktop（+隔离），TUI 改为「复制终端启动命令」（work-log/95）**：删「终端启动」「手动绑定（自定义）」两种方式、整张「启动方式」设置卡片、`LaunchModeVisibility`/`TerminalWorkingDirectory`/`TerminalAskWorkspaceEachTime`/`CustomOpenTargetPath` 四个字段与 `VersionOpenTargetService`；**保留**隔离启动（崩溃恢复/逐插件定位在用 `SafeProfileService`）与「打开窗口」；扩展页新增「终端启动（TUI 插件）」卡片：命令 `$env:DSH_HOME='…'; & '<版本>\dsh.cmd' --profile <活动 profile>` + 复制按钮；顺带修掉**设置窗口保存路径漏 `ActiveProfile`**（会把活动 profile 抹成 null→web）；SelfTest 155 / harness 348 / UIA 13 PASS；发布 MD5 `7dfe3a40…` |
| 113 | `ExtensionWindow.xaml(.cs)` + `VersionSettingsWindow.xaml(.cs)` | **终端命令卡片搬进实例设置（work-log/96）**：扩展/Agent 页删掉 112 新加的命令卡片（Agent 页本来就靠 `agentOnly ? Collapsed : Visible` 自动屏蔽 Profile 面板，卡片当时漏了门控）；实例设置→个性化新增卡片「终端启动（TUI 插件）」（位置：版本数据位置之后、实例环境变量之前），含只读命令 + 复制按钮，扫到 TUI 插件才显示；Profile 下拉保留（共用 + Agent 页自动隐藏）；SelfTest 155 / harness 348 / UIA 10 PASS；发布 MD5 `f0acc258…` |
| 114 | `ExtensionWindow.xaml(.cs)` + `VersionSettingsWindow.xaml.cs` | **扩展页去掉「当前 Profile」；命令卡片自动指向能起 TUI 的 profile（work-log/97）**：删掉 `ProfilePanel`（下拉/提示）与三个方法两个字段零残留；活动 profile 切回 `web`（否则主按钮被非 Web 面拒绝且界面已无切换入口）；「终端启动（TUI 插件）」命令改为**优先指向承载 TUI 的终端面 profile**（活动 profile=web 时自动给 `--profile dsh-tui`）；harness 源码契约同步改为“断言扩展页已无 ProfileComboBox/ProfileHintText”；SelfTest 155 / harness 348 / UIA 9 PASS；发布 MD5 `b2e201ab…` |
| 115 | `Services/TerminalLaunchService.cs` + `VersionSettingsWindow.xaml.cs` + `tests/…/Program.cs` | **手敲 TUI 命令不再弹浏览器：自动补 `--no-open`（work-log/98）**：`--no-open` 由 dsh-web-app 层定义（启动器托管启动一直传它，手敲命令没传→默认弹浏览器）；卡片生成命令时若目标 profile 含 web-app 且版本支持（`SupportsNoOpen`）则自动追 `--no-open`，提示语说明“不会弹浏览器”；纯终端面 profile 不加（TUI 插件不认该选项）；同时把验收脚本改为**按实例名动态定位**（用户重建实例，旧 ID `e4111c8d…` → `04ff1d2f…`）；SelfTest 155 / harness 348 / UIA 8 PASS；发布 MD5 `be9f3c0c…` |
| 116 | `Services/ExtensionService.cs` + `tests/…/Program.cs` | **独立终端面 profile + 受限插件同步（work-log/99）**：给重建后的实例建独立 `dsh-tui` profile（base + dsh-tui）；`ExtensionService` 新增 `SyncToTerminalProfileAsync`——`plugin add/update` 成功后，若实例存在“呈现面为 Terminal”且不同于活动 profile 的 profile，则用同参数 sibling service 再跑一次（失败只附注不阻断）；`PickTerminalProfile` 可测；`List()` 默认跳过 `.dsh-safe`/`.dsh-bisect`；remove/启用/禁用**不**镜像；SelfTest 156 / harness 348 / UIA 6 PASS（卡片命令 `--profile dsh-tui` 且无 `--no-open`）；发布 MD5 `8a8c0ae0…` |
| 117 | `Services/TerminalLaunchService.cs` + `VersionSettingsWindow.xaml(.cs)` + `tests/…/Program.cs` | **TUI 命令卡片给三条命令（work-log/100）**：插件 helpText 证实“启动命令首次/后续相同，差别在①首次要先装 profile ②续上次会话加 `--continue`/`--resume`”；卡片改为三行——①安装/升级（包名取扫描到的 TUI 插件名，拿不到则隐藏该行）②新会话 ③继续上次会话（`--continue`），各行独立复制按钮；`BuildPowerShellCommand` 新增 `extraArguments`、新增 `BuildPluginInstallCommand`；SelfTest 156 / harness 348 / UIA 7 PASS；发布 MD5 `bfe60517…` |
| 118 | `VersionSettingsWindow.xaml` | **启动卡片挪到环境变量之后（work-log/101）**：「终端启动（TUI 插件）」卡片从「版本数据位置」之后、环境变量之前 → 移到「实例环境变量」之后；纯 XAML 版式搬移（保留行尾风格，无逻辑改动）；个性化页顺序现为 当前版本 → 版本数据位置 → 实例环境变量 → 终端启动；SelfTest 156 / harness 348 / UIA 8 PASS（Y 坐标 546 < 683 < 951 + 三条命令仍正确）；发布 MD5 `6d92cfb9…` |
| 119 | （已被 120 整包退回） | 两栏页面统一 320+18 + 1200 居中——**已回滚**（源码与该行内容均随 revert 撤回；原始记录见 work-log/102） |
| 120 | （revert 119）`VersionSettingsWindow.xaml` + `MainWindow.xaml(.cs)` + `ExtensionWindow.xaml` + `VersionControlWindow.xaml` | **退回变更集 119（两栏统一）（work-log/103）**：用户反馈“宽度出问题”→ `git revert b7b9f5c`（提交 `e0b87d6`），恢复实例设置左栏 180、设置/诊断 196 + 面板 980、扩展 340+16、版本控制 280+20，并去掉各页 1200 上限与居中；构建 0/0、SelfTest 156、harness 348（verify-120.txt）；发布 MD5 `e064cafc…`（dist == 安装副本） |
| 121 | `VersionSettingsWindow.xaml` + `MainWindow.xaml.cs` + `ExtensionWindow.xaml` + `VersionControlWindow.xaml` | **两栏页第二版：左栏固定 320+18、右栏占满、圆角统一（work-log/104）**：左栏 180/196/340/280 → 统一 **320** + 18 间隙；**去掉 1200 上限与居中、删设置页面板 980 上限**（119 导致“右栏过窄”的根因），右栏占满窗口；实例设置左栏从贴边侧栏 → **CardCornerRadius=12 圆角卡**（与其他两栏页一致）；SelfTest 156 / harness 348 / UIA 3 PASS（内容起点 474px、设置页最右控件 1907/1936px）；发布 MD5 `320fd1ce…` |
| 122 | `AppDialog.cs`（新）+ 9 个 `*.cs`（MessageBox 调用点）+ `VersionControlWindow.xaml.cs` | **自绘对话框全量替换 + 删除版本卡顿修复（work-log/105）**：新增 `AppDialog`（与 `MessageBox` 同形 API；卡片 + `CardCornerRadius` + 阴影 + `PrimaryButton` 自绘样式；Esc=取消、Enter=默认），全仓 **54 处** `MessageBox.Show` → `AppDialog.Show`（删除确认与关闭防呆不再系统灰窗）；`DeleteVersion_Click` 的只读文件预扫描移入 `Task.Run`（修“删实例卡一下”）；SelfTest 156 / harness 348；UIA 自绘框端到端**未完成**（脚本元素定位失败，待目视确认）；发布 MD5 `92d6b53c…` |
| 123 | `Services/SkillMarketQuery.cs`（新）+ `ExtensionWindow.xaml(.cs)` + `tests/DshLauncher.SelfTest/Program.cs` | **Agent 页（技能市场）排序 / 来源（work-log/106）**：Agent 页分类工具栏新增「排序」（综合排序 / 热门（Star）/ 最近更新——技能只有 `Stars` 与 `UpdatedAt`、**无发布日**，且服务内部本就是 Star 降序 + 路径优先级，所以“热门”≈现状、“最近更新”才是真正新增）与「来源」（按「设置 / 诊断 → 市场源」里为 **Skill** 配置的仓库逐项列，默认「全部来源」，停用源标注“（已停用）”）；筛选/排序抽为**纯函数** `SkillMarketQuery.Apply`（来源匹配双向包含，兼容 `owner/repo` 与完整 GitHub URL），自测可直接覆盖；**修初始化期 NRE**——新下拉的 `SelectedIndex="0"` 会在 `InitializeComponent` 期间触发 `SelectionChanged`，此时同页 `SkillMarketSearchBox`/`SkillMarketCategoryList`/`SkillMarketList` 尚未构造 → 处理函数加加载守卫（harness `--ui` 当场抓到并复验）；SelfTest **163**（新增 7 条）/ harness **348**；发布 MD5 `93bed33c…` |
| 124 | `Services/SkillMarketQuery.cs` + `ExtensionWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **Agent 页「来源」下拉为空——根因与修复（work-log/107）**：真机反馈“来源菜单是空的（下面有具体 skill）”。根因两层：① 填充只因 `RefreshSkillMarketAsync` 调用，而 `SetupSkillMarketAsync` 在“缓存非空且校验版本一致”时**不刷新** → 缓存路径下下拉从未被填充；② 原口径（只列设置页为 Skill 配的仓库）本身就是空集——`SeedBuiltInAdaptersIfMissing` 只对 Plugin 生效、无 `skill-market-sources.json`，而技能实来自内置动态 GitHub 搜索（`q=skill in:name`），不存在固定“内置来源清单”。修法（用户确认口径“配置源 ∪ 当前列表实际仓库”）：新增纯函数 `SkillMarketQuery.BuildSourceChoices`，`RenderSkillMarketItems` 开头重建选项（签名比对，不打断选择），从刷新方法移除填充；SelfTest **166**（+3）/ harness **348**；发布 MD5 `65c3b01a…` |
| 125 | `Services/SkillMarketQuery.cs` + `ExtensionWindow.xaml(.cs)` + `tests/DshLauncher.SelfTest/Program.cs` | **Agent 页「来源」标签与出处说明（work-log/108）**：真机复测反馈“有内容但我看不懂”→ 确认是“不知道选了有什么用”。取证：用户缓存 234 技能 / 17 仓库，下拉原显示 17 个裸 `owner/repo`（均来自内置动态 GitHub 搜索、非用户配置，多数恰好 20 条＝每仓库上限）。改法：新增 `SkillSourceChoice`（Tag/Label/Selectable/SkillCount），标签带技能数（`anthropics/skills · 20 个技能`、`全部来源（234 个技能）`），下拉第二行加**不可选**说明行“（仓库来自内置 GitHub 搜索）”，宽度 168→240 + ToolTip 说明用途；默认选择改为“首个可选项”；SelfTest **166** / harness **348**；发布 MD5 `06086506…` |
| 126 | `Services/CrashCauseClassifier.cs` + `Models/CrashCauseModels.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **崩溃归因细化：退出码专项映射 + 多因复合（work-log/109）**：先取证——本机无真实崩溃记录、无 Windows SDK 头可作 `STATUS_*` 权威来源，故改用**本机实测**码表（node v24.18.1：未捕获异常/缺模块/栈溢出/自 SIGTERM **全是 1**；`abort()`=**134**；参数非法=**9**；`.NET Kill()`=**-1**；`taskkill /F`=1 不可辨）。改法（用户确认“只做可验证码 + 多因复合 + 只并入现有链路”）：`CrashCauseKind` 新增 `ProcessAborted`/`InvalidLaunchArguments`/`ForceKilled`/`NativeCrash`（负值只报原始码）；分类器重构为**规则表 + 主/次因**（`ClassifyAll` → `CrashCauseReport`，签名优先于退出码，同类去重，已知文案**逐字保留**，脚本比对 64 个字面量仅 1 处有意替换）；次因并入崩溃记录证据与 E1016 日志，通知仍只显主因；SelfTest **175**（+9）/ harness **348**；发布 MD5 `dccfee40…`；**真机验收**（work-log/110）：临时实例 `.NET Kill()` → `ExitCode=-1`、`Cause=进程被强制结束`、E1016 `secondary=null`、UI 通知显示归因；检测走 watchdog（约 15~20 秒延迟） |
| 127 | `App.xaml` + `MainWindow.xaml` + `ExtensionWindow.xaml` + `VersionControlWindow.xaml` + `VersionSettingsWindow.xaml` + `PluginMatrixWindow.xaml` + `EnvironmentScanWindow.xaml` + `ChatWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 A：颜色令牌化（work-log/118）**：把 `App.xaml` 之外的 **38 处**硬编码颜色（7 个文件）全部收敛为令牌，新增 **16 个**（标题栏 chrome 5 / 语义面 6 / 状态点 1 / 空态插画 4，其中 2 个是 `Color` 供 `GradientStop` 用）；顺带把 4 处语义词的值统一到既有令牌（`#8B5A12`、`#B86D1D` → `WarningTextBrush`；`#1B5E20` → `SuccessTextBrush`；`#D94A4A` → `DangerBrush`）；harness 断言从「6 个窗口 + 8 色白名单」升级为**全量门禁**（遍历所有 XAML 断言无颜色字面量）；构建 0/0、harness 348 PASS、25 张截图逐像素对比无布局回归 |
| 128 | `ExtensionWindow.xaml` + `VersionControlWindow.xaml` + `MainWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 B：字号阶梯（work-log/119）**：解决两份文档的口径冲突——`UI-DESIGN.md` 只列到 12px，与代码实况（**11px 用于辅助文字 103 处**）及 TODO §1.3 都不一致 → **把 11 正式补入阶梯**，最终口径 **11/12/13/14/15/16/18/20 + 24/28/30 品牌例外**（而不是把 103 处 11px 全改成 12px，也不按粗阶梯归并 26 处）；实际归并 **6 处**：`10→12`×2（ExtensionWindow:504、MainWindow:241 角标）、`17→18`（「当前实例」）、`17→16`（版本名）、`21→20`×2（「插件市场」「Skill 市场」）；图标字形（`— □ ×`，含 22/28px）留变更集 C；新增 harness 门禁「字号全部落在规范阶梯」（图标字形豁免）；构建 0/0、harness **349 PASS**、25 张截图无布局回归；`≤12px 是否一律 SemiBold` 实测 112/129 未加粗 → 列待决策 |
| 129 | `PluginMatrixWindow.xaml` + `EnvironmentScanWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 B 收尾：小字字重规则（work-log/120）**：把「≤12px 一律 SemiBold」按角色收窄为**仅胶囊/徽标/状态标签（12px + SemiBold）**——实测 129 个 ≤12px 文本里 **112 个未加粗**，照字面执行会让辅助文字整体变重；按「小圆角 `CornerRadius="3"` + 浅色底 + 短标签」特征精确识别出 **6 个真胶囊**（PluginMatrix「已启用/已装未启用/未安装」+ EnvironmentScan 的来源/登记/来源·类型标签）并修为 12px + SemiBold；新增**规则型** harness 断言（逐个小圆角胶囊扫描，对未来新增胶囊同样生效，而非白名单）；构建 0/0、harness **350 PASS**、25 张截图对比无布局回归 |
| 130 | `App.xaml` + `MainWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 C：图标体系（work-log/121）**：决策＝**统一字符集 + 规范文档**（矢量图标集不做，留独立主题）；重扫得到完整清单 **19 处 / 17 种**（首轮漏了 `⧗`「任务」、`＋` 空态加号、`↗` 外链箭头——原字符集是手写的，本来就不全）；新增令牌 **`IconTextGap`**（`0,0,8,0`）并把导航 6 个字形 + 返回 `←` + 启动按钮 `▶` 共 **8 处**间距改为令牌（原 6/7/9px 混用）；**标题栏三字形 22/18/28 → 统一 18px**；折叠箭头 `⌄` → `▾`；17 种字形写入 `docs/UI-DESIGN.md`「图标（字形集）」；新增两条门禁（字形表外检测按 Unicode 符号类别 + emoji 检测、三字形同号 + 间距令牌）；构建 0/0、harness **352 PASS**、25 张截图 + **识图目视验收**（三字形已同号、导航间距一致、无回归） |
| 131 | `App.xaml` + `MainWindow.xaml.cs` + `_verify-p0/func-check/*` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 D：菜单统一（work-log/122）**：复核发现 §1.13 记反了——真正缺样式的是 `MainWindow` 的启动方式分体菜单（当时**没有隐式 MenuItem 样式**），导入菜单早已挂样式；本轮新增**隐式 MenuItem 样式** + **禁用态**（灰字/无悬停底/箭头光标）+ **`MenuItemDangerStyle`**；弹出层 `White`→`CardBrush`、`#C8DDF0`→`LineBrush`；菜单项悬停 `#E0EAFD`→新令牌 **`MenuHoverBrush`**、选中 →`PageBrush`；`MainWindow.xaml.cs` 动态菜单 3 处 `FromRgb` 状态点改 `FindResource`；`docs/UI-DESIGN.md` 增「菜单」小节（含 **StaticResource 前向引用会导致启动崩溃** 的警示）；**过程中把启动器改崩一次**（样式插在被引用样式之前 → `XamlParseException`），harness 冒烟断言当场抓住（341 PASS/1 FAIL）→ 修复后 **352 PASS**；截图脚本新增 `popup` 模式并补 2 个菜单屏（16 屏），识图确认分体菜单已成卡片式圆角菜单 |
| 132 | `App.xaml` + `MainWindow.xaml.cs` + `EnvironmentScanWindow.xaml.cs` + `VersionSettingsWindow.xaml.cs` + `Models/ProviderModels.cs` + `Services/UiBrush.cs`（新） + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一 A2：颜色令牌化收尾（work-log/123）**：补上变更集 127 的**盲区**——「XAML 里、App.xaml 之外」这条规则放过了两整块：`App.xaml` 样式/模板内 **28 处**字面色 + C# 内 **24 处** `FromRgb/FromArgb`（含子目录 `Models/`、`Controls/`）；新增 **16 个令牌**（渐变 Color 2 / 阴影 Color 1 / 品牌底文字 / 控件描边 / 次级面 / 选择高亮 / 滚动条滑块 3 / 状态胶囊底 3 / 状态文字 2 / 来源紫）；新增 `Services/UiBrush.cs`（静态字段用不了实例 `FindResource`；资源未就绪时返回透明画刷而非崩溃）；`MetricChart` 的 `FromArgb(36, solid.Color.R, …)` 属「从变量派生 alpha」，门禁按**全数字参数**放行并写明理由；**门禁升级**：颜色扫描扩到「App.xaml 非令牌行 + C#」+ 新增「菜单样式统一」断言；构建 0/0、harness **353 PASS**、度量颜色 **0 处**、16 屏截图无回归 |
| 133 | `VersionControlWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 §1.13 内容层：导入实例菜单双行化（work-log/124）**：「导入实例」5 个动作由单行标题改为**双行项**（标题 + 11px `MutedBrush` 说明，说明写在 Header 内容里、**不覆写共享模板**以免复制三个触发器）；`导入实例` 按钮 ToolTip 改写为**与「新建干净版本」的区别**；5 项补 `AutomationProperties.Name`（Header 非字符串时 WPF 不上报 UIA 名称，读屏与自动化都拿不到）；新增 harness 断言「双行项 5 条 + ToolTip 说明」；工具侧：popup 截图改为**不动窗口状态 + 整屏截图**（改窗口状态会关掉 WPF Popup）；`.ps1` 恢复纯 ASCII（中文只能来自 UTF-8 JSON）并加语法自检；构建 0/0、harness **354 PASS**、截图 27 张 |
| 134 | `ConversationWindow.xaml(.cs)` + `ExtensionWindow.xaml` + 7 个窗口 XAML（虚拟化） + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一 E：表格与侧栏自适应（work-log/125）**：对话页三张表 **17 列**改为**代码按可用宽度分配**（`GridViewColumn.Width` 是像素 double——**没有 MinWidth、不支持星号**，故 XAML 写初值、`SizeChanged`/`Loaded` 时按权重分配剩余宽度）；扩展页侧栏 `320` → `0.26*`+`MinWidth 260`/`MaxWidth 380`，三个筛选控件固定宽 → `MinWidth`；**16 个列表元素**补显式虚拟化（`IsVirtualizing` + `Recycling` + `CanContentScroll`）；**踩坑 3 个并如实记录**：① 隐式样式 `BasedOn="{StaticResource {x:Type ListView}}"` 在本应用解析不到主题样式 → **创建 ListView 时抛 XamlParseException**、点「对话」页面构造失败（靠截图识图发现"文件名是对话页、内容是 Agent 页"），改为显式属性并**列入门禁禁止**；② 批量脚本正则过宽，误伤筛选行 `Width="2*"` 列（被既有断言抓住）；③ 插属性的正则匹配到属性元素致 6 文件 `MC3057`；构建 0/0、harness **357 PASS**、27 张截图 + 识图确认表格铺满宽度且页面正常 |
| 135 | `App.xaml` + `ConversationWindow.xaml` + `ExtensionWindow.xaml` + `VersionControlWindow.xaml` + `VersionSettingsWindow.xaml` + `MainWindow.xaml` + `PackImportWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一 F：卡片/圆角/阴影/按钮三级（work-log/126）**：**6 个危险按钮**补 `DangerButton`（删除选中对话/删除/卸载/删除版本/回滚选中快照/插件删除）→ 复核 5/5 合规、0 缺失；**9 处裸圆角令牌化**（`9`→`PanelCornerRadius`、`8`→`ItemCornerRadius`、`12`→`CardCornerRadius`），白名单例外 `3`（胶囊）/`17`·`18`（窗口外框、空态插画）写入规范；`PrimaryButton`/`DangerButton` 补 **`MinHeight=38` + `Padding=16,0`**（此前共享样式**没有尺寸**，各页自写 8 种 Padding）；顺带修正盘点结论：按钮高度实际只有 `40×6`（先前的 44/42/34 是多行标签里的 ComboBox）、页面外边距实测 `6~12`（非 TODO 记的 14/16/20/22）；新增门禁「危险按钮必挂 DangerButton + 非白名单裸圆角 0 + 主/危险按钮同尺寸」；构建 0/0、harness **358 PASS**、识图确认三级层次清晰 |
| 136 | `ExtensionWindow.xaml.cs` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` + `docs/UI-UNIFICATION-TODO.md` | **UI 统一 G：空/加载/错误三态（work-log/127）**：盘点发现**全仓 0 处「重试」入口**、失败/加载/成功共用同一条灰字状态行；新增 `SetStatusText(block, text, isError:, isWarning:)` 并把市场与 Skill 状态行 **11 处**改为三态分色（加载＝`MutedBrush`、部分失败＝`WarningTextBrush`、失败＝`DangerTextBrush`），失败文案统一补 **"（点「刷新目录」重试）"**；`docs/UI-DESIGN.md` 新增「空态/加载态/错误态」规范（含纪律：错误不允许只进日志）；新增门禁「三态分色 + 失败文案自带重试入口 + 重试按钮真实存在」；构建 0/0、harness **359 PASS**；其余四页三态列为遗留 |
| 137 | `EnvironmentScanWindow.xaml` + `LauncherTaskWindow.xaml` + `LogCenterWindow.xaml` + `PluginMatrixWindow.xaml` + `NewVersionWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一 H：窗口与页面框架（work-log/128）**：4 个内嵌页标题 `18 → 20`（规范：页面标题 20 / 卡片标题 18）；`NewVersionWindow` 补 `MinWidth 430`/`MinHeight 320`（其余 4 个窗口本来就有 → 5/5 全覆盖）；返回/关闭交互**复核符合规则并写入规范**（内嵌页「返回 <来源页>」；模态窗「取消」+ `IsCancel` 3/3，Esc 可关；向导另有「返回上一步」）；新增门禁同时守住「页面标题 20px」「独立窗口必有最小尺寸」「模态窗取消键可 Esc」；构建 0/0、harness **360 PASS**、识图确认「日志中心」标题已 20px 且返回按钮符号约定；滚动位置记忆（4 个内嵌页）列为遗留 |
| 138 | `App.xaml` + `MainWindow.xaml` + `ExtensionWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一 I：键盘可达性与自动化（work-log/129）**：盘点发现 **`PathLinkButton` 显式把焦点框设为 `{x:Null}`**（键盘用户看不到焦点）、**全仓 0 个按钮有 `AutomationProperties.Name`**；新增统一焦点框 `AppFocusVisual`（蓝虚线 2px）挂到 **5 个 keyed 按钮样式**，并给 **15 个无文字按钮**（导航 6 / 路径链接 3 / 标题栏 3 / 实例 2 / 启动 1）补可访问名；⚠️ 中途踩坑：焦点框用**隐式样式 + `BasedOn="{StaticResource {x:Type Button}}"`** 会让 **App.xaml 加载即崩**（`XamlParseException: ResourceDictionary.DeferrableContent`，与变更集 134 的 ListView 同源）——harness 冒烟断言当场抓住（341 PASS/1 FAIL），改为挂 keyed 样式后 **361 PASS**；该禁令已写入 `docs/UI-DESIGN.md`；构建 0/0、27 张截图 |
| 139 | 13 个窗口/页面 XAML 根元素 + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一 J：高 DPI 与渲染（work-log/130）**：盘点发现**根元素一个都没声明像素对齐**（`UseLayoutRounding`/`SnapsToDevicePixels` 只在 App.xaml 与 MainWindow 内部零星出现）→ **13 个根元素全覆盖**；明确 **ClearType 降级范围＝仅 `MainWindow`**（透明窗，决定保留透明圆角）、DPI 感知走框架默认（无自定义 manifest）、图标为字体字形（矢量）缩放不糊；新增门禁「13 个根元素全覆盖」；**踩坑（本轮第三次批量脚本事故）**：找根元素的正则因根标签超 400 字符而回退匹配到 `<Window.Resources>` 属性元素，插出非法 XML（`MC3000`），改用 `<(Window|UserControl) `（带空格）识别并修复；构建 0/0、harness **362 PASS**、27 张截图（本机 125% 实测）；150%/200% 人工核对清单已写入规范 |
| 140 | `VersionControlWindow.xaml` + `App.xaml` + `MainWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **UI 统一验收修复（work-log/132）**（用户在发布版验收日报 3 处）：① **「导入实例」菜单只出窄白框** —— 根因是变更集 133 把两行内容写成 `MenuItem` 的**子元素**（→ `Content` 属性），而共享模板渲染的是 **`Header`**（`ContentPresenter ContentSource="Header"`）→ 内容从未渲染；改为 `<MenuItem.Header>`（5 项）+ 保留 `MinWidth` 防御 + 删掉解析不到的 `PlacementTarget` RelativeSource 绑定；② 危险按钮悬停"整块变红"改为**只加深描边**（新增 `DangerStrongBrush`；全仓仅此一处红色悬停定义，5 个危险按钮一次统一）；③ 标题栏三字形 **18 → 22px**（用户选定）；新增门禁「危险按钮悬停不变实心红」「三字形 22px」；构建 0/0、harness **363 PASS**、识图确认菜单双行项正常渲染；已重新发布（dist = 安装副本 MD5 `137145df…`）。**教训**：结构型断言（数 XAML 行数）证明不了渲染，是"截图+识图"抓到的 |
| 141 | `App.xaml` + `MainWindow.xaml` + `_verify-p0/Program.cs` + `docs/UI-DESIGN.md` | **验收第二轮修正（work-log/133）**（用户澄清两处）：① 危险按钮悬停按用户澄清**改回「红底白字（底色反转）」** —— 变更集 140 误改成"只加深描边"，老样式本就是 `DangerBrush` 底 + `OnBrandTextBrush` 字；按下态用新增的 `DangerStrongBrush`；② 标题栏 `×` 比另两个小 —— 新增**墨迹度量工具**（`_measure-glyphs.py`，连通块测像素）实测 22px 下 `—` 28×2 / `□` 13×13 / `×` 11×11，`×` 改 **26px** 后复量 **13×13 与 `□` 等大**；门禁口径由"三字形同号"改为"**按墨迹等大（— 22 / □ 22 / × 26）**"；门禁模式匹配改用**纯字符串**（IndexOf/Substring）不再用正则（避免 CS1009 反复踩坑）；构建 0/0、harness **363 PASS**、识图+度量双重复核；已重新发布（dist = 安装副本 MD5 `24275ecae3ae…`）。**教训**：选择题里没把"用户已认可的现状"列为选项 → 问错了方向 |
| 142 | `VersionControlWindow.xaml` + `_verify-p0/Program.cs` + `capture-ui.ps1` + `docs/UI-DESIGN.md` | **「删除版本」悬停红底红字修复（work-log/134）**：该按钮内联了 `Foreground=DangerBrush`，而 **WPF 本地值优先级高于样式触发器** → 触发器改得动底色、改不动字色（红底红字）；去掉内联值即修复，常态色统一走 `DangerTextBrush`；**门禁升级**为「危险按钮不得内联 `Foreground`/`Background`」（从"样式写对了"→"没有更高优先级覆盖"）；截图工具新增 **hover 步骤/模式**（真实光标触发 `IsMouseOver`）+ `_measure-hover.py` 像素分类：白字 **19.5%** / 红底 **72.9%** / 红字 1.7% ✅；构建 0/0、harness **364 PASS**、发布（MD5 `db4c698e5bde…`） |
| 143 | `MainWindow.xaml.cs` + `VersionSettingsWindow.xaml.cs` + `ExtensionWindow.xaml` + `_verify-p0/Program.cs` + `probe-layout.ps1` | **验收第三轮（work-log/135）**：① 「插件与技能来源」列表（设置→市场源）的删除按钮是**代码动态创建**的普通按钮 → 套 `DangerButton`（并避开 CS0104 用窗口实例 `FindResource`）；② **门禁补 C# 侧盲区**（.cs 里 `Content="删除/卸载/移除/回滚/清理"` 必须有 DangerButton）→ 立刻又抓出第二处（`VersionSettingsWindow.xaml.cs:349` 环境变量行）并修复；③ **Skill 市场卡片统一为插件市场卡片形态**（CardBrush + ItemCornerRadius + Padding 14/Margin 0,0,0,10 + 36px 头像列 + 12px 描述 + 11px 元信息行 + `PrimaryButton(12,6)`；标题不做链接因其模型无 URL）；④ **修既有功能 bug**：Skill 市场列表横向溢出（`CanContentScroll=True` 下虚拟化面板以无限宽度测量条目，长描述把「安装」按钮挤出窗口，实测 x=1554 > 列表右边 1406）→ 两个市场列表条目绑定 `Width` 到列表宽度，复量按钮回到 **x=1315** ✓ 并加门禁；截图归档改到工作区根目录 `screenshots/<轮次>/`；构建 0/0、harness **366 PASS**、发布（MD5 `4a9dd1fc3147…`） |
| 144 | `MainWindow.xaml` + `MainWindow.xaml.cs` + `ExtensionWindow.xaml` + `ExtensionWindow.xaml.cs` + `_verify-p0/Program.cs` | **UI 遗留清账（一）（work-log/137）**：① 提示条**按文字长度自适应消失**（`4s + 字数/30`，上限 12s；「失败/异常/错误/崩溃」类或带 detail 的**常驻**；悬停暂停并按剩余时间继续；右侧 `×` 手动关闭）——真机实测 103 字文案 **7.0s** 消失（公式值 7s）；② **扩展列表 `Delete` 快捷键**（复用 `Remove_Click` 同一流程，仍走二次确认；守门：输入框内不抢键 / 未选中 / 按钮不可用不动）。harness **368 PASS** |
| 145 | `Services/StatusTextStyler.cs`（新增）+ `ConversationWindow` / `VersionControlWindow` / `LogCenterWindow` / `LauncherTaskWindow` / `ExtensionWindow` + `_verify-p0/Program.cs` | **UI 遗留清账（二）（work-log/138）**：三态分色收尾——把 `ExtensionWindow.SetStatusText` 提为**共享 `Services/StatusTextStyler`**（委托复用，11 处调用不变），四页接入（对话 8 处；版本控制 17 处失败转错误色 + 切换历史；日志中心；任务中心），**版本列表补空态**（`HasItems` 触发）；既有门禁断言改指共享实现。harness **370 PASS** |
| 146 | `Services/ScrollMemory.cs`（新增）+ `Services/UiStateStore.cs` + 对话 / 任务 / 日志 / 设置页 + `_verify-p0/Program.cs` | **UI 遗留清账（三）（work-log/139）**：滚动位置记忆扩展到内嵌页——`ui-state.json` 通用字段 `ScrollOffsets` + 共享 `ScrollMemory`（600ms 防抖写盘 / 数据填充后恢复）；**修两处真实缺陷**：设置页换完内容立即 `ScrollToVerticalOffset` 会被忽略（→ 改到下一轮布局后执行）、日志页滚动区需具名 `LogScroll`（原先取"视觉树第一个"可能命中别的控件）。真机播种法取得确定性证据：`page/logs=600` → 日志页打开即 **pct=6.2%** 且存档未被覆盖。harness **372 PASS** |
| 147 | `App.xaml` + `Controls/UiIcon.xaml(.cs)`（新增）+ `MainWindow` / `VersionSettingsWindow` / `LogCenterWindow` / `ExtensionWindow` + `docs/THIRD-PARTY-NOTICES.md`（新增）+ `_verify-p0/Program.cs` | **图标体系改用矢量注册表（work-log/141）**：引入 **Tabler Icons v3.46.0（MIT）** —— `App.xaml` 19 个 `Icon.*` 几何资源 + 共享 `Controls/UiIcon`（颜色随继承前景色、粗细=Size/12、像素对齐）；替换全部 17 处字形（导航 6 / 标题栏 3 / 启动 / 返回 / 空态＋ / 提示条× / 下拉 2 / 版本设置导航 5 / 日志返回 / 扩展外链）；新增 `IconGhostButton` 样式（`PathLinkButton` 模板只渲染文本，放不下图标）；门禁改为"注册表 + 图标阶梯 + 不得裸字形（保留禁 emoji）"；两处真实缺陷已修：Tabler 画布边框路径致图标外多出方框、误删 `IconGhostButton` 致加载期异常窗口不出现；构建 0/0、harness **372 PASS**、识图确认无方框且标题栏三枚等大、最大化/还原像素比对差异 691 px；发布（MD5 `015bcd64…`） |
| 148 | `MainWindow.xaml` + `App.xaml` + `ExtensionWindow.xaml` + `LogCenterWindow.xaml(.cs)` + `_verify-p0/Program.cs` | **图标轮验收反馈修正 + 日志中心一键清理（work-log/142）**：① 标题栏三枚矢量图标 22 → **18px**（用户反馈偏大；矢量墨迹≈Size×0.83，18px 对应≈15px）；② 新增 `IconTextGapTrailing`（8,0,0,0）令牌，修「中文官网 ↗」尾随图标贴字（原 `IconTextGap` 是**右侧**间距，放文字后等于没间距）；③ **日志中心新增「清理日志」按钮** —— 复用既有 `LauncherStorageService.Clean(["crash","old-logs"])` + 二次确认 + `DangerButton`（门禁抓出我最初漏用危险样式）；**当前 launcher.log 刻意保留**（活跃日志），要全清走「存储与清理」；harness **373 PASS**、识图三项确认、清理功能真机实测（`launcher.log.old` 删除 / `crash.log` 清零 / 活跃日志保留）；发布（MD5 `99255783…`） |
| 149 | `LogCenterWindow.xaml(.cs)` + `_verify-p0/Program.cs` | **日志页布局修复 + 清理不阻塞 UI（work-log/143）**：① 头部 Grid 由"文字与按钮叠在同一格"改为**两列**（文字 `*` 换行 / 按钮 `Auto`），修掉第 4 个按钮压住灰色说明文字；② 清理与日志解析移出 UI 线程（`await Task.Run`）——耗时实测 `LauncherStorageService.Scan()` **13,299 ms**、日志解析 1 ms，此前这 13 秒全在 UI 线程；`RefreshData` 拆为 `ApplySnapshot`（渲染）+ 异步加载，清理期间按钮置忙并显示"正在清理日志…"，「刷新」与首次加载同样异步化；harness **375 PASS**；识图确认头部不再重叠 |
| 150 | `LogCenterWindow.xaml` | **日志页按钮排成两行（work-log/144）**：用户验收反馈"四个按钮横向占用太大" → 改为两行右对齐（`[返回设置 \| 刷新]` / `[打开日志目录 \| 清理日志]`，行距 7），横向占用约减半；识图确认两行布局且灰色说明文字仍不被压；构建 0/0、harness **375 PASS** |
| 151 | `LogCenterWindow.xaml` + `_verify-p0/Program.cs` + 新工具 `quick-shot.ps1` / `quick-zoom.py` | **日志页按钮列对齐 + 轻量检查脚本（work-log/145）**：① 150 的两行 `StackPanel` 各自右对齐导致**列边不齐** → 改 **2×2 Grid**（共享 `Auto` 列，同列同宽同边）；门禁断言由写死 `StackPanel` 改为"两列 + 2×2 列对齐 + 后台 I/O"；② 新增 **`quick-shot.ps1 -Page <页名> [-Zoom x,y,w,h]`**：只拍**一个页面**（11 个页名，每步打印结果），替代 18 屏全量截图；裁剪放大交给 `quick-zoom.py`；构建 0/0、harness **375 PASS**、识图确认两行四按钮成列对齐 |
| 152 | `MainWindow.xaml.cs` + `Services/LauncherStorageService.cs` + `_verify-p0/Program.cs` | **存储清理按钮不再卡死界面（work-log/146）**：实测 `Scan()` 13.2 s（99.9% 花在**不清理**的实例数据类别上，且产物界面上根本没用到）而处理器是**同步**调用 ✗ → 改 `async` + `Task.Run`，期间按钮置忙 + 「正在扫描可清理项…」/「正在清理…」；服务侧新增 `CleanAllCleanable()`（只扫一次）替掉确认后的重复扫描。**A/B 实测**：期间 UIA 最大延迟 **11,469 ms → 40 ms**，文案与数字与旧版逐字一致（用户选 A，不改测量口径）|
| 153 | `Services/LauncherStorageService.cs` + `_verify-p0/Program.cs` | **修掉"删除静默失效"（work-log/146）**：`BuildFileCategory` 的 `primary` 取路径列表**第一条**（未判存在），而 `EnumerateCategoryFiles` 只认这一条 → 轮转旧日志（`watchdog.log.1`）与市场目录缓存（`skill-market-cache.json`）**永远删不掉且不报错** ✗，可统计时却遍历全部路径（预览承诺与行为不符）。修法：record 新增可选 `Paths` 保留完整列表、逐条路径清理；门禁补两条断言 + 新增"第一条不存在时仍清理其余"回归用例。真机实测：`已清理 1 个文件/0.0 KB` → `已清理 3 个文件/1168.6 KB`（与清点合计一致），两文件确认删除 |
| 154 | `Services/DetectedRuntimeRegistrationService.cs` + `Services/DshRuntimeDetector.cs` + `MainWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **修掉「主运行根被自动注册成幽灵实例」（work-log/159）**：用户报「打开启动器就有一个来源不明的实例（显示 0.1.5-rc.1），删了再开还在」。根因：扫描器把**配置的安装目录自身**当成合法运行时根（`DshRuntimeDetector.FindKnownPackageRoots` 候选清单首条是 `string.Empty`），而自动注册对「扫到但未登记」的运行时根**无条件** `Register` → `run_time` 那份载荷（`dsh.cmd` + `node_modules\@deepseek-ai\dsh`）每次启动都被重新登记（名字被占就 `(2)` 递增），而**删除只移除登记与 dsh-home、载荷原地不动**；同一时刻的日志可证（`RegisteredAt` = 启动后 1 秒）。修法：`ImportAsync` 新增 `excludedRuntimeRoots` 入参（默认 null = **行为完全不变**），**只在启动/刷新路径** `RefreshDshAsync` 传入「安装目录级」包根（复用 `FindKnownPackageRoots`，可见性改为 `internal`；封装为 `MainWindow.ResolveInstallDirectoryRuntimeRoots`），手动「环境扫描导入」路径**不传**（那是用户明确意图）；排除粒度**只到安装目录级**，**不含** `versions\<版本>\`——那是实例真正该在的位置，不能误伤。顺带消除副作用：每次自动注册还会新建 dsh-home 并从 `~/.dsh` 导入一份数据。另：环境状态（`_dshRuntime`）来自扫描结果、在自动注册**之前**赋值，所以修后启动页仍显示「可用」，不会退化成「未安装」。新增 **4** 条仓库自测（不排除时会登记 = 复刻缺陷 / 排除后不登记 / 不误伤 versions / 无效排除路径被忽略且不报错）；构建 0/0、SelfTest **179 PASS / 0 FAIL**、harness **378 PASS / 0 FAIL / 0 SKIP**（真实 `instances.json` MD5 前后一致）；已发布（dist = 安装副本 MD5 `be91b41e67dead9b4eddaa3893a3f33c`） |
| 155 | `Models/VersionSettingsModels.cs` + `Services/DshInstallService.cs` + `Services/InstanceVersionSwitchService.cs` + `FirstRunSetupWindow.cs` + `MainWindow.xaml.cs` + `NewVersionWindow.xaml(.cs)` + `VersionControlWindow.xaml.cs` + `VersionSwitchWindow.xaml(.cs)` + `tests/DshLauncher.SelfTest/Program.cs` | **版本下载源提成设置项 + 两处弹窗可覆盖（work-log/161）**：`work-log/160` 发现「新建版本 / 更换运行版本 / 导入整合包按需下载」三条路的下载源**写死 `https://registry.npmjs.org`**（本网络慢/失败根因），而「准备运行环境」早已提供国内镜像。改法：新增 `DshDownloadSource` 枚举（**替换**同义的 `FirstRunDownloadSource`，不留两份）+ `LauncherSettingsData.DownloadSource`（字符串落盘、**旧设置文件缺字段 → 默认官方源**，向后兼容）+ `DshInstallService.RegistryFor/DisplayNameFor` 两个映射；设置→运行环境新增「版本下载源」下拉（改动即落盘），「准备运行环境」两个按钮保留一次性语义但**把选择记入设置**成为默认；`NewVersionWindow`/`VersionSwitchWindow` 各加「下载源」下拉（默认跟随设置、**只临时覆盖不回写**）；三条下载调用改走 `RegistryFor(...)`。**不动“要不要下载”的判断逻辑**（160 §五 B 明确不碰）。SelfTest **186 PASS / 0 FAIL**（+7：映射 / 旧文件默认 / 未知写法回落 / 字符串落盘）、harness **378 PASS / 0 FAIL / 0 SKIP**；已发布（dist = 安装副本 MD5 `40432ee641ea18453cf01f8d160f1364`） |
| 156 | `Services/MarketplaceService.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **修掉插件市场「同名不同包被合并成一张卡」导致装不上（work-log/162）**：用户实测在插件市场装 `dsh-genui` 连续 5 次报「npm 仓库没有找到可读取的版本信息。」（任务台账 `launcher-tasks.json` 有据）。根因：`MergeItems` 用 `GetPluginIdentities` 去重，而该函数把**显示名**也当成 npm 包身份（`npm:<name>`）——社区目录里 `lhuans/dsh-genui`（npm `dsh-genui`@0.2.1，★4）与 `omdsh-dev/dsh-genui`（npm `@changfenhuang/dsh-genui`@0.11.0，★461）显示名同为 `dsh-genui` → 被合并成一条，字段各取各的（包名取主条、版本取较大者、Star 取较大者），拼出现实中不存在的 `dsh-genui@0.11.0`，安装前校验按它查 npm 必然查不到。修法：新增 `GetMergeIdentities`（只用**强身份**：npm 包名 / 安装 spec / GitHub 仓库；仅在完全没有强身份时才退回显示名），`MergeItems` 改用它；`GetPluginIdentities` 保留显示名别名，**只服务 `FindInstalledPlugin` 的“已安装匹配”**。按在线目录（3836 条）复算：旧的按名合并会把 **170 组同名、432 条**目录条目并成串味卡片（`dsh-memory` 10 并 1、`dsh-balance` 7 并 1、`dsh-prompt-optimizer` 6 并 1 等），修后 `dsh-genui` 正确拆成两张各自可安装的卡（两包在 npm 上都有 `dsh.bundle.patch`）。新增 4 条自测（复刻 dsh-genui / 同插件跨来源仍合并 / GitHub 仓库身份仍合并 / 显示名别名仍用于已安装匹配）；构建 0/0、SelfTest **190 PASS / 0 FAIL**（+4）、harness **378 PASS / 0 FAIL / 0 SKIP**；已发布（dist = 安装副本 MD5 `d446bda2fa0a3f4c3f874cc9d1e7fc7c`） |
| 157 | `Services/MarketplaceService.cs` + `ExtensionWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **市场目录下载改为自动解压 + 超时放宽（work-log/163）**：用户复测「安装仍报同样的错」→ 查明不是 156 失效，而是**插件市场有缓存就不自动联网**（`ExtensionWindow.xaml.cs:463-471` 的 `if (!hasCache) RefreshMarketplaceAsync()`，**必须手点「刷新目录」**），而目录 `plugins.json` 已涨到 **3.9 MB 明文**，市场 `HttpClient` **没开自动解压**（全仓无 `AutomaticDecompression`）→ 本网络实测明文 20–90 s+ 波动、常超 90 s 源级超时，旧缓存里的坏卡永远换不掉（用户仍点到旧合并卡 `dsh-genui@0.11.0`，安装必败）。改法：新增 `CreateHttpHandler()` 用 `SocketsHttpHandler { AutomaticDecompression = DecompressionMethods.All }`（gzip/deflate/br；仍是默认系统代理、不改既有代理行为），`CreateHttpClient()` 改用它；`SourceTimeout` 与刷新总预算 **90 s → 180 s**。仓库外端到端实测（同配置）：`Accept-Encoding: gzip` 传输 **978,759 B / Content-Encoding: gzip**，`AutomaticDecompression` 下解开为 **3,918,007 B / 1.5 s**。另附更正：162 初稿写的“打开插件市场会自动联网刷新”是错的（有缓存就不联）。SelfTest **192 PASS / 0 FAIL**（+2：handler 开解压 / 代理不变）、harness **378 PASS / 0 FAIL / 0 SKIP**；已发布（dist = 安装副本 MD5 `407f0badd809d50dd14621dab6c25aeb`） |
| 158 | `src/DshLauncher/ExtensionWindow.xaml` + `docs/UI-DESIGN.md` | **插件页按钮尺寸微调（work-log/164）**：用户反馈「插件矩阵 / 手动安装 Plugin 比旁边按钮大」「当前选择栏四个按钮偏大」。UIA 实测（1475×900 @125%，物理 px）：工具条行 1 = 32，行 2 插件矩阵/手动安装 Plugin = **48/48**；「当前选择」启用/禁用/更新 = 40，删除 = **48**（且换行）。根因：`CompactToolbarPrimaryButton` 基于 `PrimaryButton`，**继承 `MinHeight=38`**，在 `WrapPanel` 里把整行撑高，同行次级按钮因默认 `VerticalAlignment=Stretch` 被拉到同高（所以同款的「插件矩阵」也变高）；删除走 `DangerButton`（`MinHeight=38`）故更高。修法：紧凑变体**显式 `MinHeight=0`**（新增 `CompactToolbarDangerButton`），「当前选择」启用/禁用/更新改挂 `CompactToolbarButton`、删除改挂 `CompactToolbarDangerButton`，并删掉内联 Padding/Margin（口径＝UI-DESIGN §按钮「信息卡内多按钮工具条 9,4 + 12px」）；`UI-DESIGN.md` 补记「紧凑变体必须清零 MinHeight」。**不改 `App.xaml` 里 `PrimaryButton`/`DangerButton` 的 `MinHeight=38`**（三级动作按钮统尺寸规则不动）。UIA 复测：工具条与四按钮**全部 32**，四按钮同排一行；构建 0/0、SelfTest **192 PASS / 0 FAIL**、harness **378 PASS / 0 FAIL / 0 SKIP**；已发布（dist = 安装副本 MD5 `2745d1d285d3ff9c62e4460438bc940a`） |
| 159 | `ExtensionWindow.xaml(.cs)` + `VersionSettingsWindow.xaml(.cs)` + `_verify-p0/Program.cs` | **依赖自检迁到实例设置 + 插件卡去掉横向滚动（work-log/165）**（用户两条要求）：① 扩展页工具条「依赖自检」按钮**删除**，功能合并到**实例设置 → 插件管理**「已安装 Plugin」卡头部（新增 `x:Name="DoctorButton"` + `RunDoctor_Click`，复用 `ExtensionService.RunDoctorAsync` 与原发现汇总 / `AppDialog` 文案，执行期间置忙）—— harness 的扩展页 UI 断言由「DoctorButton 存在」改为 **`is null`**，并新增「入口已迁移」门禁。② 插件管理已安装列表卡片过宽：与变更集 143 两个市场列表**同一个坑**——`CanContentScroll=True` 下虚拟化面板按无限宽度 measure，`TextWrapping="Wrap"` 的长描述把条目撑到 **2110px**（视口仅 878px）→ 无意义横向滚动、右侧「启用/禁用/删除」被挤出视口；修法＝条目 `Width` 绑到 `ListBox.ActualWidth` + `ScrollViewer.HorizontalScrollBarVisibility="Disabled"` + 描述 `NoWrap`/`CharacterEllipsis`/`ToolTip`（`Name` 补 `ToolTip`），即「省略后部分、悬停看全文」。③ 顺手修掉同卡头部 `DockPanel` 因 `LastChildFill` 把「刷新」拉伸到约 760px 宽（改 `Grid`：文字 `*` / 按钮 `Auto` 右对齐）。UIA 实测：条目 **2110→878**、`HScrollable` **True→False**；点「依赖自检」实测弹出结果框且 `PluginStatusText` 更新。新增 2 条 harness 门禁；构建 0/0、SelfTest **192 PASS / 0 FAIL**、harness **380 PASS / 0 FAIL / 0 SKIP**；已发布（dist = 安装副本 MD5 `cc4f1cf3e238dfa5bd01e03313647dac`） |
| 160 | `Services/ExtensionService.cs` + `tests/DshLauncher.SelfTest/Program.cs` | **MCP 注入改用 dsh patch 的 `insert` 列表（work-log/170）**：dsh 的 patch 方言里「带 `id` 不带 `insert`」= 按 id 修改已存在条目，找不到目标只 warn + skip ⇒ 启动器自造的 `launcher-mcp-*` 永远匹配不上，整份 `launcher.patch.yml` 等于空操作（**既有缺陷**，dsh 0.1.1-rc.2 起一直如此；用户侧表现为 MCP 工具静默消失、无任何报错）。改为把条目包进 `- insert: [ … ]`，空/全禁用仍写 `[]`；方法放宽为 `internal` 供自测断言。**真机端到端**：用产出的字节起 dsh 0.1.7-alpha.1 → 自写留痕 MCP server 被拉起并完成 `initialize`/`tools/list` 握手（修复前写法完全不被拉起） |
| 161 | `src/DshLauncher/DshLauncher.csproj` + `tests/DshLauncher.SelfTest/DshLauncher.SelfTest.csproj` + `.github/workflows/ci.yml` + `README.md` + `CURRENT_DESIGN.md` + `DEV_STATE.md` + `docs/CHANGESETS.md` + `docs/BEHAVIOR-CHANGES.md` + `_verify-p0/Program.cs` | **发布形态改为框架依赖单文件（work-log/184）**：exe **69.5 MB → 3.61 MB**（实算 3,787,000 B；`dist\` 里只剩一个 exe，顺带用 `AllowedReferenceRelatedFileExtensions=none` 排掉旁边 3 个 WebView2 xml）。`EnableCompressionInSingleFile` 改为按 `SelfContained` 条件开启（FDD + 压缩会报 NETSDK1176）→ `-p:SelfContained=true` 仍能出压缩自包含包（69.50 MB）。自测工程与 harness 工程随之 `SelfContained=false`（否则引用产品工程报 NETSDK1150）。**代价**：目标机需预装 .NET 8 Desktop Runtime (x64)，缺失时由 .NET apphost 弹官方「You must install .NET」对话框（exe 内 UTF-16LE 命中该串 ×2、`aka.ms/dotnet` ×5；该对话框**本机未实测**——需要一台未装 .NET 8 的机器）。取证：应用本体仅 2.0 MB（FDD 目录共 4.4 MB）、未压缩自包含 157.6 MB、trimming 不可用（`PublishTrimmed=true` 报 NETSDK1175）、上游官方包 64.6 MB 且发布参数逐条相同（差异 = 上游 csproj 有 `SatelliteResourceLanguages`；自包含变体加回该行可省 5.5 MB）。验证：构建 0/0、SelfTest **194 PASS / 0 FAIL**、harness **358 PASS / 2 FAIL / 0 SKIP**（A/B：`-p:SelfContained=true` 同样 358/2/0 ⇒ 条数与发布形态无关；2 条 FAIL = 既有 C1 哨兵「上游=4 已装=3 启动器=3」与 C2/C6 已装运行时正则缺失）；FDD 产物起窗口冒烟通过（进程存活、标题 `DSH Launcher`、截图 `screenshots/184-fdd-smoke.png`） |
| 162 | `src/DshLauncher/DshLauncher.csproj` + `tests/DshLauncher.SelfTest/DshLauncher.SelfTest.csproj` + `README.md` + `CURRENT_DESIGN.md` + `DEV_STATE.md` + `_verify-p0/Program.cs` | **发布形态改回自包含（绿色便携版第一步，work-log/186）**：默认 `SelfContained=true` —— .NET 8 运行时进 exe，产物 **67,974,420 B（64.8 MB）单文件**（变更集 161 的框架依赖小包 3.61 MB 仍可用 `-p:SelfContained=false` 出）；同时按上游 v1.2.4 口径加 `SatelliteResourceLanguages=en;zh-Hans;zh-Hant`（只留中英语言资源，实测这一步把 69.50 MB 压到 64.8 MB，与上游 64.6 MB 对齐）。自测工程与 harness 工程随之改回 `SelfContained=true`（NETSDK1150 不变量）。验证：构建 0/0、SelfTest **194 PASS / 0 FAIL**、`dist` 里只有一个 exe、冒烟起窗口通过（PID/标题正常）；小包后路重新实测 3,787,000 B。另：本次只动发布形态与语言资源，**未**改任何运行时逻辑；便携化的后续步骤（便携数据根 / 便携 node / WebView2 固定版）见 work-log/186 分步计划 |
| 163 | `src/DshLauncher/Services/InstanceRegistry.cs` + `src/DshLauncher/Services/ErrorCodes.cs` + `tests/DshLauncher.SelfTest/Program.cs` + `_verify-p0/Program.cs` | **便携数据根：实例 DSH_HOME 加载时重定位（work-log/186）**：原逻辑要求 `DshHome` 必须等于**当前**数据根下的 `instances/<id>/dsh-home`，否则直接抛异常 ⇒ 把整个文件夹拷到别处（或启用 exe 旁 `launcher-data`）后**实例列表加载失败**。改为：仍形如 `<任一数据根>/instances/<本实例 id>/dsh-home` 的路径重写到当前数据根并**落盘** + 记 `E1018`（含 from/to）；结构不匹配的一律照旧拒绝。验证：构建 0/0、SelfTest **198 PASS / 0 FAIL**（+4 用例）、harness **360 PASS / 1 FAIL / 0 SKIP**（唯一 FAIL 为既有 C1 哨兵）；真机端到端：便携根生效（`launcher-data/runtime-cache.json` mtime 改变）+ `E1018` 一条 + `launcher-data/instances.json` 被改写指向便携根 + UI 正常列出实例（截图 `screenshots/186-portable-root-loaded.png`） |
| 164 | `src/DshLauncher/DshLauncher.csproj` + `tests/DshLauncher.SelfTest/DshLauncher.SelfTest.csproj` + `README.md` + `CURRENT_DESIGN.md` + `DEV_STATE.md` + `_verify-p0/Program.cs` | **发布形态定稿：框架依赖小包（work-log/187）**：`SelfContained` true→**false** ⇒ 产物 **3,791,120 B（3.62 MB）**、`dist` 单文件；自包含大包（64.8 MB）仍可用 `-p:SelfContained=true` 出。自测工程与 harness 工程同步改回非自包含（NETSDK1150 不变量）；harness 的 publish 探针改为断言小包参数。验证：构建 0/0、SelfTest **198 PASS / 0 FAIL**、harness **362 PASS / 1 FAIL / 0 SKIP**（唯一 FAIL 为既有 C1） |
| 165 | `src/DshLauncher.Bootstrapper/`（新增：csproj + Program.cs）+ `src/DshLauncher/DshLauncher.csproj`（主程序改名）+ `README.md` + `_verify-p0/Program.cs` | **新增极小自检引导器 + 主程序改名（work-log/187）**：小包缺 .NET 8 Desktop Runtime 时主程序无法自检（起不来），故提醒必须外部化 —— 引导器用 **.NET Framework 4.8**（Win10 1903+/Win11 自带）做成 **22,016 B**，检测 x64 `Microsoft.WindowsDesktop.App` 8.x（注册表优先、目录兜底）：命中则拉起同目录 `DSH Launcher.App.exe`；未命中弹中文对话框（官方下载页 / 复制 `winget install Microsoft.DotNet.DesktopRuntime.8` / 我已安装重试）。含 `--check-only` 与 `--simulate-missing` 两个可测开关。主程序 `AssemblyName` 改为 `DSH Launcher.App`（同目录改名，路径逻辑不受影响）。验证：引导器与主程序构建 0/0、`--check-only` 退出码 0 且报 `desktop-runtime=8.0.31`、`--simulate-missing` 弹窗截图、dist 与安装目录端到端拉起主程序均通过；harness 新增两条 bootstrap 探针通过 |
| 166 | `src/DshLauncher/Services/DshInstallService.cs` + `tests/DshLauncher.SelfTest/Program.cs` + `_verify-p0/Program.cs` | **修掉「装版本会装出坏树」（work-log/188）**：原 `npm install --global` + `NPM_CONFIG_PREFIX` 装出来的依赖树深度嵌套——240 个 `@deepseek-ai/*` 包里有 **120 个从 dsh 安装锚点不可达**，dsh 解析层文件插件名随之失败（实例崩在 `plugin(s) failed to load: @deepseek-ai/dsh-sandbox-local`）。改为**非 global 安装**（`install <spec>` + `WorkingDirectory` = 安装目录）+ 装完 `WriteRuntimeCommandShims` 补 `<安装目录>/dsh.cmd`（改写 `.bin` shim 的目标路径，找不到则自建骨架）+ `FindUnreachableLayerPackages` **可达性回归门**（不合格即报失败而不是留坑）。验证：构建 0/0、SelfTest **203 PASS / 0 FAIL**（+5）、harness **363 PASS / 1 FAIL / 0 SKIP**（唯一 FAIL 为既有 C1）；真实 npm 安装（npmmirror）**240 包 / 0 不可达**、`.bin/dsh.cmd --version` → 0.1.5-rc.2；新 exe 已部署并冒烟通过 |
| 167 | `src/DshLauncher/Services/SessionFileNames.cs` + `tests/DshLauncher.SelfTest/Program.cs` + `docs/DSH_CONTRACT_INVENTORY.md` + `docs/BEHAVIOR-CHANGES.md` + `_verify-p0/Program.cs` | **会话格式常量 3 → 4（work-log/190）**：上游 `SESSION_FORMAT_VERSION` 已到 4（v3→v4 只改头部 version，其余头部字段不变，变动在记录体：工具角色结果/producer 来源/**父目录补全**/引用重映射）。启动器不读记录体、只按头部 version 命名文件，故只需放开已核对范围；同时修正哨兵语义：harness 的 C1 由「已装 == 常量」改为「上游 ≤ 常量 且 已装 ≤ 常量」（旧写法只在已装=常量时成立）。自测新增 v4 用例：v4 文件名解析/拼名、目录最高代际取 4、**v4 会话列表项 `GenerationVersion = 4` 且头部有效**。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **364 PASS / 0 FAIL / 0 SKIP**（**长期挂着的 C1 FAIL 转为 PASS**：`上游=4 已装=3 启动器已核对=4`） |
| 168 | `src/DshLauncher.Bootstrapper/RuntimeLocator.cs`（新增）+ `PortableRuntimeInstaller.cs`（新增）+ `Program.cs` + `DshLauncher.Bootstrapper.csproj` + `_verify-p0/Program.cs` | **引导器扩成"三条路"，含一键下载便携运行时（work-log/193）**：① `<exe 目录>/runtime/dotnet`（便携，可用则优先，注入 `DOTNET_ROOT`/`DOTNET_ROOT_X64`/`DOTNET_MULTILEVEL_LOOKUP=0` 后拉起主程序）② 系统级 ③ `%USERPROFILE%/.dotnet`；都没有时对话框新增「下载便携运行时（免管理员）」——读官方 `release-metadata/8.0/releases.json` 取最新 8.0.x 的两份 win-x64 zip（`dotnet-runtime` 含宿主、`windowsdesktop-runtime` 含 WPF），下载→解压到 staging→校验（8.x 桌面框架 + host/fxr）→原子替换，全程免管理员不写注册表。CLI 新增 `--install-portable-runtime`、`--print-release-urls`，`--check-only` 增打 `source`/`dotnet-root`。验证：引导器 0/0、产物 36,864 B；便携路对照实验（coreclr 从便携目录加载）、真实下载 66.9 MB/16 秒得到 162 MB 运行时并启动成功；harness **365 PASS / 0 FAIL / 0 SKIP** |
| 169 | `src/DshLauncher/DshLauncher.csproj` + `README.md` + `CURRENT_DESIGN.md` + `DEV_STATE.md` + `_verify-p0/Program.cs` | **发布形态定稿：退役默认自检引导器，回到单个框架依赖 exe（work-log/195）**：`AssemblyName` 由 `DSH Launcher.App` 改回 **`DSH Launcher`**，入口名与上游 Release 一致（同目录改名，`AppContext.BaseDirectory` 不变 ⇒ run_time / launcher-data / WebView2 路径逻辑不受影响）；缺 .NET 8 Desktop Runtime 的提醒改由 **.NET apphost 的系统官方对话框**负责（"You must install .NET" + 下载链接）。变更集 165–168 的引导器（中文提醒 / 三条路找运行时 / 一键下载便携运行时）**源码保留为可选形态**，要发两个文件版本时：主程序发布加 `-p:AssemblyName="DSH Launcher.App"` + 单独 build 引导器并改名。harness 门禁同步改为断言 `<AssemblyName>DSH Launcher</AssemblyName>` 且否定 `.App`。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **380 PASS / 1 FAIL / 3 SKIP**（唯一 FAIL = 已安装运行时前置缺失，因本机 `run_time` 于变更集外已删；新门禁 PASS）、`dist` 重建为**单文件** `DSH Launcher.exe` 3,795,192 B / MD5 `be8435f66f7dcdb9912aecb460c13207`，起窗口冒烟通过（进程名/标题 `DSH Launcher`，数据根已用 `DSH_LAUNCHER_DATA_ROOT` 重定向到临时目录）。**未覆盖**：真缺 .NET 8 的机器上系统对话框（只按产物内 UTF-16LE 字符串取证）；**未替换安装副本**（用户口径） |
| 170 | `src/DshLauncher/VersionSettingsWindow.xaml(.cs)` + `src/DshLauncher/ExtensionWindow.xaml` + `src/DshLauncher/MainWindow.xaml.cs` + `Services/ProviderDiagnosticService.cs`（删除）+ `docs/FEATURE-INVENTORY.md`（新）+ `.github/workflows/ci.yml` + 四份文档 | **S1 入口去重与启动方式回归（2026-09-24）**：① 新增 `docs/FEATURE-INVENTORY.md`（12 域 / 85 L2 / 281 L3 + 完整性四口径判定 + 缺口清单 + 布局建议，本次审计的依据）；② 通过审计发现并修掉 CI 会把 22 KB 引导器复制覆盖主程序（`ci.yml` 末两步删除）；③ 修正 `VERIFICATION.md` 的 `DOTNET_ROOT` 前置、`CURRENT_DESIGN.md` 三处页面描述、契约清单 C18 保留号与最新核对信息；④ 删除全仓零引用的 `ProviderDiagnosticService.cs`（Provider 诊断已从界面移除）；⑤「默认启动方式」下拉回到「实例设置 → 个性化」（Web / Desktop / 隔离 / 未设置，与实例卡片 ▼ 菜单共用同一份 `OpenMode`，只改启动方式、不触发同步、不建快照）；⑥ 重复入口互相跳转：实例设置→插件管理新增「去扩展页」，实例设置→配置新增「去设置 → 常规」，设置→常规新增「打开该版本的实例设置」（跳转直接落在“常规”分类）。`VersionSettingsWindow` 新增三个可选回调（`openExtensions` / `openSettingsSync` / `launchModeChanged`），`MainWindow` 新增 `_pendingSettingsCategory`。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **388 PASS / 0 FAIL / 0 SKIP**（含 2 条新增 S1 门禁）。**未覆盖**：新下拉与两个跳转按钮的**真机 UIA 目视**（会触碰真实实例数据与同步，未做；见 `docs/FEATURE-INVENTORY.md` §5）。 |
| 171 | `src/DshLauncher/ExtensionWindow.xaml` | **两栏页左栏口径统一为 320+18（2026-09-24，用户拍板；回退变更集 134 的自适应）**：扩展页左栏 `0.26*`（MinWidth 260 / MaxWidth 380）→ **固定 `320` + 18 间隙**，与主窗启动页 / 版本控制 / 实例设置 / 设置页一致；市场与 Skill 列表继续占满右侧剩余宽度（左栏在实际窗口宽度 1180–1900 下变化约 ±60px，未涉及任何绑定到左栏宽度的代码）。harness 的「扩展页侧栏自适应」门禁同步改写为「两栏页左栏统一 320+18（不再用比例侧栏）」。依据：`docs/FEATURE-INVENTORY.md` P3（S2）——审计发现的两套口径，用户 2026-09-24 重新确认后选择统一（已知会回退 134）。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **388 PASS / 0 FAIL / 0 SKIP**。**未覆盖**：扩展页在 1180×720 / 最大化下的真机截图目视（打开扩展页需真实实例） |
| 172 | `docs/UI-DESIGN.md` + `VersionSettingsWindow.xaml` + `PackVersionMismatchWindow.cs` + `MainWindow.xaml.cs` + `ExtensionWindow.xaml` + `Services/ErrorCodes.cs` | **B4 术语与文案统一（2026-09-24）**：① `docs/UI-DESIGN.md` 新增「术语与文案」小节——术语表（实例 / 版本 / 运行时 / DSH_HOME / Plugin / Skill / MCP / Agent Preset / profile / Provider / 市场 / 快照 / 整合包，含本次实测用量：“实例” 761、“Plugin” 755 vs “插件” 333、“Skill” 401 vs “技能” 22、Provider 259 vs “供应商” 0、`DSH_HOME` 144 vs `dsh-home` 26）+ **四条写法规则**（对象名保留英文原形 / 复合词与叙述用中文但不同句混指 / 按钮＝动词+宾语且同类同动词 / 错误码文案与 `ErrorCodes` 一致且保留号不复用）。② 按规则改掉 4 处实际不一致：Node 路径选择按钮 `浏览…` → **`选择文件`**；整合包版本不符弹窗的目录选择 `选择…` → **`选择文件夹`**；实例守护卡片的裸 `保存` → **`保存守护设置`**；扩展页提示句里 Plugin 与“插件”同句混指 → 统一。③ `Services/ErrorCodes.cs` 补 `E1007` 保留号注释（实测：26 个常量全部被引用，无孤儿引用）。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **389 PASS / 0 FAIL / 0 SKIP**（+1 条“术语与文案规范已建立”门禁）。 |
| 173 | `App.xaml` + `ConversationWindow.xaml` + `LauncherTaskWindow.xaml` + `PluginMatrixWindow.xaml.cs` + `docs/UI-DESIGN.md` | **B3a 表格列头与行高统一（2026-09-24）**：`App.xaml` 新增两个 **keyed** 样式——`TableColumnHeaderStyle`（GridViewColumnHeader：`PanelBackgroundBrush` 底 + `LineBrush` 下边框 + `MutedBrush` 字 + 12px SemiBold + Height 34 + Padding 10,0，**不重写 Template** 以保留拖拽/宽度调整手柄）与 `TableRowStyle`（ListViewItem：12px、MinHeight 34、Padding 10,6、Stretch，悬停 `HoverSurfaceBrush` / 选中 `SubtleSurfaceBrush`）；按本仓禁令**不用隐式样式、不用 `BasedOn={StaticResource {x:Type ...}}`**（变更集 134/138 因此崩过两次）。挂载：对话页 3 张表 `ColumnHeaderContainerStyle` + `ItemContainerStyle`；任务页行样式改 `BasedOn` 指向 keyed 样式；插件矩阵的动态列在代码里 `TryFindResource` 挂。`UI-DESIGN.md`「数据表」小节将“列头与行高统一：待做”改为已完成做法 + 记录一条**既有缺陷**（任务页行三列重叠，旧截图已存在）。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **390 PASS / 0 FAIL / 0 SKIP**（+1 条样式门禁）+ 真机截图目视（`_verify-p0/screenshots/quick/b3a-tasks.png` 行/选中态、`b3a-matrix.png` 列头与分隔线）。 |
| 174 | `ConversationWindow.xaml.cs` + `PluginMatrixWindow.xaml.cs` + `docs/UI-DESIGN.md` | **B3b 列头排序（2026-09-24）**：只开**插件矩阵**（插件名 / 版本 / 各实例状态列）与**对话列表**（名称 / 实例 / 更新时间 / 大小 / 格式 / 代际）——任务页行用 `ItemTemplate`、无 `GridView` 列头；检索结果（已按命中数）与备份（已按时间）有固定语义，不参与。实现：`AddHandler(GridViewColumnHeader.ClickEvent, …)` + 列序→排序键表 + 再点同列反向；未点列头时**保持服务层默认顺序**；排序方向写进**状态行文字**（“，按 插件 名称升序（点列头排序）”、“；按 更新时间降序”）。**为什么不做列头箭头**：`GridViewColumnHeader` 无 `SortDirection` 属性（实测写进 `ControlTemplate.Triggers` 直接 `MC4109` 编译失败），要箭头必须自定义 `HeaderTemplate` + 附加属性；而且本仓已把图标收归 `UiIcon` 矢量注册表，不应再往列头文字塞字形。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **391 PASS / 0 FAIL / 0 SKIP**（+1 条排序门禁）+ **真机 UIA 实测**（给 `quick-shot.ps1` 新增 `matrix-sort` 页与 `header\|` 步骤：点“插件”列头后状态行确为“按 插件 名称升序”，截图 `b3b-matrix-sort.png`）。**未覆盖**：**多行时的实际重排顺序**未目视（本机矩阵只有 1 行；对话列表排序未做真机验证，因为打开对话页会触发跨版本会话同步） |
| 175 | `Services/DshInstallService.cs` + `Services/InstanceRuntimeRebinder.cs` + `Services/InstanceVersionSwitchService.cs` + `_verify-p0/Program.cs` | **修掉「精确版本被装进共享运行根」+ 重绑失败可归因（work-log/198）**：用户报「无法建立新实例或切换已有实例版本」，实际报错 `npm 安装完成，但没有找到 DSh 0.1.7-rc.2 的有效运行目录。`。根因：`InstallExactVersionAsync` 只把 npm 的 `WorkingDirectory` 设为 `<安装根>\versions\.<版本>.install-<guid>`，而 **npm 的项目根不是 cwd，而是就近向上第一个含 `package.json`/`node_modules` 的祖先** —— 变更集 166 改成非 global 安装后 `<安装根>\package.json` 必然存在 ⇒ 安装**落进共享运行根**（实测 npm 日志 568 条 reify 路径全在 `run_time\node_modules\…`、暂存目录留空），于是 `InstalledVersionMatches(暂存目录)` 必假、`versions\<版本>` 永远建不出来，且共享运行根被静默换版本（本机 `run_time` 一天内 rc.1 → rc.2 → alpha.2，实例登记却是 rc.1）。修法（A+B+守卫）：① 新增 `PrepareStagingProject` 给暂存目录写最小 `package.json`；② npm 命令加 `--prefix "<暂存目录>"` 显式钉住项目根；③ 安装前后对「会被顶掉的祖先项目根」取 `package.json`+`package-lock.json` 指纹，被改即报失败并写明现场（不留半成品）。另：`InstanceRuntimeRebinder` 的 6 条守卫提成 `DescribeBlocker`（两处判定共用一处），`SwitchAsync` 的失败文案从只有「目标运行时不完整，未执行切换。」改为**带具体守卫**；并把预检的运行判断统一到 `_isRunning` 单一来源（对象上陈旧的 `RuntimeStatus=Running` 先就地收敛，不再出现「预检放行、重绑却拒绝」）。验证：构建 0/0、SelfTest **204 PASS / 0 FAIL**、harness **395 PASS / 0 FAIL / 0 SKIP**（+4 门禁）；沙箱前后对照（真实 npm + 真实产品代码，`D:\_probe`）：修前 `IsSuccess=False`+`versions\0.1.7-rc.2` 不存在+安装根被改，修后 `IsSuccess=True`+版本目录建成（283 层包 / 0 不可达、`dsh.cmd --version` = 0.1.7-rc.2）+安装根指纹未变；根级安装（「准备运行环境」）路径同样实跑通过。**未覆盖**：安装副本未换（用户口径），真机 UI 点击流程未走 |
| 176 | `Services/ScheduleSnapshotService.cs`（新）+ `Services/ErrorCodes.cs` + `Models/ManagerInstance.cs` + `MainWindow.xaml.cs` + `MainWindow.xaml` + `VersionControlWindow.xaml` + `VersionSwitchWindow.xaml.cs` + `tests/DshLauncher.SelfTest/Program.cs` + `_verify-p0/Program.cs` | **定时提醒 → 关闭防呆 + 卡片只读提示（上游集成计划 A，work-log/200）**：dsh 0.1.7-rc.1 起 `web` profile 默认挂载 Schedule，提醒存于 `<DSH_HOME>/storages/schedule/tasks/*.json`，而启动器此前对它**一无所知** ⇒ 空闲自动停止会静默掉用户的提醒。新增只读 `ScheduleSnapshotService`（storage-json 的 per-record 布局、域版本 1；**读不到/看不懂一律降级为「未知」**，绝不阻拦实例操作）+ 四处接线：① 用户主动停止（启动页按钮 / 托盘菜单）先确认；② **空闲自动停止遇到待执行提醒直接跳过**并记 `E1019`；③ 退出启动器的既有确认里补一句「其中 N 个实例还有 M 个定时提醒待执行」；④ 更换运行版本的检查报告里列出提醒条数与最近一次时间。实例卡片（启动页 + 版本控制页）只读显示「提醒 N · 下次 …」（`ManagerInstance.ScheduleSummaryText`，空串整行收起）。**只读**：不写上游存储、不做提醒新建/编辑/删除。验证：构建 0/0、SelfTest **212 PASS / 0 FAIL**（+8）、harness **398 PASS / 0 FAIL / 0 SKIP**（+2 门禁 + C20 契约哨兵） |
