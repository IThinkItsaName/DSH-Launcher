# 行为 × 验证方式矩阵

> 目的：一眼看清"哪些行为有自动化保护、哪些只能人工验收、哪些没覆盖"。
> 图例：**ST** = SelfTest（`tests/DshLauncher.SelfTest`，133 条）；**HV** = harness（`_verify-p0`，含 `--ui`，348 条）；
> **人工** = 需真人点击/观察（系统文件对话框、渲染宿主、模态确认等自动化不友好的路径）；**⛔** = 无覆盖（已知缺口）。
> 维护规则：新增/修改行为时，先在这里登记"打算怎么验证"，再实现（work-log/27 #23）。

## 一、实例与运行时

| 行为 | ST | HV | 人工 | 备注 |
|---|---|---|---|---|
| 实例注册/注销/唯一名 | ✅ | | | `InstanceRegistry` 自测 |
| 启动/停止 + 就绪 URL 识别 | | ✅ | | harness 跑真实运行时 |
| 运行版本切换（含降级警告、会话导出） | ✅ | ✅ | | harness e2e（`e2e-version-switch*.ps1`） |
| 多代际会话归并 | ✅ | ✅ | | 变更集 78 |
| 隔离启动（A2） | | ✅ | ✅ | 需真实起进程 |
| 关闭防呆（A5） | | | ✅ | 模态确认；自动化开关 `DSH_LAUNCHER_SUPPRESS_CLOSE_CONFIRM=1` |
| 实例数据不被测试触碰 | ✅ | ✅ | | ADR-001 |

## 二、整合包（#24）

| 行为 | ST | HV | 人工 | 备注 |
|---|---|---|---|---|
| 容器/manifest v2–v5 解析与校验 | ✅ | | | `DshPackFormat` + `DshPackArchive` |
| 导入（新建实例、权威重建、整体回滚） | ✅ | | | 不变量：instances 目录数 == 台账数 |
| `files[]` 下载（默认拒绝、sha256+size、镜像回退） | ✅ | | ⛔ | 真实下载未自动化（需网络与真实包） |
| 导出 v4 + `.dspack` v2（含往返） | ✅ | | | 导出→自家导入器读回 |
| 旧 v1（`.dshpack`）导入 | ✅ | | ✅ | 修复见 work-log/79 前段；面板为窗口内自绘 |
| 导入向导面板（含下载同意勾选） | | | ✅ | 系统文件对话框无法自动化 |
| 版本不符「先提示再选择」 | ✅(纯逻辑) | | ✅ | `PackTemplateResolution` + 面板需人工 |

## 三、安全（#20）

| 行为 | ST | HV | 人工 | 备注 |
|---|---|---|---|---|
| 凭据体检：只报位置、默认无预览 | ✅ | | | 反证 A/B/C/D |
| 脱敏预览（显式开启，前 3+后 4） | ✅ | | | 中段特征串必须查不到 |
| 危险配置检查（8 项 + 双向自校准） | ✅ | | | 安全样本零发现 / 危险样本全命中 |
| 体检纯读取（不调 dsh CLI） | ✅ | | | 反证 B/E（ADR-004） |
| 探针时间轴（`raw` 永不读取） | ✅ | | | 反证 F/G |
| 权限键位/环境变量/遥测域哨兵 | | ✅ | | C14–C16（读已安装运行时出厂层） |
| 体检导出物保留 20 份 + 存储清理 | ✅ | | | `audit-exports` 分类 |
| 真实数据体检（本机实例） | | | ✅ | 2026-09-11 已实测：命中位置、无值回显 |

## 四、界面与交互

| 行为 | ST | HV | 人工 | 备注 |
|---|---|---|---|---|
| 设置导航 5 类顺序 | | ✅ | | 变更集 89 |
| 导入实例下拉（含整合包项 + 样式） | | | ✅ | 菜单外观需肉眼 |
| 菜单分隔线样式 | | | ✅ | 修"白线左右顶满" |
| 市场：搜索/筛选/分页追加 | | | ⛔ | 追加逻辑**已搁置**（work-log/79；诊断日志默认关，`DSH_LAUNCHER_MARKET_SCROLL_DIAG=1` 开） |
| 场景：WebView2 缓存清理（A4） | | | ✅ | 下次启动执行 + 桌面窗口探针 |
| 页面错误文本不截断（A1） | ✅ | | | 含事故原文回归断言 |
| 重复入口互相可达（插件管理 / 版本同步，S1） | | ✅ | | 变更集 170；结构门禁 `p1/ui: …互相可达（S1）`；跳转手感需肉眼 |
| 实例设置可改默认启动方式（S1） | | ✅ | | 变更集 170；门禁 `p1/ui: 实例设置可改默认启动方式…`；与卡片 ▼ 菜单同一份数据 |
| profile 卫生检查 + 一键复位（A3） | ✅ | | ✅ | 复位会写配置 → 人工执行且先快照 |
| 托盘/任务栏图标 | | | ✅ | 属 Windows 外壳行为（今天经历图标缓存问题） |

## 五、工程与契约

| 行为 | ST | HV | 人工 | 备注 |
|---|---|---|---|---|
| 契约哨兵 C1–C19 | | ✅ | | 上游克隆缺失时 SKIP 不误报 |
| 真实运行时比对 | | ✅ | | 已安装运行时 vs 启动器预期 |
| 日志隔离（`DSH_LAUNCHER_LOG_ROOT`） | ✅ | ✅ | | ADR-001 |
| 发布一致性（dist == 安装副本 MD5） | | | ✅ | ADR-010 固定 SOP |
| 联网类自测（zh1024/dshfind 真实拉取） | ⚠️ | | | **已知隐患**：测试会真连外网（可能影响用户），待改可注入/离线（work-log/79） |

## 六、已知无覆盖（待补）

1. **市场滚动追加**：已搁置（用户决定）；若重启议题，先读诊断日志再改（ADR-008）。
2. **真实第三方 `.dspack`**：手上无样本，仅自造样本 + 往返覆盖（work-log/70 §G）。
3. **`files[]` 真实下载**：需要网络 + 真实包，未自动化。
4. **联网自测的离线化**：待做（改注入式 fetch）。
5. **托盘/任务栏/DPI 等外壳行为**：只能人工（含 125%/150% DPI 复核，见 UI TODO 1.11）。
